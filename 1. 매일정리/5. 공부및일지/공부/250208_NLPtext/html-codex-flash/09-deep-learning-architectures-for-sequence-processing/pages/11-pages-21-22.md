# Page 21

## Block 1
- type: other
- source_text: 9.7 • SELF-ATTENTION NETWORKS: TRANSFORMERS 21
- korean_translation: 9.7 • 자기 주의 집중 네트워크: 트랜스포머 21
- study_note: 9.7 섹션의 연속된 내용과 페이지 번호입니다.

## Block 2
- type: paragraph
- source_text: that follow the query. This is inappropriate in the setting of language modeling since guessing the next word is pretty simple if you already know it. To fix this, the elements in the upper-triangular portion of the matrix are zeroed out (set to −∞), thus eliminating any knowledge of words that follow in the sequence. Fig. 9.17 depicts the QK^T matrix. (we’ll see in Chapter 11 how to make use of words in the future for tasks that need it).
- korean_translation: [이전 페이지에서 계속] ...쿼리 다음에 오는 단어들입니다. 언어 모델링 상황에서 다음 단어를 이미 알고 있다면 이를 추측하는 것은 너무나 쉽기 때문에, 이는 부적절합니다. 이를 해결하기 위해 행렬의 상삼각형(upper-triangular) 부분의 요소들을 0으로 만듭니다($-\infty$로 설정). 이를 통해 시퀀스에서 뒤따라오는 단어들에 대한 지식을 제거합니다. 그림 9.17은 $QK^T$ 행렬을 묘사합니다. (미래의 단어들을 활용해야 하는 과제들에 대해 어떻게 이를 사용하는지는 11장에서 살펴볼 것입니다).
- study_note: 언어 모델링 시 미래 정보를 보지 못하게 하는 '마스킹(Masking)'의 필요성과 방법을 설명합니다.

## Block 3
- type: figure
- source_text:
  N x N Matrix (QK^T)
  Rows: queries q_1 to q_5
  Cols: keys k_1 to k_5
  Upper triangle: -inf (masked)
  Lower triangle: valid scores (q_i * k_j)
- korean_translation:
  N x N 행렬 (QK^T)
  행: 쿼리 q_1 ~ q_5
  열: 키 k_1 ~ k_5
  상삼각형 영역: -inf (마스킹됨)
  하삼각형 영역: 유효한 점수들 (q_i * k_j)
- study_note: 인과적 자기 주의 집중을 위한 마스킹된 점수 행렬입니다. $j > i$인 영역을 $-\infty$로 채워 소프트맥스 후 확률이 0이 되게 만듭니다.

## Block 4
- type: figure_caption
- source_text: Figure 9.17 The N × N QT^T matrix showing the q_i · k_j values, with the upper-triangle portion of the comparisons matrix zeroed out (set to −∞, which the softmax will turn to zero).
- korean_translation: 그림 9.17 $q_i \cdot k_j$ 값들을 보여주는 $N \times N$ $QT^T$ 행렬. 비교 행렬의 상삼각형 부분은 0으로 처리되었습니다($-\infty$로 설정되어 소프트맥스를 거치면 0이 됨).
- study_note: 그림 9.17에 대한 캡션입니다. (원문의 $QT^T$는 오타로 보이며 $QK^T$를 의미합니다)

## Block 5
- type: paragraph
- source_text: Fig. 9.17 also makes it clear that attention is quadratic in the length of the input, since at each layer we need to compute dot products between each pair of tokens in the input. This makes it extremely expensive for the input to a transformer to consist of long documents (like entire Wikipedia pages, or novels), and so most applications have to limit the input length, for example to at most a page or a paragraph of text at a time. Finding more efficient attention mechanisms is an ongoing research direction.
- korean_translation: 그림 9.17은 또한 주의 집중 연산이 입력 길이의 제곱($quadratic$)에 비례한다는 점을 명확히 해줍니다. 매 층마다 입력의 모든 토큰 쌍 사이의 내적을 계산해야 하기 때문입니다. 이로 인해 전체 위키피디아 페이지나 소설과 같이 긴 문서를 트랜스포머의 입력으로 사용하는 것은 매우 비용이 많이 들게 되며, 따라서 대부분의 애플리케이션에서는 입력 길이를 한 번에 최대 한 페이지나 한 단락 정도로 제한해야 합니다. 더 효율적인 주의 집중 메커니즘을 찾는 것은 현재 진행 중인 연구 방향입니다.
- study_note: 어텐션의 연산 복잡도($O(N^2)$) 문제를 짚어주며 그로 인한 실무적 한계를 설명합니다.

## Block 6
- type: section_title
- source_text: 9.7.1 Transformer Blocks
- korean_translation: 9.7.1 트랜스포머 블록
- study_note: 어텐션 층을 포함하여 실제 모델을 구성하는 기본 단위인 트랜스포머 블록의 전체 구조를 다룹니다.

## Block 7
- type: paragraph
- source_text: The self-attention calculation lies at the core of what’s called a transformer block, which, in addition to the self-attention layer, includes additional feedforward layers, residual connections, and normalizing layers. The input and output dimensions of these blocks are matched so they can be stacked just as was the case for stacked RNNs.
- korean_translation: 자기 주의 집중 계산은 이른바 '트랜스포머 블록'의 핵심에 자리 잡고 있습니다. 트랜스포머 블록은 자기 주의 집중 층 외에도 추가적인 피드포워드 층, 잔차 연결(residual connections), 그리고 정규화 층들을 포함합니다. 이러한 블록들의 입출력 차원은 서로 맞춰져 있어서, 스택형 RNN의 경우와 마찬가지로 층층이 쌓아 올릴 수 있습니다.
- study_note: 트랜스포머 블록의 구성 요소와 계층적 구조 가능성을 설명합니다.

## Block 8
- type: paragraph
- source_text: Fig. 9.18 illustrates a standard transformer block consisting of a single attention layer followed by a fully-connected feedforward layer with residual connections and layer normalizations following each. We’ve already seen feedforward layers in Chapter 7, but what are residual connections and layer norm? In deep networks, residual connections are connections that pass information from a lower layer to a higher layer without going through the intermediate layer. Allowing information from the activation going forward and the gradient going backwards to skip a layer improves learning and gives higher level layers direct access to information from lower layers (He et al., 2016). Residual connections in transformers are implemented by added a layer’s input vector to its output vector before passing it forward. In the transformer block shown in Fig. 9.18, residual connections are used with both the attention and feedforward sublayers. These summed vectors are then normalized using layer normalization (Ba et al., 2016). If we think of a layer as one long vector
- korean_translation: 그림 9.18은 단일 주의 집중 층과 그 뒤를 잇는 완전 연결 피드포워드 층으로 구성된 표준 트랜스포머 블록을 보여주며, 각각의 뒤에는 잔차 연결과 층 정규화(layer normalization)가 따릅니다. 우리는 이미 7장에서 피드포워드 층을 보았습니다만, 잔차 연결과 층 정규화는 무엇일까요? 깊은 네트워크에서 잔차 연결이란 중간 층을 거치지 않고 하위 층에서 상위 층으로 정보를 전달하는 연결입니다. 활성화 정보가 앞으로 나갈 때와 그래디언트가 뒤로 갈 때 층을 건너뛸 수 있게 함으로써 학습을 개선하고 상위 레벨의 층들이 하위 층의 정보에 직접 접근할 수 있게 해줍니다(He et al., 2016). 트랜스포머의 잔차 연결은 한 층의 입력 벡터를 출력 벡터에 더한 뒤 앞으로 전달하는 방식으로 구현됩니다. 그림 9.18에 나타난 트랜스포머 블록에서 잔차 연결은 주의 집중 및 피드포워드 서브레이어 모두에서 사용됩니다. 이렇게 합쳐진 벡터들은 층 정규화(Ba et al., 2016)를 사용하여 정규화됩니다. 만약 우리가 한 층을 하나의 긴 벡터라고 생각한다면... [다음 페이지에서 계속]
- study_note: 모델 학습을 돕는 두 가지 핵심 기술인 잔차 연결(Residual Connection)과 층 정규화(Layer Norm)의 도입 배경과 작동 원리를 상세히 설명합니다.

# Page 22

## Block 1
- type: other
- source_text: 22 CHAPTER 9 • DEEP LEARNING ARCHITECTURES FOR SEQUENCE PROCESSING
- korean_translation: 22 제9장 • 시퀀스 처리를 위한 딥러닝 아키텍처
- study_note: 페이지 상단 헤더입니다.

## Block 2
- type: figure
- source_text:
  Transformer Block Structure:
  Input {x_1, x_2, ..., x_n}
  -> Self-Attention Layer (+ Residual)
  -> Layer Normalize
  -> Feedforward Layer (+ Residual)
  -> Layer Normalize
  -> Output {y_n}
- korean_translation:
  트랜스포머 블록 구조:
  입력 {x_1, x_2, ..., x_n}
  -> 자기 주의 집중 층 (+ 잔차 연결)
  -> 층 정규화(Layer Normalize)
  -> 피드포워드 층 (+ 잔차 연결)
  -> 층 정규화(Layer Normalize)
  -> 출력 {y_n}
- study_note: 단일 트랜스포머 블록의 내부 구성을 보여주는 순서도입니다. 어텐션과 피드포워드 층이 각각 잔차 연결 및 층 정규화와 짝을 이루어 배치됨을 알 수 있습니다.

## Block 3
- type: figure_caption
- source_text: Figure 9.18 A transformer block showing all the layers.
- korean_translation: 그림 9.18 모든 층들을 보여주는 트랜스포머 블록.
- study_note: 그림 9.18에 대한 캡션입니다.

## Block 4
- type: paragraph
- source_text: of units, the resulting function computed in a transformer block can be expressed as:
- korean_translation: [이전 페이지에서 계속] ...유닛들의 집합으로 생각한다면, 트랜스포머 블록에서 계산되는 결과 함수는 다음과 같이 표현될 수 있습니다.
- study_note: 트랜스포머 블록 연산의 수식화입니다.

## Block 5
- type: formula
- source_text:
  z = LayerNorm(x + SelfAttn(x))
  y = LayerNorm(z + FFNN(z))
- korean_translation:
  $z = \text{LayerNorm}(x + \text{SelfAttn}(x))$
  $y = \text{LayerNorm}(z + \text{FFNN}(z))$
- study_note: 트랜스포머 블록의 최종 연산 공식(9.37, 9.38)입니다. $x + \text{SelfAttn}(x)$ 부분이 잔차 연결을 의미합니다.

## Block 6
- type: margin_note
- source_text: layer norm
- korean_translation: 층 정규화(layer norm)
- study_note: 각 유닛의 활성화 값을 평균과 표준편차를 이용해 정규화하여 학습의 안정성을 높이는 기법입니다.

## Block 7
- type: paragraph
- source_text: Layer normalization (or layer norm) is one of many forms of normalization that can be used to improve training performance in deep neural networks by keeping the values of a hidden layer in a range that facilitates gradient-based training. Layer norm is a variation of the standard score, or z-score, from statistics applied to a single hidden layer. The first step in layer normalization is to calculate the mean, μ, and standard deviation, σ, over the elements of the vector to be normalized. Given a hidden layer with dimensionality d_h, these values are calculated as follows.
- korean_translation: 층 정규화(또는 layer norm)는 은닉층의 값들을 경사 하강법 기반 학습이 용이한 범위 내로 유지함으로써 깊은 신경망의 학습 성능을 개선하는 데 사용되는 다양한 정규화 방식 중 하나입니다. 층 정규화는 통계학의 표준 점수(또는 z-점수)를 단일 은닉층에 적용한 변형입니다. 층 정규화의 첫 번째 단계는 정규화할 벡터의 요소들에 대해 평균 $\mu$와 표준편차 $\sigma$를 계산하는 것입니다. 차원이 $d_h$인 은닉층이 주어졌을 때, 이 값들은 다음과 같이 계산됩니다.
- study_note: 층 정규화의 정의와 통계적 배경(z-score), 그리고 계산 절차를 설명합니다.

## Block 8
- type: formula
- source_text:
  μ = 1/d_h * ∑_{i=1}^{d_h} x_i
  σ = √ ( 1/d_h * ∑_{i=1}^{d_h} (x_i - μ)^2 )
- korean_translation:
  $\mu = \frac{1}{d_h} \sum_{i=1}^{d_h} x_i$
  $\sigma = \sqrt{\frac{1}{d_h} \sum_{i=1}^{d_h} (x_i - \mu)^2}$
- study_note: 벡터 요소별 평균(9.39)과 표준편차(9.40) 계산 공식입니다.

## Block 9
- type: paragraph
- source_text: Given these values, the vector components are normalized by subtracting the mean from each and dividing by the standard deviation. The result of this computation is a new vector with zero mean and a standard deviation of one.
- korean_translation: 이 값들이 주어지면, 각 벡터 성분에서 평균을 빼고 표준편차로 나눔으로써 성분들을 정규화합니다. 이 계산의 결과는 평균이 0이고 표준편차가 1인 새로운 벡터입니다.
- study_note: z-score 변환 과정을 설명합니다.

## Block 10
- type: formula
- source_text: x_hat = (x - μ) / σ
- korean_translation: $\hat{x} = \frac{(x - \mu)}{\sigma}$
- study_note: 정규화된 벡터 $\hat{x}$ 산출 공식(9.41)입니다.

## Block 11
- type: paragraph
- source_text: Finally, in the standard implementation of layer normalization, two learnable parameters, γ and β, representing gain and offset values, are introduced.
- korean_translation: 마지막으로, 층 정규화의 표준 구현에서는 게인(gain)과 오프셋(offset) 값을 나타내는 두 개의 학습 가능한 파라미터 $\gamma$와 $\beta$가 도입됩니다.
- study_note: 정규화 후 정보 복원 및 변형을 위한 파라미터를 추가함을 설명합니다.

## Block 12
- type: formula
- source_text: LayerNorm = γ * x_hat + β
- korean_translation: $\text{LayerNorm} = \gamma\hat{x} + \beta$
- study_note: 층 정규화의 최종 결과값 공식(9.42)입니다.

## Block 13
- type: subsection_title
- source_text: 9.7.2 Multihead Attention
- korean_translation: 9.7.2 멀티헤드 주의 집중
- study_note: 한 번의 어텐션만으로는 부족한 다양한 관계 정보를 병렬적으로 포착하는 멀티헤드 어텐션 기법을 다룹니다.

## Block 14
- type: paragraph
- source_text: The different words in a sentence can relate to each other in many different ways simultaneously. For example, distinct syntactic, semantic, and discourse relationships
- korean_translation: 한 문장 내의 서로 다른 단어들은 동시에 여러 가지 다른 방식으로 서로 관련될 수 있습니다. 예를 들어 뚜렷한 통사적, 의미적, 그리고 담화적 관계들이... [다음 페이지에서 계속]
- study_note: 단어 간 복합적인 관계를 포착해야 하는 필요성을 서술하며 멀티헤드 어텐션의 동기를 부여합니다.
