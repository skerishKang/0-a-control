# Page 9

## Block 1
- type: other
- source_text: 9.4 • RNNS FOR OTHER NLP TASKS 9
- korean_translation: 9.4 • 다른 NLP 작업을 위한 RNN 9
- study_note: 9.4 섹션의 시작과 페이지 번호입니다.

## Block 2
- type: subsection_title
- source_text: 9.4.1 Sequence Labeling
- korean_translation: 9.4.1 시퀀스 레이블링
- study_note: 시퀀스의 각 요소에 태그를 붙이는 작업인 시퀀스 레이블링을 RNN으로 수행하는 방법을 다룹니다.

## Block 3
- type: paragraph
- source_text: In sequence labeling, the network’s task is to assign a label chosen from a small fixed set of labels to each element of a sequence, like the part-of-speech tagging and named entity recognition tasks from Chapter 8. In an RNN approach to sequence labeling, inputs are word embeddings and the outputs are tag probabilities generated by a softmax layer over the given tagset, as illustrated in Fig. 9.7.
- korean_translation: 시퀀스 레이블링에서 네트워크의 작업은 8장에서 다룬 품사 태깅이나 개체명 인식 과제와 같이, 시퀀스의 각 요소에 미리 정해진 작은 레이블 집합 중 하나를 할당하는 것입니다. 시퀀스 레이블링에 대한 RNN 접근 방식에서는 그림 9.7에 묘사된 것처럼 입력은 단어 임베딩이고, 출력은 주어진 태그 집합에 대해 소프트맥스 층에서 생성된 태그 확률입니다.
- study_note: RNN을 이용한 시퀀스 레이블링의 기본적인 입출력 구조를 설명합니다.

## Block 4
- type: figure
- source_text:
  Argmax: NNP, MD, VB, DT, NN
  Softmax over tags
  RNN hidden layers (h)
  Embeddings (e)
  Words: Janet, will, back, the, bill
- korean_translation:
  Argmax 결과: NNP, MD, VB, DT, NN
  태그들에 대한 소프트맥스
  RNN 은닉층 (h)
  임베딩 (e)
  단어: Janet, will, back, the, bill
- study_note: "Janet will back the bill" 문장에 대한 RNN 품사 태깅 과정을 보여주는 도식입니다. 각 시점마다 독립적인 태그 분류 결과(Argmax)를 도출합니다.

## Block 5
- type: figure_caption
- source_text: Figure 9.7 Part-of-speech tagging as sequence labeling with a simple RNN. Pre-trained word embeddings serve as inputs and a softmax layer provides a probability distribution over the part-of-speech tags as output at each time step.
- korean_translation: 그림 9.7 단순 RNN을 이용한 시퀀스 레이블링으로서의 품사 태깅. 사전 학습된 단어 임베딩이 입력으로 사용되며, 소프트맥스 층이 매 시간 단계에서 출력으로 품사 태그들에 대한 확률 분포를 제공합니다.
- study_note: 그림 9.7에 대한 캡션으로, 사전 학습된 임베딩과 소프트맥스 출력의 역할을 명시합니다.

## Block 6
- type: paragraph
- source_text: In this figure, the inputs at each time step are pre-trained word embeddings corresponding to the input tokens. The RNN block is an abstraction that represents an unrolled simple recurrent network consisting of an input layer, hidden layer, and output layer at each time step, as well as the shared U, V and W weight matrices that comprise the network. The outputs of the network at each time step represent the distribution over the POS tagset generated by a softmax layer.
- korean_translation: 이 그림에서 각 시간 단계의 입력은 입력 토큰에 대응하는 사전 학습된 단어 임베딩입니다. RNN 블록은 각 시간 단계의 입력층, 은닉층, 출력층뿐만 아니라 네트워크를 구성하는 공유 가중치 행렬 $U, V, W$를 포함하는 '펼쳐진 단순 순환 신경망'을 나타내는 추상화된 표현입니다. 각 시간 단계에서의 네트워크 출력은 소프트맥스 층에 의해 생성된 품사 태그셋에 대한 분포를 나타냅니다.
- study_note: 도식화된 RNN 블록이 내부적으로는 펼쳐진 네트워크 구조를 의미함을 부연 설명합니다.

## Block 7
- type: paragraph
- source_text: To generate a sequence of tags for a given input, we run forward inference over the input sequence and select the most likely tag from the softmax at each step. Since we’re using a softmax layer to generate the probability distribution over the output tagset at each time step, we will again employ the cross-entropy loss during training.
- korean_translation: 주어진 입력에 대한 태그 시퀀스를 생성하기 위해, 우리는 입력 시퀀스에 대해 순방향 추론을 실행하고 각 단계의 소프트맥스에서 가장 가능성이 높은 태그를 선택합니다. 매 시간 단계에서 출력 태그셋에 대한 확률 분포를 생성하기 위해 소프트맥스 층을 사용하므로, 학습 중에는 다시 교차 엔트로피 손실을 채택할 것입니다.
- study_note: 추론 시의 태그 선택 방식(Argmax)과 학습 시의 손실 함수(교차 엔트로피)를 설명합니다.

## Block 8
- type: subsection_title
- source_text: 9.4.2 RNNs for Sequence Classification
- korean_translation: 9.4.2 시퀀스 분류를 위한 RNN
- study_note: 시퀀스 내 각 요소가 아닌, 시퀀스 전체를 하나의 카테고리로 분류하는 작업을 RNN으로 수행하는 방법을 다룹니다.

## Block 9
- type: paragraph
- source_text: Another use of RNNs is to classify entire sequences rather than the tokens within them. We’ve already encountered sentiment analysis in Chapter 4, in which we classify a text as positive or negative. Other sequence classification tasks for mapping sequences of text to one from a small set of categories include document-level topic classification, spam detection, or message routing for customer service applications.
- korean_translation: RNN의 또 다른 용도는 시퀀스 내의 토큰들이 아니라 시퀀스 전체를 분류하는 것입니다. 우리는 이미 4장에서 텍스트를 긍정 또는 부정으로 분류하는 감성 분석을 접했습니다. 텍스트 시퀀스를 작은 범주 집합 중 하나로 매핑하는 다른 시퀀스 분류 과제로는 문서 수준의 주제 분류, 스팸 탐지, 또는 고객 서비스 애플리케이션을 위한 메시지 라우팅 등이 있습니다.
- study_note: 시퀀스 분류의 정의와 구체적인 응용 사례(감성 분석, 스팸 탐지 등)를 나열합니다.

## Block 10
- type: paragraph
- source_text: To apply RNNs in this setting, we pass the text to be classified through the RNN a word at a time generating a new hidden layer at each time step. We can then take the hidden layer for the last token of the text, h_n, to constitute a compressed representation of the entire sequence. We can pass this representation h_n to a feedforward network that chooses a class via a softmax over the possible classes. Fig. 9.8 illustrates this approach.
- korean_translation: 이러한 상황에 RNN을 적용하기 위해, 우리는 분류할 텍스트를 한 번에 한 단어씩 RNN에 통과시켜 매 시간 단계마다 새로운 은닉층을 생성합니다. 그 다음 텍스트의 마지막 토큰에 대한 은닉층 $h_n$을 취하여 전체 시퀀스의 압축된 표현으로 구성할 수 있습니다. 우리는 이 표현 $h_n$을 가능한 클래스들에 대해 소프트맥스를 통해 클래스를 선택하는 피드포워드 신경망으로 보낼 수 있습니다. 그림 9.8이 이러한 접근 방식을 보여줍니다.
- study_note: 마지막 시점의 은닉 상태($h_n$)가 시퀀스 전체의 정보를 담고 있다고 가정하고 이를 분류기의 입력으로 사용하는 기법을 설명합니다.

## Block 11
- type: paragraph
- source_text: Note that in this approach there don’t need intermediate outputs for the words
- korean_translation: 이 접근 방식에서는 단어들에 대한 중간 출력이 필요하지 않다는 점에 주목하십시오. [다음 페이지에서 계속]
- study_note: 중간 시점의 출력을 사용하지 않는 분류 모델의 특징을 언급합니다.

# Page 10

## Block 1
- type: other
- source_text: 10 CHAPTER 9 • DEEP LEARNING ARCHITECTURES FOR SEQUENCE PROCESSING
- korean_translation: 10 제9장 • 시퀀스 처리를 위한 딥러닝 아키텍처
- study_note: 페이지 상단 헤더입니다.

## Block 2
- type: figure
- source_text:
  Softmax
  FFN
  h_n
  RNN (processing x_1, x_2, x_3, ..., x_n)
- korean_translation:
  소프트맥스(Softmax)
  피드포워드 신경망(FFN)
  h_n
  RNN (x_1, x_2, x_3, ..., x_n 처리 중)
- study_note: 시퀀스 분류를 위한 RNN 구조도입니다. 시퀀스 끝까지 처리한 후의 마지막 은닉 상태 $h_n$을 추출하여 별도의 분류용 FFN에 전달합니다.

## Block 3
- type: figure_caption
- source_text: Figure 9.8 Sequence classification using a simple RNN combined with a feedforward network. The final hidden state from the RNN is used as the input to a feedforward network that performs the classification.
- korean_translation: 그림 9.8 피드포워드 신경망과 결합된 단순 RNN을 이용한 시퀀스 분류. RNN의 마지막 은닉 상태가 분류를 수행하는 피드포워드 신경망의 입력으로 사용됩니다.
- study_note: 그림 9.8에 대한 캡션으로, 마지막 은닉 상태의 활용 방식을 재차 강조합니다.

## Block 4
- type: paragraph
- source_text: in the sequence preceding the last element. Therefore, there are no loss terms associated with those elements. Instead, the loss function used to train the weights in the network is based entirely on the final text classification task. The output from the softmax output from the feedforward classifier together with a cross-entropy loss drives the training. The error signal from the classification is backpropagated all the way through the weights in the feedforward classifier through, to its input, and then through to the three sets of weights in the RNN as described earlier in Section 9.2.2. The training regimen that uses the loss from a downstream application to adjust the weights all the way through the network is referred to as end-to-end training.
- korean_translation: [이전 페이지에서 계속] ...마지막 요소 앞에 오는 시퀀스의 요소들에게는 해당됩니다. 따라서 이러한 요소들과 연관된 손실 항은 존재하지 않습니다. 대신, 네트워크의 가중치들을 학습시키는 데 사용되는 손실 함수는 전적으로 최종 텍스트 분류 과제에 기반합니다. 피드포워드 분류기의 소프트맥스 출력과 교차 엔트로피 손실이 학습을 주도합니다. 분류 과정에서 발생한 오차 신호는 9.2.2절에서 이미 설명한 것처럼 피드포워드 분류기의 가중치들을 거쳐 그 입력으로, 그리고 다시 RNN의 세 가지 가중치 세트로 끝까지 역전파됩니다. 다운스트림 애플리케이션(최종 작업)의 손실을 사용하여 네트워크 전체의 가중치를 조정하는 학습 체계를 '엔드 투 엔드 학습(end-to-end training)'이라고 부릅니다.
- study_note: 중간 손실 없이 최종 출력의 손실만으로 전체 네트워크를 학습시키는 '엔드 투 엔드 학습'의 개념과 오차 전파 경로를 설명합니다.

## Block 5
- type: margin_note
- source_text: end-to-end training
- korean_translation: 엔드 투 엔드 학습(end-to-end training)
- study_note: 입력부터 최종 출력까지 모든 파라미터를 하나의 손실 함수를 통해 동시에 최적화하는 학습 방식입니다.

## Block 6
- type: margin_note
- source_text: pooling
- korean_translation: 풀링(pooling)
- study_note: 여러 개의 벡터 정보를 집약하여 하나의 대표 벡터로 만드는 기법입니다.

## Block 7
- type: paragraph
- source_text: Another option, instead of using just the last token h_n to represent the whole sequence, is to use some sort of pooling function of all the hidden states h_i for each word i in the sequence. For example, we can create a representation that pools all the n hidden states by taking their element-wise mean:
- korean_translation: 전체 시퀀스를 표현하기 위해 마지막 토큰 $h_n$만을 사용하는 대신 또 다른 선택지는, 시퀀스 내 각 단어 $i$에 대한 모든 은닉 상태 $h_i$의 일종의 '풀링(pooling)' 함수를 사용하는 것입니다. 예를 들어, 모든 $n$개의 은닉 상태의 요소별 평균(element-wise mean)을 취함으로써 이들을 풀링하는 표현을 만들 수 있습니다.
- study_note: 마지막 상태값만 쓰는 방식의 대안으로, 시퀀스 전체의 상태값들을 평균내어 정보를 집약하는 '평균 풀링'을 소개합니다.

## Block 8
- type: formula
- source_text: h_{mean} = \frac{1}{n} \sum_{i=1}^n h_i
- korean_translation: $h_{mean} = \frac{1}{n} \sum_{i=1}^n h_i$
- study_note: 평균 풀링 공식(9.14)입니다. 시점별 모든 은닉 상태의 평균을 구합니다.

## Block 9
- type: paragraph
- source_text: Or we can take the element-wise max; the element-wise max of a set of n vectors is a new vector whose kth element is the max of the kth elements of all the n vectors.
- korean_translation: 또는 요소별 최댓값(element-wise max)을 취할 수도 있습니다. $n$개 벡터 세트의 요소별 최댓값은, 각 성분이 $n$개 벡터들의 대응하는 성분 중 최댓값인 새로운 벡터가 됩니다.
- study_note: 정보의 최대 강도를 보존하는 방식인 '맥스 풀링'을 설명합니다.

## Block 10
- type: section_title
- source_text: 9.4.3 Generation with RNN-Based Language Models
- korean_translation: 9.4.3 RNN 기반 언어 모델을 이용한 생성
- study_note: 학습된 언어 모델을 사용하여 새로운 텍스트 시퀀스를 생성해내는 '텍스트 생성' 작업을 다룹니다.

## Block 11
- type: paragraph
- source_text: RNN-based language models can also be used to generate text. Text generation is of enormous practical importance, part of tasks like question answering, machine translation, text summarization, and conversational dialogue; any ask where a system needs to produce text, conditioned on some other text.
- korean_translation: RNN 기반 언어 모델은 텍스트를 생성하는 데에도 사용될 수 있습니다. 텍스트 생성은 질의응답, 기계 번역, 텍스트 요약, 대화와 같은 과제들의 일부로서 엄청난 실용적 중요성을 갖습니다. 즉, 시스템이 다른 어떤 텍스트를 조건으로 하여 새로운 텍스트를 출력해야 하는 모든 요구 사항에 해당합니다.
- study_note: 텍스트 생성 기술이 현대 NLP의 핵심적인 응용 분야들(번역, 요약 등)에서 갖는 가치를 강조합니다.

## Block 12
- type: paragraph
- source_text: Recall back in Chapter 3 we saw how to generate text from an n-gram language model by adapting a technique suggested contemporaneously by Claude Shannon (Shannon, 1951) and the psychologists George Miller and Selfridge (Miller and Selfridge, 1950). We first randomly sample a word to begin a sequence based on its suitability as the start of a sequence. We then continue to sample words conditioned on our previous choices until we reach a pre-determined length, or an end of sequence token is generated.
- korean_translation: 3장에서 우리는 클로드 섀넌(Shannon, 1951)과 심리학자인 조지 밀러 및 셀프리지(Miller and Selfridge, 1950)에 의해 동시에 제안된 기술을 응용하여, n-그램 언어 모델로부터 텍스트를 생성하는 방법을 살펴보았습니다. 먼저 시퀀스의 시작으로서의 적합성을 토대로 시퀀스를 시작할 단어를 무작위로 샘플링합니다. 그런 다음 미리 정해진 길이에 도달하거나 시퀀스 종료 토큰이 생성될 때까지, 이전의 선택들을 조건으로 하여 단어들을 계속 샘플링합니다.
- study_note: 3장에서 배운 고전적인 언어 모델 기반 생성 방식(무작위 샘플링 및 조건부 샘플링)을 복습합니다.

## Block 13
- type: paragraph
- source_text: Today, this approach of using a language model to incrementally generate words by repeatedly sampling the next word conditioned on our previous choices is called
- korean_translation: 오늘날, 이전의 선택들을 조건으로 다음 단어를 반복적으로 샘플링하여 점진적으로 단어들을 생성하는 이러한 언어 모델 활용 방식을... [다음 페이지에서 계속]
- study_note: 자동 회기(Autoregressive) 생성 방식의 현대적 정의를 도입합니다.
