# Page 21

## Block 1
- type: other
- source_text: 6.8 • WORD2VEC 21
- korean_translation: 6.8 • WORD2VEC 21
- study_note: 페이지 상단 헤더 및 페이지 번호입니다.

## Block 2
- type: paragraph
- source_text:
  and P(b) = .01:
  P_\alpha(a) = \frac{.99^{.75}}{.99^{.75} + .01^{.75}} = .97
  P_\alpha(b) = \frac{.01^{.75}}{.99^{.75} + .01^{.75}} = .03 (6.33)
- korean_translation:
  그리고 P(b) = .01일 때:
  P_\alpha(a) = \frac{.99^{.75}}{.99^{.75} + .01^{.75}} = .97
  P_\alpha(b) = \frac{.01^{.75}}{.99^{.75} + .01^{.75}} = .03 (6.33)
- study_note: 식 (6.33). 수치 예시를 통해 $\alpha=0.75$ 가중치가 저빈도 단어(b)의 선택 확률을 1%에서 3%로 높여주는 효과를 계산으로 보여줍니다.

## Block 3
- type: paragraph
- source_text: Given the set of positive and negative training instances, and an initial set of embeddings, the goal of the learning algorithm is to adjust those embeddings to
- korean_translation: 긍정 및 부정 훈련 사례 세트와 초기 임베딩 세트가 주어지면, 학습 알고리즘의 목표는 다음과 같이 임베딩을 조정하는 것입니다.
- study_note: 학습 알고리즘의 두 가지 핵심 목적을 제시하기 전 도입부입니다.

## Block 4
- type: paragraph
- source_text:
  • Maximize the similarity of the target word, context word pairs (w, c_{pos}) drawn from the positive examples
  • Minimize the similarity of the (w, c_{neg}) pairs from the negative examples.
- korean_translation:
  • 긍정적 예시에서 추출된 타겟 단어와 문맥 단어 쌍 $(w, c_{pos})$의 유사도를 최대화한다.
  • 부정적 예시에서 추출된 $(w, c_{neg})$ 쌍의 유사도를 최소화한다.
- study_note: 학습의 방향성(긍정 쌍은 가깝게, 부정 쌍은 멀게)을 정의합니다.

## Block 5
- type: paragraph
- source_text: If we consider one word/context pair (w, c_{pos}) with its k noise words c_{neg_1} \dots c_{neg_k}, we can express these two goals as the following loss function L to be minimized (hence the −); here the first term expresses that we want the classifier to assign the real context word c_{pos} a high probability of being a neighbor, and the second term expresses that we want to assign each of the noise words c_{neg_i} a high probability of being a non-neighbor, all multiplied because we assume independence:
- korean_translation: 하나의 단어/문맥 쌍 $(w, c_{pos})$와 그에 딸린 k개의 노이즈 단어 $c_{neg_1} \dots c_{neg_k}$를 고려할 때, 이 두 가지 목표를 최소화해야 할 다음과 같은 손실 함수 L로 표현할 수 있습니다(따라서 음의 부호 $-$가 붙습니다). 여기서 첫 번째 항은 분류기가 진짜 문맥 단어 $c_{pos}$에 대해 '이웃일 확률'을 높게 할당하기를 원함을 나타내고, 두 번째 항은 각 노이즈 단어 $c_{neg_i}$에 대해 '이웃이 아닐 확률'을 높게 할당하기를 원함을 나타냅니다. 독립성을 가정하기 때문에 이들은 모두 곱해집니다.
- study_note: 손실 함수(Loss Function)의 구성 원리와 각 항이 의미하는 바를 확률론적으로 설명합니다.

## Block 6
- type: formula
- source_text:
  L_{CE} = -\log \left[ P(+|w, c_{pos}) \prod_{i=1}^k P(-|w, c_{neg_i}) \right] \\
  = -\left[ \log P(+|w, c_{pos}) + \sum_{i=1}^k \log P(-|w, c_{neg_i}) \right] \\
  = -\left[ \log P(+|w, c_{pos}) + \sum_{i=1}^k \log (1 - P(+|w, c_{neg_i})) \right] \\
  = -\left[ \log \sigma(\mathbf{c}_{pos} \cdot \mathbf{w}) + \sum_{i=1}^k \log \sigma(-\mathbf{c}_{neg_i} \cdot \mathbf{w}) \right] (6.34)
- korean_translation:
  L_{CE} = -\log \left[ P(+|w, c_{pos}) \prod_{i=1}^k P(-|w, c_{neg_i}) \right] \\
  = -\left[ \log P(+|w, c_{pos}) + \sum_{i=1}^k \log P(-|w, c_{neg_i}) \right] \\
  = -\left[ \log P(+|w, c_{pos}) + \sum_{i=1}^k \log (1 - P(+|w, c_{neg_i})) \right] \\
  = -\left[ \log \sigma(\mathbf{c}_{pos} \cdot \mathbf{w}) + \sum_{i=1}^k \log \sigma(-\mathbf{c}_{neg_i} \cdot \mathbf{w}) \right] (6.34)
- study_note: 식 (6.34). 네거티브 샘플링을 이용한 교차 엔트로피(CE) 손실 함수의 유치 과정과 최종 형태입니다.

## Block 7
- type: paragraph
- source_text: That is, we want to maximize the dot product of the word with the actual context words, and minimize the dot products of the word with the k negative sampled non-neighbor words.
- korean_translation: 즉, 우리는 타겟 단어와 실제 문맥 단어 사이의 내적값은 최대화하고, k개의 네거티브 샘플링된 이웃이 아닌 단어들과의 내적값은 최소화하고자 하는 것입니다.
- study_note: 손실 함수 최소화의 기하학적 의미(내적 조절)를 설명합니다.

## Block 8
- type: paragraph
- source_text: We minimize this loss function using stochastic gradient descent. Fig. 6.14 shows the intuition of one step of learning.
- korean_translation: 우리는 확률적 경사 하강법(Stochastic Gradient Descent, SGD)을 사용하여 이 손실 함수를 최소화합니다. 그림 6.14는 학습의 한 단계를 직관적으로 보여줍니다.
- study_note: 최적화 알고리즘으로 SGD를 사용함을 명시합니다.

## Block 9
- type: paragraph
- source_text: To get the gradient, we need to take the derivative of Eq. 6.34 with respect to the different embeddings. It turns out the derivatives are the following (we leave the proof as an exercise at the end of the chapter):
- korean_translation: 그래디언트(기울기)를 구하기 위해서는 각각의 임베딩에 대해 식 6.34를 미분해야 합니다. 미분 결과는 다음과 같습니다(증명은 장 끝의 연습 문제로 남겨둡니다).
- study_note: 파라미터 업데이트를 위한 미분식 도출을 안내합니다.

## Block 10
- type: formula
- source_text:
  \frac{\partial L_{CE}}{\partial c_{pos}} = [\sigma(\mathbf{c}_{pos} \cdot \mathbf{w}) - 1]\mathbf{w} (6.35) \\
  \frac{\partial L_{CE}}{\partial c_{neg}} = [\sigma(\mathbf{c}_{neg} \cdot \mathbf{w})]\mathbf{w} (6.36) \\
  \frac{\partial L_{CE}}{\partial w} = [\sigma(\mathbf{c}_{pos} \cdot \mathbf{w}) - 1]\mathbf{c}_{pos} + \sum_{i=1}^k [\sigma(\mathbf{c}_{neg_i} \cdot \mathbf{w})]\mathbf{c}_{neg_i} (6.37)
- korean_translation:
  \frac{\partial L_{CE}}{\partial c_{pos}} = [\sigma(\mathbf{c}_{pos} \cdot \mathbf{w}) - 1]\mathbf{w} (6.35) \\
  \frac{\partial L_{CE}}{\partial c_{neg}} = [\sigma(\mathbf{c}_{neg} \cdot \mathbf{w})]\mathbf{w} (6.36) \\
  \frac{\partial L_{CE}}{\partial w} = [\sigma(\mathbf{c}_{pos} \cdot \mathbf{w}) - 1]\mathbf{c}_{pos} + \sum_{i=1}^k [\sigma(\mathbf{c}_{neg_i} \cdot \mathbf{w})]\mathbf{c}_{neg_i} (6.37)
- study_note: 식 (6.35)~(6.37). 각 벡터(c_pos, c_neg, w)에 대한 손실 함수의 편미분값입니다.

## Block 11
- type: paragraph
- source_text: The update equations going from time step t to t + 1 in stochastic gradient descent
- korean_translation: 확률적 경사 하강법에서 타임 스텝 t에서 t+1로 진행되는 업데이트 식은 [다음 페이지에서 계속]
- study_note: 미분값을 이용한 실제 가중치 업데이트 식으로 넘어가는 도입부입니다.

# Page 22

## Block 1
- type: other
- source_text: 22 CHAPTER 6 • VECTOR SEMANTICS AND EMBEDDINGS
- korean_translation: 22 제6장 • 벡터 의미론과 임베딩
- study_note: 페이지 상단 헤더 및 페이지 번호입니다.

## Block 2
- type: image
- source_text: [Figure 6.14 showing intuition of SGD: move apricot and jam closer, while moving apricot and matrix/Tolstoy further apart]
- korean_translation: [그림 6.14 SGD의 직관적 모습: apricot과 jam은 더 가깝게 이동시키고, apricot과 matrix/Tolstoy는 더 멀어지게 이동시킴]
- study_note: 경사 하강법을 통해 벡터 간의 거리가 어떻게 조정되는지 시각적으로 보여주는 그림입니다.

## Block 3
- type: figure_caption
- source_text: Figure 6.14 Intuition of one step of gradient descent. The skip-gram model tries to shift embeddings so the target embeddings (here for apricot) are closer to (have a higher dot product with) context embeddings for nearby words (here jam) and further from (lower dot product with) context embeddings for noise words that don’t occur nearby (here Tolstoy and matrix).
- korean_translation: 그림 6.14 경사 하강법 단계의 한 과정에 대한 직관. 스킵-그램 모델은 타겟 임베딩(여기서는 apricot)이 주변 단어의 문맥 임베딩(여기서는 jam)과는 더 가까워지고(더 높은 내적값을 가짐), 주변에 나타나지 않는 노이즈 단어의 문맥 임베딩(여기서는 Tolstoy와 matrix)과는 더 멀어지도록(더 낮은 내적값을 가짐) 임베딩을 이동시키려 시도합니다.
- study_note: 그림 6.14의 상세 캡션입니다.

## Block 4
- type: paragraph
- source_text: are thus:
- korean_translation: 다음과 같습니다.
- study_note: 업데이트 공식 도입부입니다.

## Block 5
- type: formula
- source_text:
  \mathbf{c}_{pos}^{t+1} = \mathbf{c}_{pos}^t - \eta[\sigma(\mathbf{c}_{pos}^t \cdot \mathbf{w}^t) - 1]\mathbf{w}^t (6.38) \\
  \mathbf{c}_{neg}^{t+1} = \mathbf{c}_{neg}^t - \eta[\sigma(\mathbf{c}_{neg}^t \cdot \mathbf{w}^t)]\mathbf{w}^t (6.39) \\
  \mathbf{w}^{t+1} = \mathbf{w}^t - \eta \left[ [\sigma(\mathbf{c}_{pos}^t \cdot \mathbf{w}^t) - 1]\mathbf{c}_{pos} + \sum_{i=1}^k [\sigma(\mathbf{c}_{neg_i}^t \cdot \mathbf{w}^t)]\mathbf{c}_{neg_i} \right] (6.40)
- korean_translation:
  \mathbf{c}_{pos}^{t+1} = \mathbf{c}_{pos}^t - \eta[\sigma(\mathbf{c}_{pos}^t \cdot \mathbf{w}^t) - 1]\mathbf{w}^t (6.38) \\
  \mathbf{c}_{neg}^{t+1} = \mathbf{c}_{neg}^t - \eta[\sigma(\mathbf{c}_{neg}^t \cdot \mathbf{w}^t)]\mathbf{w}^t (6.39) \\
  \mathbf{w}^{t+1} = \mathbf{w}^t - \eta \left[ [\sigma(\mathbf{c}_{pos}^t \cdot \mathbf{w}^t) - 1]\mathbf{c}_{pos} + \sum_{i=1}^k [\sigma(\mathbf{c}_{neg_i}^t \cdot \mathbf{w}^t)]\mathbf{c}_{neg_i} \right] (6.40)
- study_note: 식 (6.38)~(6.40). 학습률 $\eta$를 적용하여 각 벡터들을 업데이트하는 최종 공식들입니다.

## Block 6
- type: paragraph
- source_text: Just as in logistic regression, then, the learning algorithm starts with randomly initialized W and C matrices, and then walks through the training corpus using gradient descent to move W and C so as to maximize the objective in Eq. 6.34 by making the updates in (Eq. 6.39)-(Eq. 6.40).
- korean_translation: 로지스틱 회귀에서와 마찬가지로, 학습 알고리즘은 무작위로 초기화된 행렬 W와 C에서 시작합니다. 그 후 훈련 코퍼스를 훑어보며 경사 하강법을 사용하여 식 6.39~6.40의 업데이트 과정을 거침으로써, 식 6.34의 목적 함수를 최대화하도록 W와 C를 조정해 나갑니다.
- study_note: 전체적인 학습 흐름(초기화 -> 업데이트 반복)을 요약합니다.

## Block 7
- type: paragraph
- source_text: Recall that the skip-gram model learns two separate embeddings for each word i: the target embedding w_i and the context embedding c_i, stored in two matrices, the target matrix W and the context matrix C. It’s common to just add them together, representing word i with the vector w_i + c_i. Alternatively we can throw away the C matrix and just represent each word i by the vector w_i.
- korean_translation: 스킵-그램 모델은 각 단어 i마다 두 개의 개별적인 임베딩을 학습한다는 점을 상기하십시오. 즉, 두 개의 행렬인 타겟 행렬 W와 문맥 행렬 C에 각각 저장되는 타겟 임베딩 $w_i$와 문맥 임베딩 $c_i$입니다. 일반적으로 단어 i를 $w_i+c_i$로 합쳐서 표현하는 것이 흔합니다. 또는 C 행렬을 버리고 각 단어 i를 단순히 타겟 벡터 $w_i$로만 표현할 수도 있습니다.
- study_note: 학습된 두 가지 벡터(입력/출력)를 최종적으로 어떻게 활용하는지(합산 또는 타겟만 사용)를 설명합니다.

## Block 8
- type: margin_note
- source_text:
  target embedding
  context embedding
- korean_translation:
  타겟 임베딩(target embedding)
  문맥 임베딩(context embedding)
- study_note: 단어 하나가 가지는 두 가지 역할에 따른 임베딩 명칭입니다.

## Block 9
- type: paragraph
- source_text: As with the simple count-based methods like tf-idf, the context window size L affects the performance of skip-gram embeddings, and experiments often tune the parameter L on a devset.
- korean_translation: tf-idf와 같은 단순 빈도 기반 방식에서와 마찬가지로, 문백 윈도우 크기 L은 스킵-그램 임베딩의 성능에 영향을 주며, 실험을 통해 개발 세트(devset)에서 파라미터 L을 튜닝하는 경우가 많습니다.
- study_note: 윈도우 크기가 성능에 미치는 영향과 튜닝의 필요성을 언급합니다.

## Block 10
- type: section_title
- source_text: 6.8.3 Other kinds of static embeddings
- korean_translation: 6.8.3 다른 종류의 정적 임베딩들
- study_note: Word2vec 이외에 정체 임베딩을 구성하는 다른 기법들(FastText 등)을 소개하는 섹션입니다.

## Block 11
- type: margin_note
- source_text: fasttext
- korean_translation: 패스트텍스트(fasttext)
- study_note: 단어를 하위 단어 유닛으로 쪼개어 학습하는 기법입니다.

## Block 12
- type: paragraph
- source_text: There are many kinds of static embeddings. An extension of word2vec, fasttext (Bojanowski et al., 2017), addresses a problem with word2vec as we have presented it so far: it has no good way to deal with unknown words — words that appear in a test corpus but were unseen in the training corpus. A related problem is word sparsity, such as in languages with rich morphology, where some of the many forms for each noun and verb may only occur rarely. Fasttext deals with these problems by using subword models, representing each word as itself plus a bag of constituent n-grams, with special boundary symbols < and > added to each word. For example,
- korean_translation: 정적 임베딩에는 여러 종류가 있습니다. Word2vec의 확장 버전인 패스트텍스트(FastText, Bojanowski et al., 2017)는 지금까지 설명한 word2vec의 문제점, 즉 '미등록 단어(unknown words)' 문제(테스트 코퍼스에는 등장하지만 훈련 코퍼스에서는 본 적 없는 단어들)를 해결하려 합니다. 이와 관련된 또 다른 문제는 풍부한 형태론(morphology)을 가진 언어들에서 명사와 동사의 다양한 형태 변화 중 일부가 드물게만 나타나는 단어 희소성 문제입니다. FastText는 하위 단어 모델(subword models)을 사용하여 각 단어를 단어 자체뿐만 아니라 이를 구성하는 n-그램들의 집합으로 표현함으로써 이 문제들을 해결합니다. 이때 각 단어에는 <와 >라는 특별한 경계 기호가 추가됩니다. 예를 들어,
- study_note: FastText의 등장 배경(미등록 단어 문제, 형태론적 복잡성 해결)과 하위 단어 유닛(subword unit) 기반의 작동 방식을 설명합니다.
