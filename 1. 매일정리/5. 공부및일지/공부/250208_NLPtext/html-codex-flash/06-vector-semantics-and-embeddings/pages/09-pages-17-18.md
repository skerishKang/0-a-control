# Page 17

## Block 1
- type: other
- source_text: 6.8 • WORD2VEC 17
- korean_translation: 6.8 • WORD2VEC 17
- study_note: 페이지 상단 헤더 및 페이지 번호입니다.

## Block 2
- type: paragraph
- source_text: Given two documents, we can then compute their document vectors d_1 and d_2, and estimate the similarity between the two documents by cos(d_1, d_2). Document similarity is also useful for all sorts of applications; information retrieval, plagiarism detection, news recommender systems, and even for digital humanities tasks like comparing different versions of a text to see which are similar to each other.
- korean_translation: 두 개의 문서가 주어졌을 때, 각 문서 벡터 $d_1$과 $d_2$를 계산한 후 $\cos(d_1, d_2)$를 구하여 두 문서 간의 유사도를 측정할 수 있습니다. 문서 유사도는 정보 검색, 표절 탐지, 뉴스 추천 시스템뿐만 아니라, 서로 다른 텍스트 판본들을 비교하여 유사성을 찾는 디지털 인문학 연구 등 다양한 분야에서 유용하게 사용됩니다.
- study_note: [6.7절 마무리] 문서 벡터를 이용한 유사도 측정의 응용 분야를 설명합니다.

## Block 3
- type: paragraph
- source_text: Either the PPMI model or the tf-idf model can be used to compute word similarity, for tasks like finding word paraphrases, tracking changes in word meaning, or automatically discovering meanings of words in different corpora. For example, we can find the 10 most similar words to any target word w by computing the cosines between w and each of the V − 1 other words, sorting, and looking at the top 10.
- korean_translation: PPMI 모델이나 tf-idf 모델은 단어의 유의어 찾기, 단어 의미의 변화 추적, 또는 서로 다른 코퍼스에서 단어의 의미를 자동으로 발견하는 등의 작업에서 단어 유사도를 계산하는 데 사용될 수 있습니다. 예를 들어, 타겟 단어 w와 나머지 $V-1$개의 다른 단어들 사이의 코사인 값을 계산하여 정렬한 뒤 상위 10개를 확인하면, 단어 w와 가장 유사한 10개의 단어를 찾을 수 있습니다.
- study_note: 단어 벡터를 이용한 유사 단어 추출 방식을 설명합니다.

## Block 4
- type: section_title
- source_text: 6.8 Word2vec
- korean_translation: 6.8 Word2vec
- study_note: 현대 NLP의 핵심적인 단어 표현 기법인 Word2vec을 소개합니다.

## Block 5
- type: paragraph
- source_text: In the previous sections we saw how to represent a word as a sparse, long vector with dimensions corresponding to words in the vocabulary or documents in a collection. We now introduce a more powerful word representation: embeddings, short dense vectors. Unlike the vectors we’ve seen so far, embeddings are short, with number of dimensions d ranging from 50-1000, rather than the much larger vocabulary size |V| or number of documents D we’ve seen. These d dimensions don’t have a clear interpretation. And the vectors are dense: instead of vector entries being sparse, mostly-zero counts or functions of counts, the values will be real-valued numbers that can be negative.
- korean_translation: 이전 섹션들에서 우리는 단어를 어휘 목록의 단어나 컬렉션의 문서에 대응하는 차원을 가진, 길고 희소한(sparse) 벡터로 표현하는 방법을 살펴보았습니다. 이제 우리는 더 강력한 단어 표현 방식인 임베딩(embeddings), 즉 짧고 밀집된(dense) 벡터를 소개합니다. 지금까지 봐왔던 벡터들과 달리 임베딩은 길이가 짧으며, 차원 수 $d$는 앞서 보았던 거대한 어휘 크기 $|V|$나 문서 수 $D$가 아니라 대개 50에서 1000 사이의 값을 가집니다. 이 $d$개의 차원들은 명확한 개별적 해석이 불가능합니다. 또한 이 벡터들은 밀집되어 있습니다. 벡터의 성분들이 대부분 0인 빈도수나 그 함수로 이루어진 희소한 형태가 아니라, 음수를 포함할 수 있는 실숫값들로 채워집니다.
- study_note: 희소 벡터(Sparse Vectors)와 밀집 벡터(Dense Vectors/Embeddings)의 차이점 및 특징을 비교 설명합니다.

## Block 6
- type: paragraph
- source_text: It turns out that dense vectors work better in every NLP task than sparse vectors. While we don’t completely understand all the reasons for this, we have some intuitions. Representing words as 300-dimensional dense vectors requires our classifiers to learn far fewer weights than if we represented words as 50,000-dimensional vectors, and the smaller parameter space possibly helps with generalization and avoiding overfitting. Dense vectors may also do a better job of capturing synonymy. For example, in a sparse vector representation, dimensions for synonyms like car and automobile dimension are distinct and unrelated; sparse vectors may thus fail to capture the similarity between a word with car as a neighbor and a word with automobile as a neighbor.
- korean_translation: 밀집 벡터가 모든 NLP 작업에서 희소 벡터보다 더 우수한 성능을 보인다는 사실이 밝혀졌습니다. 그 이유를 완벽하게 다 이해하는 것은 아니지만, 몇 가지 직관적인 이유는 있습니다. 단어를 300차원의 밀집 벡터로 표현하면, 50,000차원 벡터로 표현할 때보다 분류기가 학습해야 할 가중치(weights)의 수가 훨씬 적어지며, 이렇게 파라미터 공간이 작아지면 일반화(generalization)와 오버피팅(overfitting) 방지에 도움이 될 수 있습니다. 또한 밀집 벡터는 유의어 관계를 포착하는 데 더 유리할 수 있습니다. 예를 들어, 희소 벡터 표현에서는 'car'와 'automobile' 같은 유의어들이 서로 별개의 무관한 차원으로 취급됩니다. 따라서 희소 벡터는 'car'를 이웃으로 가진 단어와 'automobile'을 이웃으로 가진 단어 사이의 유사성을 포착하지 못할 수도 있습니다.
- study_note: 밀집 벡터가 선호되는 이유(학습 파라미터 감소, 일반화 유리, 유의어 포착 용이성)를 설명합니다.

## Block 7
- type: margin_note
- source_text:
  skip-gram
  SGNS
  word2vec
- korean_translation:
  스킵-그램(skip-gram)
  SGNS
  word2vec
- study_note: Word2vec의 핵심 알고리즘과 소프트웨어 패키지 명칭입니다.

## Block 8
- type: paragraph
- source_text: In this section we introduce one method for computing embeddings: skip-gram with negative sampling, sometimes called SGNS. The skip-gram algorithm is one of two algorithms in a software package called word2vec, and so sometimes the algorithm is loosely referred to as word2vec (Mikolov et al. 2013a, Mikolov et al. 2013b). The word2vec methods are fast, efficient to train, and easily available online with code and pretrained embeddings. Word2vec embeddings are static embeddings, meaning that the method learns one fixed embedding for each word in the vocabulary. In Chapter 11 we’ll introduce methods for learning dynamic contextual embeddings like the popular family of BERT representations, in which the vector for each word is different in different contexts.
- korean_translation: 이 섹션에서는 임베딩을 계산하는 한 가지 방법인 '네거티브 샘플링을 결합한 스킵-그램(skip-gram with negative sampling)', 흔히 SGNS라고 부르는 방법을 소개합니다. 스킵-그램 알고리즘은 word2vec이라는 소프트웨어 패키지에 포함된 두 가지 알고리즘 중 하나이며, 그래서 때로는 이 알고리즘 자체가 느슨하게 word2vec이라고 불리기도 합니다(Mikolov et al. 2013a, 2013b). word2vec 방식은 학습 속도가 빠르고 효율적이며, 코드와 사전 학습된 임베딩(pretrained embeddings)을 온라인에서 쉽게 구할 수 있습니다. word2vec 임베딩은 정적 임베딩(static embeddings)입니다. 즉, 어휘 목록의 각 단어에 대해 고정된 하나의 임베딩을 학습한다는 의미입니다. 11장에서는 각 단어가 문맥에 따라 서로 다른 벡터를 갖게 되는 BERT 계열과 같은 동적 문맥적 임베딩(contextual embeddings) 학습 방법을 다룰 예정입니다.
- study_note: SGNS의 정의, Word2vec의 특징(빠른 속도, 대중성), 그리고 정적 임베딩과 문맥적 임베딩의 차이를 설명합니다.

## Block 9
- type: margin_note
- source_text: static embeddings
- korean_translation: 정적 임베딩(static embeddings)
- study_note: 단어마다 하나의 고정된 벡터값을 가지는 임베딩 방식입니다.

## Block 10
- type: paragraph
- source_text: The intuition of word2vec is that instead of counting how often each word w occurs near, say, apricot, we’ll instead train a classifier on a binary prediction task: “Is word w likely to show up near apricot?” We don’t actually care about this prediction task; instead we’ll take the learned classifier weights as the word embeddings.
- korean_translation: word2vec의 직관은 어떤 단어 w가 예를 들어 'apricot' 근처에 얼마나 자주 나타나는지를 세는 대신, "단어 w가 'apricot' 근처에 나타날 가능성이 있는가?"라는 이진 예측 작업(binary prediction task)으로 분류기를 학습시키는 것입니다. 우리는 사실 이 예측 작업 자체에는 관심이 없습니다. 대신 학습된 분류기의 가중치를 단어 임베딩으로 사용하고자 하는 것입니다.
- study_note: Word2vec의 학습 원리(이진 분류 문제를 통한 가중치 학습)를 설명합니다.

## Block 11
- type: paragraph
- source_text: The revolutionary intuition here is that we can just use running text as implicitly supervised training data for such a classifier; a word c that occurs near the target word apricot acts as gold ‘correct answer’ to the question “Is word c likely to show
- korean_translation: 여기서 혁신적인 직관은 일반 텍스트 자체를 이러한 분류기를 위한 암시적으로 지도된(implicitly supervised) 학습 데이터로 사용할 수 있다는 점입니다. 타겟 단어 'apricot' 근처에 나타나는 단어 c는 "단어 c가 [다음 페이지에서 계속]에 나타날 가능성이 있는가?"라는 질문에 대한 골드 '정답(correct answer)' 역할을 하게 됩니다.
- study_note: 별도의 라벨링 없이 텍스트 자체에서 지도 학습 데이터를 추출하는 '자기 지도 학습'의 개념을 소개합니다.

# Page 18

## Block 1
- type: other
- source_text: 18 CHAPTER 6 • VECTOR SEMANTICS AND EMBEDDINGS
- korean_translation: 18 제6장 • 벡터 의미론과 임베딩
- study_note: 페이지 상단 헤더 및 페이지 번호입니다.

## Block 2
- type: margin_note
- source_text: self-supervision
- korean_translation: 자기 지도 학습(self-supervision)
- study_note: 정답 라벨이 데이터 자체에 포함되어 있어 스스로 학습 데이터를 생성하는 방식입니다.

## Block 3
- type: paragraph
- source_text: up near apricot?” This method, often called self-supervision, avoids the need for any sort of hand-labeled supervision signal. This idea was first proposed in the task of neural language modeling, when Bengio et al. (2003) and Collobert et al. (2011) showed that a neural language model (a neural network that learned to predict the next word from prior words) could just use the next word in running text as its supervision signal, and could be used to learn an embedding representation for each word as part of doing this prediction task.
- korean_translation: [이전 페이지에서 계속] 'apricot' 주변에 나타날 가능성이 있는가?"라는 질문에 대한 증거가 됩니다. 흔히 자기 지도 학습(self-supervision)이라고 불리는 이 방식은 수작업으로 라벨링된 지도 신호가 필요 없습니다. 이러한 아이디어는 신경망 언어 모델링 작업에서 처음 제안되었는데, Bengio 등(2003)과 Collobert 등(2011)은 (이전 단어들로부터 다음 단어를 예측하는 법을 배우는 신경망인) 신경망 언어 모델이 일반 텍스트의 다음 단어를 스스로의 지도 신호로 사용할 수 있으며, 이 예측 작업을 수행하는 과정의 일부로서 각 단어에 대한 임베딩 표현을 학습할 수 있음을 보여주었습니다.
- study_note: 자기 지도 학습의 역사와 신경망 언어 모델에서의 적용 사례를 설명합니다.

## Block 4
- type: paragraph
- source_text: We’ll see how to do neural networks in the next chapter, but word2vec is a much simpler model than the neural network language model, in two ways. First, word2vec simplifies the task (making it binary classification instead of word prediction). Second, word2vec simplifies the architecture (training a logistic regression classifier instead of a multi-layer network with hidden layers that demand more sophisticated training algorithms). The intuition of skip-gram is:
- korean_translation: 다음 장에서 신경망을 다루는 법을 배우겠지만, word2vec은 신경망 언어 모델보다 두 가지 면에서 훨씬 단순한 모델입니다. 첫째, word2vec은 작업을 단순화했습니다(단어 예측 대신 이진 분류로 만듦). 둘째, word2vec은 구조를 단순화했습니다(더 정교한 학습 알고리즘을 요구하는 은닉층이 있는 다층 신경망 대신 로지스틱 회귀 분류기를 학습시킴). 스킵-그램(skip-gram)의 직관은 다음과 같습니다.
- study_note: Neural LM 대비 Word2vec이 가지는 단순성(이진 분류, 로지스틱 회귀 구조)을 강조합니다.

## Block 5
- type: paragraph
- source_text:
  1. Treat the target word and a neighboring context word as positive examples.
  2. Randomly sample other words in the lexicon to get negative samples.
  3. Use logistic regression to train a classifier to distinguish those two cases.
  4. Use the learned weights as the embeddings.
- korean_translation:
  1. 타겟 단어와 주변 문맥 단어를 '긍정적 예시(positive examples)'로 취급한다.
  2. 어휘 목록에서 다른 단어들을 무작위로 샘플링하여 '부정적 예시(negative samples)'를 얻는다.
  3. 로지스틱 회귀를 사용하여 이 두 경우를 구분하는 분류기를 학습시킨다.
  4. 이때 학습된 가중치를 임베딩으로 사용한다.
- study_note: 스킵-그램(SGNS)의 학습 알고리즘 4단계를 명확히 정리합니다.

## Block 6
- type: section_title
- source_text: 6.8.1 The classifier
- korean_translation: 6.8.1 분류기(The classifier)
- study_note: Word2vec 임베딩 학습의 기반이 되는 이진 분류기에 대해 자세히 다룹니다.

## Block 7
- type: paragraph
- source_text: Let’s start by thinking about the classification task, and then turn to how to train. Imagine a sentence like the following, with a target word apricot, and assume we’re using a window of ±2 context words:
- korean_translation: 먼저 분류 작업에 대해 생각한 다음, 학습 방법으로 넘어가겠습니다. 타겟 단어가 'apricot'이고 문맥 단어 윈도우 크기를 ±2로 설정한 다음과 같은 문장을 상상해 보십시오.
- study_note: 분류기의 동작 방식을 설명하기 위한 구체적인 문장 예시와 윈도우 개념을 도입합니다.

## Block 8
- type: other
- source_text:
  ... lemon, a [tablespoon of apricot jam, a] pinch ...
  c1 c2 w c3 c4
- korean_translation:
  ... lemon, a [tablespoon of apricot jam, a] pinch ...
  c1 c2 w c3 c4
- study_note: 윈도우 크기가 2일 때 타겟 단어(w) 주변의 문맥 단어(c1~c4)들이 어떻게 정의되는지 보여줍니다.

## Block 9
- type: paragraph
- source_text: Our goal is to train a classifier such that, given a tuple (w, c) of a target word w paired with a candidate context word c (for example (apricot, jam), or perhaps (apricot, aardvark)) it will return the probability that c is a real context word (true for jam, false for aardvark):
- korean_translation: 우리의 목표는 타겟 단어 w와 후보 문맥 단어 c로 이루어진 튜플 (w, c)가 주어졌을 때(예: (apricot, jam) 또는 (apricot, aardvark)), c가 '진짜(real)' 문맥 단어일 확률을 반환하는 분류기를 학습시키는 것입니다. (jam의 경우에는 참, aardvark의 경우에는 거짓이 되어야 함)
- study_note: 분류기가 수행해야 할 최종 역할을 정의합니다.

## Block 10
- type: formula
- source_text: P(+|w, c) (6.24)
- korean_translation: P(+|w, c) (6.24)
- study_note: 식 (6.24). (w, c) 쌍이 긍정적 예시(진짜 문맥)일 확률을 표기합니다.

## Block 11
- type: paragraph
- source_text: The probability that word c is not a real context word for w is just 1 minus Eq. 6.24:
- korean_translation: 단어 c가 w에 대한 진짜 문맥 단어가 아닐 확률은 단순히 1에서 식 6.24를 뺀 값입니다.
- study_note: 부정적 예시일 확률에 대한 보충 설명입니다.

## Block 12
- type: formula
- source_text: P(−|w, c) = 1 − P(+|w, c) (6.25)
- korean_translation: P(−|w, c) = 1 − P(+|w, c) (6.25)
- study_note: 식 (6.25). (w, c) 쌍이 부정적 예시일 확률 공식입니다.

## Block 13
- type: paragraph
- source_text: How does the classifier compute the probability P? The intuition of the skip-gram model is to base this probability on embedding similarity: a word is likely to occur near the target if its embedding vector is similar to the target embedding. To compute similarity between these dense embeddings, we rely on the intuition that two vectors are similar if they have a high dot product (after all, cosine is just a normalized dot product). In other words:
- korean_translation: 분류기는 확률 P를 어떻게 계산할까요? 스킵-그램 모델의 직관은 이 확률을 임베딩 유사성에 기초하는 것입니다. 즉, 어떤 단어의 임베딩 벡터가 타겟의 임베딩과 유사하다면 해당 단어가 타겟 근처에 나타날 가능성이 높다는 것입니다. 이러한 밀집 임베딩 사이의 유사도를 계산하기 위해, 우리는 두 벡터의 내적(dot product)이 높을수록 유사하다는 직관에 의존합니다. (결국 코사인 유사도는 정규화된 내적일 뿐입니다.) 다시 말해 다음과 같습니다.
- study_note: 임베딩 벡터 간의 내적을 통해 유사도를 구하고, 이를 기반으로 확률을 예측하는 원리를 설명합니다.

## Block 14
- type: formula
- source_text: \text{Similarity}(w, c) \approx \mathbf{c} \cdot \mathbf{w} (6.26)
- korean_translation: \text{Similarity}(w, c) \approx \mathbf{c} \cdot \mathbf{w} (6.26)
- study_note: 식 (6.26). 단어 간 유사도는 벡터 내적으로 근사될 수 있음을 보여줍니다.

## Block 15
- type: paragraph
- source_text: The dot product c · w is not a probability, it’s just a number ranging from −\infty to \infty (since the elements in word2vec embeddings can be negative, the dot product can be negative). To turn the dot product into a probability, we’ll use the logistic or sigmoid function \sigma(x), the fundamental core of logistic regression:
- korean_translation: 내적 값 $\mathbf{c} \cdot \mathbf{w}$는 확률이 아니며, $-\infty$에서 $\infty$ 사이의 범위를 갖는 수치일 뿐입니다(word2vec 임베딩의 성분들은 음수일 수 있으므로 내적 값 또한 음수일 수 있음). 이 내적 값을 확률로 변환하기 위해, 로지스틱 회귀의 근본적인 핵심인 로지스틱 또는 시그모이드(sigmoid, S자 곡선) 함수 $\sigma(x)$를 사용합니다.
- study_note: 내적 값을 확률값(0~1)으로 사영시키기 위한 시그모이드 함수의 역할을 설명합니다.

## Block 16
- type: formula
- source_text: \sigma(x) = \frac{1}{1 + \exp(-x)} (6.27)
- korean_translation: \sigma(x) = \frac{1}{1 + \exp(-x)} (6.27)
- study_note: 식 (6.27). 시그모이드 함수의 정의식입니다.
