# Page 3

## Block 1
- type: other
- source_text: 5.1 • CLASSIFICATION: THE SIGMOID 3
- korean_translation: 5.1 • 분류: 시그모이드 3
- study_note: 페이지 상단의 섹션 제목과 페이지 번호입니다.

## Block 2
- type: paragraph
- source_text: sentiment” versus “negative sentiment”, the features represent counts of words in a document, P(y = 1|x) is the probability that the document has positive sentiment, and P(y = 0|x) is the probability that the document has negative sentiment.
- korean_translation: [이전 페이지에서 계속] ‘긍정 감성’ 대 ‘부정 감성’의 경우, 특징들은 문서 내 단어의 빈도를 나타내며, $P(y=1|x)$는 문서가 긍정 감성을 가질 확률이고 $P(y=0|x)$는 문서가 부정 감성을 가질 확률입니다.
- study_note: 감성 분석을 예로 들어 이진 분류에서의 확률 표기를 설명합니다.

## Block 3
- type: paragraph
- source_text: Logistic regression solves this task by learning, from a training set, a vector of weights and a bias term. Each weight w_i is a real number, and is associated with one of the input features x_i. The weight w_i represents how important that input feature is to the classification decision, and can be positive (providing evidence that the instance being classified belongs in the positive class) or negative (providing evidence that the instance being classified belongs in the negative class). Thus we might expect in a sentiment task the word awesome to have a high positive weight, and abysmal to have a very negative weight. The bias term, also called the intercept, is another real number that’s added to the weighted inputs.
- korean_translation: 로지스틱 회귀는 학습 세트로부터 가중치 벡터와 편향 항(bias term)을 학습함으로써 이 작업을 해결합니다. 각 가중치 $w_i$는 실수이며, 입력 특징 $x_i$ 중 하나와 결합됩니다. 가중치 $w_i$는 해당 입력 특징이 분류 결정에 얼마나 중요한지를 나타내며, 양수(분류 대상이 긍정 클래스에 속한다는 증거 제공) 또는 음수(분류 대상이 부정 클래스에 속한다는 증거 제공)가 될 수 있습니다. 따라서 우리는 감성 분석 작업에서 'awesome' 같은 단어는 높은 양수 가중치를 갖고, 'abysmal' 같은 단어는 매우 낮은 음수 가중치를 가질 것으로 기대할 수 있습니다. 절편(intercept)이라고도 불리는 편향 항은 가중치가 적용된 입력값들에 더해지는 또 다른 실수입니다.
- study_note: 가중치가 특징의 중요도와 방향성을 결정한다는 로지스틱 회귀의 핵심 원리를 설명합니다.

## Block 4
- type: margin_note
- source_text:
  bias term
  intercept
- korean_translation:
  편향 항(bias term)
  절편(intercept)
- study_note: 선형 결합에서 특징값들과 상관없이 더해지는 상수항을 가리킵니다.

## Block 5
- type: paragraph
- source_text: To make a decision on a test instance— after we’ve learned the weights in training— the classifier first multiplies each x_i by its weight w_i, sums up the weighted features, and adds the bias term b. The resulting single number z expresses the weighted sum of the evidence for the class.
- korean_translation: 학습을 통해 가중치를 익힌 후 테스트 인스턴스에 대해 결정을 내릴 때, 분류기는 먼저 각 $x_i$에 가중치 $w_i$를 곱하고, 가중치가 적용된 특징들을 모두 합한 뒤 편향 항 $b$를 더합니다. 그 결과로 나온 단일 숫자 $z$는 클래스에 대한 증거의 가중합(weighted sum)을 표현합니다.
- study_note: 입력 데이터와 가중치의 선형 결합을 통해 결정 점수인 $z$를 도출하는 과정을 설명합니다.

## Block 6
- type: formula
- source_text:
  z = ( \sum_{i=1}^{n} w_i x_i ) + b (5.2)
- korean_translation:
  z = \left( \sum_{i=1}^{n} w_i x_i \right) + b (5.2)
- study_note: 식 (5.2). 전형적인 선형 회귀의 형태이며, 로지스틱 회귀의 입력값이 됩니다.

## Block 7
- type: margin_note
- source_text: dot product
- korean_translation: 내적(dot product)
- study_note: 두 벡터의 대응하는 성분끼리 곱해 합하는 연산입니다.

## Block 8
- type: paragraph
- source_text: In the rest of the book we’ll represent such sums using the dot product notation from linear algebra. The dot product of two vectors a and b, written as a · b is the sum of the products of the corresponding elements of each vector. Thus the following is an equivalent formation to Eq. 5.2:
- korean_translation: 책의 나머지 부분에서는 이러한 합을 선형 대수학의 내적(dot product) 표기법을 사용하여 표현하겠습니다. 두 벡터 $a$와 $b$의 내적은 $a \cdot b$로 쓰며, 각 벡터의 대응하는 원소들의 곱의 합입니다. 따라서 다음은 식 (5.2)와 동일한 형식입니다.
- study_note: 수식을 간결하게 표현하기 위한 내적 표기법의 도입을 설명합니다.

## Block 9
- type: formula
- source_text:
  z = w · x + b (5.3)
- korean_translation:
  z = w \cdot x + b (5.3)
- study_note: 식 (5.3). 가중합을 벡터 내적의 형태로 간결하게 표현한 것입니다.

## Block 10
- type: paragraph
- source_text: But note that nothing in Eq. 5.3 forces z to be a legal probability, that is, to lie between 0 and 1. In fact, since weights are real-valued, the output might even be negative; z ranges from −∞ to ∞.
- korean_translation: 하지만 식 (5.3)의 그 어떤 것도 $z$가 유효한 확률값, 즉 0과 1 사이에 있도록 강제하지 않는다는 점에 유의하십시오. 사실 가중치가 실수값이므로 출력값은 음수일 수도 있으며, $z$의 범위는 $-\infty$에서 $\infty$까지입니다.
- study_note: 선형 결합의 결과인 $z$가 왜 직접적으로 확률로 사용될 수 없는지, 그 범위의 한계를 지적합니다.

## Block 11
- type: figure_caption
- source_text: Figure 5.1 The sigmoid function σ(z) = 1 / (1 + e^{−z}) takes a real value and maps it to the range [0, 1]. It is nearly linear around 0 but outlier values get squashed toward 0 or 1.
- korean_translation: 그림 5.1 시그모이드 함수 $\sigma(z) = 1 / (1 + e^{-z})$는 실수값을 받아 $[0, 1]$ 범위로 대응시킵니다. 0 근처에서는 거의 선형적이지만, 극단적인 값(outliers)은 0이나 1로 압착(squash)됩니다.
- study_note: 시그모이드 함수의 시각적 특성과 역할(압착)을 설명합니다.

## Block 12
- type: margin_note
- source_text:
  sigmoid
  logistic function
- korean_translation:
  시그모이드(sigmoid)
  로지스틱 함수(logistic function)
- study_note: 로지스틱 회귀의 핵심인 S자형 함수를 일컫는 용어들입니다.

## Block 13
- type: paragraph
- source_text: To create a probability, we’ll pass z through the sigmoid function, σ(z). The sigmoid function (named because it looks like an s) is also called the logistic function, and gives logistic regression its name. The sigmoid has the following equation, shown graphically in Fig. 5.1:
- korean_translation: 확률을 생성하기 위해 우리는 $z$를 시그모이드 함수 $\sigma(z)$에 통과시킬 것입니다. 시그모이드 함수(S자 모양처럼 생겨서 붙여진 이름)는 로지스틱 함수라고도 불리며, 로지스틱 회귀라는 이름의 기원이기도 합니다. 시그모이드는 다음과 같은 형식을 가지며 그림 5.1에 시각화되어 있습니다.
- study_note: 시그모이드 함수가 실수 점수를 확률로 변환하는 장치임을 설명합니다.

## Block 14
- type: formula
- source_text:
  \sigma(z) = \frac{1}{1 + e^{-z}} = \frac{1}{1 + \exp(-z)} (5.4)
- korean_translation:
  \sigma(z) = \frac{1}{1 + e^{-z}} = \frac{1}{1 + \exp(-z)} (5.4)
- study_note: 식 (5.4). 시그모이드 함수의 정의식입니다.

## Block 15
- type: paragraph
- source_text: (For the rest of the book, we’ll use the notation exp(x) to mean e^x.) The sigmoid has a number of advantages; it takes a real-valued number and maps it into the range
- korean_translation: (책의 나머지 부분에서는 $e^x$를 나타내기 위해 $\exp(x)$라는 표기를 사용하겠습니다.) 시그모이드는 여러 가지 장점이 있습니다. 이는 실수값을 받아 다음 범위로 대응시킵니다. [다음 페이지에서 계속]
- study_note: 표기법에 대한 약속과 함께 시그모이드의 효용성을 강조하기 시작합니다.

# Page 4

## Block 1
- type: other
- source_text: 4 CHAPTER 5 • LOGISTIC REGRESSION
- korean_translation: 4 제5장 • 로지스틱 회귀
- study_note: 페이지 상단 헤더와 페이지 번호입니다.

## Block 2
- type: paragraph
- source_text: [0, 1], which is just what we want for a probability. Because it is nearly linear around 0 but flattens toward the ends, it tends to squash outlier values toward 0 or 1. And it’s differentiable, which as we’ll see in Section 5.8 will be handy for learning.
- korean_translation: [이전 페이지에서 계속] $[0, 1]$ 범위로 대응시키는데, 이는 우리가 확률값에 대해 원하는 바로 그것입니다. 0 주변에서는 거의 선형적이지만 양 끝으로 갈수록 평평해지기 때문에, 극단적인 값들을 0이나 1로 압착하는 경향이 있습니다. 또한 미분이 가능(differentiable)한데, 이는 5.8절에서 보게 되듯 학습에 매우 유용할 것입니다.
- study_note: 시그모이드의 세 가지 주요 특징(확률 범위 제공, 극단값 압착, 미분 가능성)을 요약합니다.

## Block 3
- type: paragraph
- source_text: We’re almost there. If we apply the sigmoid to the sum of the weighted features, we get a number between 0 and 1. To make it a probability, we just need to make sure that the two cases, p(y = 1) and p(y = 0), sum to 1. We can do this as follows:
- korean_translation: 거의 다 왔습니다. 가중치가 적용된 특징들의 합에 시그모이드를 적용하면 0과 1 사이의 숫자를 얻습니다. 이를 확률로 만들기 위해서는 단지 두 경우, 즉 $p(y=1)$와 $p(y=0)$의 합이 1이 되도록 확실히 하면 됩니다. 다음과 같이 할 수 있습니다.
- study_note: 이진 분류에서 두 클래스의 확률 합이 1이 되어야 한다는 확률의 기본 원칙을 적용합니다.

## Block 4
- type: formula
- source_text:
  P(y = 1) = \sigma(w \cdot x + b)
           = \frac{1}{1 + \exp(-(w \cdot x + b))}
  P(y = 0) = 1 - \sigma(w \cdot x + b)
           = 1 - \frac{1}{1 + \exp(-(w \cdot x + b))}
           = \frac{\exp(-(w \cdot x + b))}{1 + \exp(-(w \cdot x + b))} (5.5)
- korean_translation:
  P(y = 1) = \sigma(w \cdot x + b)
           = \frac{1}{1 + \exp(-(w \cdot x + b))}
  P(y = 0) = 1 - \sigma(w \cdot x + b)
           = 1 - \frac{1}{1 + \exp(-(w \cdot x + b))}
           = \frac{\exp(-(w \cdot x + b))}{1 + \exp(-(w \cdot x + b))} (5.5)
- study_note: 식 (5.5). 각 클래스에 대한 확률 도출 수식입니다.

## Block 5
- type: formula
- source_text:
  1 − \sigma(x) = \sigma(−x) (5.6)
- korean_translation:
  1 - \sigma(x) = \sigma(-x) (5.6)
- study_note: 식 (5.6). 시그모이드 함수의 중요한 대칭적 대수 특성을 보여줍니다.

## Block 6
- type: paragraph
- source_text: so we could also have expressed P(y = 0) as σ(−(w · x + b)). Now we have an algorithm that given an instance x computes the probability P(y = 1|x). How do we make a decision? For a test instance x, we say yes if the probability P(y = 1|x) is more than .5, and no otherwise. We call .5 the decision boundary:
- korean_translation: 따라서 $P(y=0)$를 $\sigma(-(w \cdot x + b))$로 표현할 수도 있었을 것입니다. 이제 인스턴스 $x$가 주어졌을 때 확률 $P(y=1|x)$를 계산하는 알고리즘을 갖게 되었습니다. 그렇다면 결정은 어떻게 내릴까요? 테스트 인스턴스 $x$에 대해, 확률 $P(y=1|x)$가 0.5보다 크면 '예'(긍정)라고 하고, 그렇지 않으면 '아니오'라고 합니다. 우리는 0.5를 결정 경계(decision boundary)라고 부릅니다.
- study_note: 계산된 확률을 바탕으로 최종 클래스를 선택하는 기준인 임계값(0.5)과 결정 경계의 개념을 설명합니다.

## Block 7
- type: margin_note
- source_text: decision boundary
- korean_translation: 결정 경계(decision boundary)
- study_note: 서로 다른 클래스로 분류되는 영역을 가르는 기준이 되는 경계입니다.

## Block 8
- type: formula
- source_text:
  decision(x) = { 1 if P(y = 1|x) > 0.5
                { 0 otherwise
- korean_translation:
  decision(x) = \begin{cases} 1 & \text{if } P(y = 1|x) > 0.5 \\ 0 & \text{otherwise} \end{cases}
- study_note: 최종 결정을 내리는 함수적 표현입니다.

## Block 9
- type: section_title
- source_text: 5.1.1 Example: sentiment classification
- korean_translation: 5.1.1 예제: 감성 분류
- study_note: 이론을 실제 데이터에 적용해 보는 구체적인 예제 섹션입니다.

## Block 10
- type: paragraph
- source_text: Let’s have an example. Suppose we are doing binary sentiment classification on movie review text, and we would like to know whether to assign the sentiment class + or − to a review document doc. We’ll represent each input observation by the 6 features x1 . . . x6 of the input shown in the following table; Fig. 5.2 shows the features in a sample mini test document.
- korean_translation: 예제를 들어 봅시다. 영화 리뷰 텍스트에 대해 이진 감성 분류를 수행하고 있으며, 리뷰 문서 `doc`에 감성 클래스 + 또는 -를 할당하고 싶다고 가정해 봅시다. 우리는 각 입력 관측치를 다음 표에 제시된 6개의 특징 $x_1 \dots x_6$으로 표현할 것입니다. 그림 5.2는 샘플 미니 테스트 문서에 있는 특징들을 보여줍니다.
- study_note: 감성 분석에서 사용되는 6가지 언어적 특징들을 모델의 입력값으로 설정합니다.

## Block 11
- type: table
- source_text:
  | Var | Definition | Value in Fig. 5.2 |
  |---|---|---|
  | x1 | count(positive lexicon words ∈ doc) | 3 |
  | x2 | count(negative lexicon words ∈ doc) | 2 |
  | x3 | { 1 if “no” ∈ doc, 0 otherwise | 1 |
  | x4 | count(1st and 2nd pronouns ∈ doc) | 3 |
  | x5 | { 1 if “!” ∈ doc, 0 otherwise | 0 |
  | x6 | log(word count of doc) | ln(66) = 4.19 |
- korean_translation:
  | 변수 | 정의 | 그림 5.2의 값 |
  |---|---|---|
  | x1 | 문서 내 긍정 어휘 사전 단어 수 | 3 |
  | x2 | 문서 내 부정 어휘 사전 단어 수 | 2 |
  | x3 | 문서에 “no”가 포함되면 1, 아니면 0 | 1 |
  | x4 | 문서 내 1인칭 및 2인칭 대명사 수 | 3 |
  | x5 | 문서에 “!”가 포함되면 1, 아니면 0 | 0 |
  | x6 | 로그(문서의 단어 수) | ln(66) = 4.19 |
- study_note: 텍스트에서 추출한 특징들의 구체적인 정의와 예시 값들입니다. 빈도뿐만 아니라 불리언 값(포함 여부)과 로그 스케일링된 값도 사용됨에 주목하세요.

## Block 12
- type: paragraph
- source_text: Let’s assume for the moment that we’ve already learned a real-valued weight for each of these features, and that the 6 weights corresponding to the 6 features are [2.5, −5.0, −1.2, 0.5, 2.0, 0.7], while b = 0.1. (We’ll discuss in the next section how
- korean_translation: 잠시 동안 우리가 이미 이 특징들 각각에 대한 실수 가중치를 학습했다고 가정해 봅시다. 6개의 특징에 대응하는 6개의 가중치는 $[2.5, -5.0, -1.2, 0.5, 2.0, 0.7]$이고, $b=0.1$이라고 합시다. (어떻게 [다음 페이지에서 계속]
- study_note: 학습이 완료된 가중치 값들을 예시로 들어 계산 과정을 보여주기 위한 준비 단계입니다.
