# Page 7

## Block 1
- type: other
- source_text: 3.2 • EVALUATING LANGUAGE MODELS 7
- korean_translation: 3.2 • 언어 모델의 평가 7
- study_note: 페이지 상단 헤더 및 페이지 번호입니다.

## Block 2
- type: paragraph
- source_text: 1, the more probabilities we multiply together, the smaller the product becomes. Multiplying enough n-grams together would result in numerical underflow. By using log probabilities instead of raw probabilities, we get numbers that are not as small. Adding in log space is equivalent to multiplying in linear space, so we combine log probabilities by adding them. The result of doing all computation and storage in log space is that we only need to convert back into probabilities if we need to report them at the end; then we can just take the exp of the logprob:
- korean_translation: [이전 페이지에서 계속] 1보다 작으므로, 여러 확률을 곱할수록 그 결과값은 점점 더 작아집니다. 많은 수의 N-그램 확률을 계속 곱하다 보면 결국 수치 하향 넘침(numerical underflow)이 발생할 수 있습니다. 원시 확률 대신 로그 확률을 사용하면 값이 지나치게 작아지는 것을 방지할 수 있습니다. 로그 공간에서의 덧셈은 선형 공간에서의 곱셈과 동일하므로, 로그 확률을 더하는 방식으로 확률을 결합하게 됩니다. 모든 계산과 저장을 로그 공간에서 수행하면, 최종 결과를 보고할 때만 지수 함수(exp)를 취해 원래의 확률값으로 되돌리면 됩니다.
- study_note: 확률 곱셈 연산을 로그 공간의 덧셈 연산으로 변환하여 계산의 수치적 안정성을 확보하는 이유와 그 이점을 설명합니다.

## Block 3
- type: formula
- source_text: p1 × p2 × p3 × p4 = exp(log p1 + log p2 + log p3 + log p4) (3.13)
- korean_translation: p1 × p2 × p3 × p4 = exp(log p1 + log p2 + log p3 + log p4) (3.13)
- study_note: 식 (3.13). 확률의 곱을 로그의 합과 지수 함수를 이용해 표현한 수식입니다.

## Block 4
- type: section_title
- source_text: 3.2 Evaluating Language Models
- korean_translation: 3.2 언어 모델의 평가
- study_note: 언어 모델의 성능을 측정하는 다양한 방법론을 다루는 섹션입니다.

## Block 5
- type: margin_note
- source_text: extrinsic evaluation
- korean_translation: 외부 평가(extrinsic evaluation)
- study_note: 모델을 실제 응용 프로그램에 적용하여 최종적인 성능 향상 폭을 측정하는 방식입니다.

## Block 6
- type: paragraph
- source_text: The best way to evaluate the performance of a language model is to embed it in an application and measure how much the application improves. Such end-to-end evaluation is called extrinsic evaluation. Extrinsic evaluation is the only way to know if a particular improvement in a component is really going to help the task at hand. Thus, for speech recognition, we can compare the performance of two language models by running the speech recognizer twice, once with each language model, and seeing which gives the more accurate transcription.
- korean_translation: 언어 모델의 성능을 평가하는 가장 좋은 방법은 모델을 실제 애플리케이션에 통합하여 시스템 전반의 성능이 얼마나 개선되는지 측정하는 것입니다. 이러한 종단간(end-to-end) 평가 방식을 외부 평가(extrinsic evaluation)라고 합니다. 외부 평가는 특정 컴포넌트의 개선이 실제 해결하려는 과제에 실질적인 도움이 되는지 확인할 수 있는 유일한 방법입니다. 예를 들어 음성 인식의 경우, 두 개의 언어 모델을 각각 적용하여 시스템을 두 번 실행해 본 뒤 어느 쪽이 더 정확한 전사(transcription) 결과를 내는지 비교함으로써 모델의 우열을 가릴 수 있습니다.
- study_note: 외부 평가의 정의와 의의를 설명합니다. 실제 비즈니스 가치나 실무적 효용성을 검증하는 데 가장 확실한 방법입니다.

## Block 7
- type: margin_note
- source_text: intrinsic evaluation
- korean_translation: 내부 평가(intrinsic evaluation)
- study_note: 특정 응용 프로그램과는 무관하게 모델 자체의 통계적 품질을 평가하는 방식입니다.

## Block 8
- type: paragraph
- source_text: Unfortunately, running big NLP systems end-to-end is often very expensive. Instead, it would be nice to have a metric that can be used to quickly evaluate potential improvements in a language model. An intrinsic evaluation metric is one that measures the quality of a model independent of any application.
- korean_translation: 안타깝게도 대규모 NLP 시스템을 매번 종단간으로 실행하여 평가하는 것은 비용과 시간이 너무 많이 듭니다. 대신 모델의 잠재적 개선 효과를 빠르게 가늠해 볼 수 있는 지표가 있다면 유용할 것입니다. 내부 평가(intrinsic evaluation) 지표는 특정 애플리케이션에 구애받지 않고 모델 자체의 통계적 품질을 측정하는 기준이 됩니다.
- study_note: 외부 평가의 높은 비용 문제를 보완하기 위해 도입된 내부 평가의 개념과 필요성을 설명합니다.

## Block 9
- type: margin_note
- source_text:
  training set
  test set
  held out
- korean_translation:
  학습 세트(training set)
  테스트 세트(test set)
  홀드 아웃(held out)
- study_note: 모델 학습과 평가를 위해 데이터를 나누는 표준적인 용어들입니다.

## Block 10
- type: paragraph
- source_text: For an intrinsic evaluation of a language model we need a test set. As with many of the statistical models in our field, the probabilities of an n-gram model come from the corpus it is trained on, the training set or training corpus. We can then measure the quality of an n-gram model by its performance on some unseen data called the test set or test corpus. We will also sometimes call test sets and other datasets that are not in our training sets held out corpora because we hold them out from the training data.
- korean_translation: 언어 모델의 내부 평가를 수행하려면 테스트 세트(test set)가 필요합니다. 이 분야의 여러 통계 모델들이 그렇듯, N-그램 모델의 확률값은 모델을 학습시키는 데 사용된 말뭉치인 학습 세트(training set) 또는 학습 코퍼스로부터 얻어집니다. 모델의 품질은 학습 과정에서 보지 못한 새로운 데이터인 테스트 세트(또는 테스트 코퍼스)에서의 성능으로 측정합니다. 테스트 세트처럼 학습에 사용하지 않고 따로 떼어놓은 데이터셋을 '학습 데이터에서 제외했다'는 의미에서 홀드 아웃(held out) 코퍼스라고 부르기도 합니다.
- study_note: 학습 데이터와 평가 데이터를 엄격히 분리하는 기계 학습의 기본 원칙을 언어 모델 관점에서 설명합니다.

## Block 11
- type: paragraph
- source_text: So if we are given a corpus of text and want to compare two different n-gram models, we divide the data into training and test sets, train the parameters of both models on the training set, and then compare how well the two trained models fit the test set.
- korean_translation: 따라서 텍스트 코퍼스가 주어졌을 때 두 개의 서로 다른 N-그램 모델을 비교하려면 우선 데이터를 학습용과 테스트용으로 나눕니다. 그다음 학습 세트를 이용해 두 모델의 파라미터를 각각 학습시킨 뒤, 학습된 모델들이 테스트 세트에 얼마나 잘 들어맞는지(fit)를 비교하게 됩니다.
- study_note: 모델 간 성능 비교를 위해 데이터를 분할하고 대조하는 표준적인 실험 절차를 제시합니다.

## Block 12
- type: paragraph
- source_text: But what does it mean to “fit the test set”? The answer is simple: whichever model assigns a higher probability to the test set—meaning it more accurately predicts the test set—is a better model. Given two probabilistic models, the better model is the one that has a tighter fit to the test data or that better predicts the details of the test data, and hence will assign a higher probability to the test data.
- korean_translation: 여기서 "테스트 세트에 잘 들어맞는다"는 것은 무엇을 의미할까요? 답은 간단합니다. 테스트 세트에 대해 더 높은 확률을 부여하는 모델, 다시 말해 테스트 세트의 데이터를 더 정확히 예측하는 모델이 더 우수한 모델입니다. 두 개의 확률 모델 중 테스트 데이터와 더 긴밀하게 일치하거나 세부 구조를 더 잘 예측하여, 결과적으로 해당 데이터가 나타날 확률을 더 높게 측정하는 쪽을 선택하면 됩니다.
- study_note: '좋은 언어 모델'의 기준을 확률론적으로 정의합니다. 관찰된(테스트) 데이터가 나타날 가능성을 더 높게 산출하는 모델이 더 성능이 좋다는 논리입니다.

## Block 13
- type: paragraph
- source_text: Since our evaluation metric is based on test set probability, it’s important not to let the test sentences into the training set. Suppose we are trying to compute the probability of a particular “test” sentence. If our test sentence is part of the training corpus, we will mistakenly assign it an artificially high probability when it occurs in the test set. We call this situation training on the test set. Training on the test set introduces a bias that makes the probabilities all look too high, and causes huge inaccuracies in perplexity, the probability-based metric we introduce below.
- korean_translation: 평가 지표가 테스트 세트 확률에 기반하므로, 테스트 문장이 학습 세트에 섞여 들어가지 않도록 주의해야 합니다. 만약 우리가 평가하려는 테스트 문장이 이미 학습 말뭉치에 포함되어 있다면, 모델은 해당 문장에 대해 인위적으로 높은 확률을 부여하게 됩니다. 이러한 상황을 '테스트 세트에 대한 학습(training on the test set)'이라고 합니다. 이는 확률값이 실제보다 높게 측정되는 편향(bias)을 유발하며, 이후 설명할 펄플렉서티(perplexity) 지표의 정확도를 심각하게 떨어뜨립니다.
- study_note: 테스트 데이터 누출(Data leakage)의 위험성을 경고합니다. 이는 모델의 실제 일반화 능력을 왜곡하는 치명적인 오류입니다.

## Block 14
- type: paragraph
- source_text: Sometimes we use a particular test set so often that we implicitly tune to its characteristics. We then need a fresh test set that is truly unseen. In such cases, we call the initial test set the development test set or, devset. How do we divide our
- korean_translation: 특정 테스트 세트를 반복해서 사용하다 보면, 나도 모르게 그 데이터의 특성에 맞춰 모델을 튜닝(tune)하게 될 수 있습니다. 이럴 때는 모델이 이전에는 전혀 본 적 없는 새로운 테스트 세트가 필요합니다. 이런 상황에서 실험 과정 중에 반복적으로 사용하는 데이터셋을 개발 테스트 세트(development test set) 또는 개발 세트(devset)라고 부릅니다. 그렇다면 우리의 데이터를 어떻게
- study_note: 개발 과정에서의 과적합을 방지하기 위해 검증용(devset) 데이터와 최종 평가용 테스트 세트를 분리해야 하는 이유를 설명합니다.

## Block 15
- type: margin_note
- source_text:
  development
  test
- korean_translation:
  개발용(development)
  테스트(test)
- study_note: 모델 개발 시 반복 참조하는 검증 세트와 최종 성능 확정용 테스트 세트의 차이를 나타냅니다.

# Page 8

## Block 1
- type: other
- source_text: 8 CHAPTER 3 • N-GRAM LANGUAGE MODELS
- korean_translation: 8 제3장 • N-그램 언어 모델
- study_note: 페이지 상단 헤더입니다.

## Block 2
- type: paragraph
- source_text: data into training, development, and test sets? We want our test set to be as large as possible, since a small test set may be accidentally unrepresentative, but we also want as much training data as possible. At the minimum, we would want to pick the smallest test set that gives us enough statistical power to measure a statistically significant difference between two potential models. In practice, we often just divide our data into 80% training, 10% development, and 10% test. Given a large corpus that we want to divide into training and test, test data can either be taken from some continuous sequence of text inside the corpus, or we can remove smaller “stripes” of text from randomly selected parts of our corpus and combine them into a test set.
- korean_translation: [이전 페이지에서 계속] 학습, 개발, 테스트 세트로 나눌 수 있을까요? 규모가 작은 테스트 세트는 데이터의 대표성을 잃을 위험이 있으므로 충분히 큰 것이 좋지만, 동시에 학습용 데이터도 최대한 많이 확보해야 합니다. 최소한 두 모델 사이의 성능 차이를 통계적으로 유의미하게 측정할 수 있을 만큼의 '통계적 검정력(statistical power)'을 제공하는 수준에서 테스트 세트의 크기를 정해야 합니다. 실무에서는 보통 학습 80%, 개발 10%, 테스트 10%의 비율로 나눕니다. 테스트 데이터는 전체 말뭉치의 특정 구간을 통째로 가져오거나, 여러 부분에서 무작위로 추출한 작은 '조각(stripes)'들을 합쳐서 구성할 수 있습니다.
- study_note: 데이터 분할의 일반적인 비율과 테스트 데이터를 구성하는 상이한 추출 전략을 설명합니다.

## Block 3
- type: section_title
- source_text: 3.2.1 Perplexity
- korean_translation: 3.2.1 펄플렉서티(Perplexity)
- study_note: 언어 모델 평가의 표준 지표인 펄플렉서티를 다루는 섹션입니다.

## Block 4
- type: margin_note
- source_text: perplexity
- korean_translation: 펄플렉서티(perplexity)
- study_note: 모델이 새로운 텍스트 시퀀스에 대해 느끼는 '불확실성' 내지는 '당혹도'를 정량화한 지표입니다.

## Block 5
- type: paragraph
- source_text: In practice we don’t use raw probability as our metric for evaluating language models, but a variant called perplexity. The perplexity (sometimes called PP for short) of a language model on a test set is the inverse probability of the test set, normalized by the number of words. For a test set W = w1w2...wN :
- korean_translation: 실제 평가 시에는 원시 확률값보다는 이를 변형한 펄플렉서티(perplexity, 흔히 PP라고 줄여 씀)라는 지표를 주로 사용합니다. 테스트 세트에 대한 펄플렉서티는 테스트 세트가 나타날 확률을 역수로 취한 뒤, 이를 문장 내 단어 수에 따라 정규화한 값입니다. 테스트 시퀀스 $W = w_1 w_2 ... w_N$에 대하여 펄플렉서티는 다음과 같이 정의됩니다.
- study_note: 펄플렉서티의 정의를 설명합니다. 확률의 역수를 활용해 '모델이 얼마나 헷갈려 하는지'를 측정하며, 이 값이 낮을수록 모델의 성능이 좋음을 의미합니다.

## Block 6
- type: formula
- source_text:
  PP(W) = P(w1w2...wN)^{−1/N}
        = root_N(1 / P(w1w2...wN)) (3.14)
- korean_translation:
  PP(W) = P(w1w2...wN)^{−1/N}
        = root_N(1 / P(w1w2...wN)) (3.14)
- study_note: 식 (3.14). 펄플렉서티의 기본 정의식입니다.

## Block 7
- type: paragraph
- source_text: We can use the chain rule to expand the probability of W:
- korean_translation: 확률의 연쇄 법칙을 사용하여 $W$의 결합 확률을 다음과 같이 전개할 수 있습니다.
- study_note: 결합 확률을 각 단어의 조건부 확률들의 곱으로 변환합니다.

## Block 8
- type: formula
- source_text: PP(W) = root_N(Product_{i=1}^N 1 / P(wi|w1...wi−1)) (3.15)
- korean_translation: PP(W) = root_N(Product_{i=1}^N 1 / P(wi|w1...wi−1)) (3.15)
- study_note: 식 (3.15). 연쇄 법칙을 적용한 일반적인 펄플렉서티 공식입니다.

## Block 9
- type: paragraph
- source_text: Thus, if we are computing the perplexity of W with a bigram language model, we get:
- korean_translation: 이를 바이그램 언어 모델의 상황에 적용하면 다음과 같은 식이 얻어집니다.
- study_note: 일반식을 바이그램용으로 구체화하는 과정입니다.

## Block 10
- type: formula
- source_text: PP(W) = root_N(Product_{i=1}^N 1 / P(wi|wi−1)) (3.16)
- korean_translation: PP(W) = root_N(Product_{i=1}^N 1 / P(wi|wi−1)) (3.16)
- study_note: 식 (3.16). 바이그램 모델 전용 펄플렉서티 공식입니다.

## Block 11
- type: paragraph
- source_text: Note that because of the inverse in Eq. 3.15, the higher the conditional probability of the word sequence, the lower the perplexity. Thus, minimizing perplexity is equivalent to maximizing the test set probability according to the language model. What we generally use for word sequence in Eq. 3.15 or Eq. 3.16 is the entire sequence of words in some test set. Since this sequence will cross many sentence boundaries, we need to include the begin- and end-of-sentence markers <s> and </s> in the probability computation. We also need to include the end-of-sentence marker </s> (but not the beginning-of-sentence marker <s>) in the total count of word tokens N.
- korean_translation: 식 3.15에 나타난 역수 관계 덕분에, 단어 시퀀스의 조건부 확률이 높을수록 펄플렉서티 값은 낮아집니다. 즉, 펄플렉서티를 최소화하는 것은 언어 모델이 테스트 세트를 가장 높은 확률로 예측하게 만드는 것과 같습니다. 계산 시에는 보통 테스트 세트의 전체 단어 시퀀스를 사용하며, 여기에는 여러 문장 경계가 포함되므로 `<s>`와 `</s>` 같은 마커를 확률 계산에 포함해야 합니다. 단, 전체 단어 토큰 수 $N$을 셀 때는 문장 종료 마커인 `</s>`만 포함하고 시작 마커인 `<s>`는 제외하는 것이 관례입니다.
- study_note: 펄플렉서티와 확률 사이의 역상관 관계, 그리고 실제 계산 시 경계 토큰(마커) 처리 및 단어 수 $N$ 산정 규칙을 설명합니다.

## Block 12
- type: paragraph
- source_text: There is another way to think about perplexity: as the weighted average branching factor of a language. The branching factor of a language is the number of possible next words that can follow any word. Consider the task of recognizing the digits in English (zero, one, two,..., nine), given that (both in some training set and in some test set) each of the 10 digits occurs with equal probability P = 1/10. The perplexity of this mini-language is in fact 10. To see that, imagine a test string of digits of length
- korean_translation: 펄플렉서티를 이해하는 또 다른 직관적인 방법은 이를 언어의 '가중 평균 분기 계수(branching factor)'로 보는 것입니다. 분기 계수란 어떤 단어 다음에 올 수 있는 선택 가능한 단어의 가짓수를 의미합니다. 가령 0부터 9까지의 숫자(zero, one, ..., nine) 10개가 모두 동일한 확률 $P = 1/10$로 나타나는 상황을 가정해 봅시다. 이 미니 언어의 펄플렉서티는 정확히 10이 됩니다. 이를 이해하기 위해 [다음 페이지에서 계속]
- study_note: 펄플렉서티를 '다음에 올 수 있는 단어 후보들의 평균적인 가짓수'로 해석하는 직관적인 관점을 소개합니다.
