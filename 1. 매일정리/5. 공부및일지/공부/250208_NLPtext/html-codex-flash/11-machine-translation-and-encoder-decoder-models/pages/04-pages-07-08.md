# Page 7

## Block 1
- type: paragraph
- source_text: 3. A decoder, which accepts c as input and generates an arbitrary length sequence of hidden states hm 1 , from which a corresponding sequence of output states ym 1 , can be obtained. Just as with encoders, decoders can be realized by any kind of sequence architecture.
- korean_translation: 3. $c$를 입력으로 받아 임의의 길이를 가진 은닉 상태 시퀀스 $h_1^m$을 생성하고, 이로부터 그에 대응하는 출력 상태 시퀀스 $y_1^m$을 얻을 수 있는 디코더. 인코더와 마찬가지로 디코더도 어떤 종류의 시퀀스 아키텍처로든 구현될 수 있습니다.
- study_note: 인코더-디코더의 세 번째 구성 요소인 디코더의 정의와 유연성을 설명합니다.

## Block 2
- type: other
- source_text: 11.3 • ENCODER-DECODER WITH RNNS 7
- korean_translation: 11.3 • RNN을 이용한 인코더-디코더 7
- study_note: 페이지 상단 헤더입니다.

## Block 3
- type: section_title
- source_text: 11.3 Encoder-Decoder with RNNs
- korean_translation: 11.3 RNN을 이용한 인코더-디코더
- study_note: RNN 기반의 인코더-디코더 모델을 상세히 설명하는 섹션의 시작입니다.

## Block 4
- type: paragraph
- source_text: Let’s begin by describing an encoder-decoder network based on a pair of RNNs. Recall the conditional RNN language model from Chapter 9 for computing p(y), the probability of a sequence y. Like any language model, we can break down the probability as follows:
- korean_translation: 한 쌍의 RNN을 기반으로 하는 인코더-디코더 네트워크를 설명하는 것부터 시작해 봅시다. 9장에서 시퀀스 $y$의 확률인 $p(y)$를 계산하기 위해 배웠던 조건부 RNN 언어 모델을 떠올려 보십시오. 다른 언어 모델들과 마찬가지로, 우리는 이 확률을 다음과 같이 분해할 수 있습니다.
- study_note: RNN 기반 인코더-디코더 모델의 동작 원리를 언어 모델의 확률 분해 관점에서 도입합니다.

## Block 5
- type: structural_formula
- source_text: p(y) = p(y1)p(y2|y1)p(y3|y1, y2)...P(ym|y1, ..., ym−1) (11.7)
- korean_translation: p(y) = p(y1)p(y2|y1)p(y3|y1, y2)...P(ym|y1, ..., ym−1) (11.7)
- study_note: 시퀀스 전체의 확률을 이전 단어들이 주어졌을 때 다음 단어가 나타날 조건부 확률들의 곱으로 나타내는 체인 룰(chain rule) 공식입니다.

## Block 6
- type: paragraph
- source_text: At a particular time t, we pass the prefix of t − 1 tokens through the language model, using forward inference to produce a sequence of hidden states, ending with the hidden state corresponding to the last word of the prefix. We then use the final hidden state of the prefix as our starting point to generate the next token.
- korean_translation: 특정 시점 $t$에서, 우리는 순방향 추론(forward inference)을 사용하여 $t-1$개 토큰의 접두사(prefix)를 언어 모델에 통과시켜 은닉 상태 시퀀스를 생성하며, 이때 접두사의 마지막 단어에 해당하는 은닉 상태에서 끝납니다. 그런 다음 이 접두사의 마지막 은닉 상태를 다음 토큰을 생성하기 위한 시작점으로 삼습니다.
- study_note: RNN 언어 모델에서 이전 정보를 함축하고 있는 마지막 은닉 상태를 활용하여 다음 단어를 생성하는 추론 과정을 설명합니다.

## Block 7
- type: paragraph
- source_text: More formally, if g is an activation function like tanh or ReLU, a function of the input at time t and the hidden state at time t − 1, and f is a softmax over the set of possible vocabulary items, then at time t the output yt and hidden state ht are computed as:
- korean_translation: 좀 더 형식적으로 표현하면, 만약 $g$가 $t$ 시점의 입력과 $t-1$ 시점의 은닉 상태에 대한 함수인 tanh나 ReLU와 같은 활성화 함수이고, $f$가 가능한 어휘 집합에 대한 소프트맥스 함수라면, $t$ 시점의 출력 $y_t$와 은닉 상태 $h_t$는 다음과 같이 계산됩니다.
- study_note: RNN의 은닉 상태 전이와 출력 계산 과정을 수식으로 정의하기 위한 전제 조건입니다.

## Block 8
- type: structural_formula
- source_text:
  ht = g(ht−1, xt) (11.8)
  yt = f(ht) (11.9)
- korean_translation:
  ht = g(ht−1, xt) (11.8)
  yt = f(ht) (11.9)
- study_note: RNN의 기본 업데이트 수식으로, 이전 은닉 상태와 현재 입력을 통해 새로운 은닉 상태를 구하고(11.8), 이를 바탕으로 출력을 생성함(11.9)을 보여줍니다.

## Block 9
- type: margin_note
- source_text: source
- korean_translation: 소스(source)
- study_note: 번역의 대상이 되는 원본 언어의 텍스트입니다.

## Block 10
- type: margin_note
- source_text: target
- korean_translation: 타깃(target)
- study_note: 번역 결과로 생성되는 목적 언어의 텍스트입니다.

## Block 11
- type: paragraph
- source_text: We only have to make one slight change to turn this language model with autoregressive generation into a translation model that can translate from a source text in one language to a target text in a second: add a sentence separation marker at the end of the source text, and then simply concatenate the target text. We briefly introduced this idea of a sentence separator token in Chapter 9 when we considered using a Transformer language model to do summarization, by training a conditional language model.
- korean_translation: 이러한 자기 회귀 생성을 활용하는 언어 모델을 한 언어의 소스 텍스트(source text)에서 두 번째 언어의 타깃 텍스트(target text)로 번역할 수 있는 번역 모델로 바꾸기 위해서는 단 한 가지만 약간 변경하면 됩니다. 즉, 소스 텍스트의 끝에 문장 구분 마커를 추가한 다음, 타깃 텍스트를 단순히 이어 붙이는 것입니다. 우리는 9장에서 조건부 언어 모델을 학습시켜 요약을 수행하는 트랜스포머 언어 모델의 활용을 고려할 때, 이러한 문장 구분 토큰의 아이디어를 간략히 소개한 바 있습니다.
- study_note: 언어 모델링 방식을 기계 번역에 적용하기 위해 소스 문장과 타깃 문장을 구분자 토큰으로 연결하여 하나의 시퀀스로 만드는 기법을 설명합니다.

## Block 12
- type: paragraph
- source_text: If we call the source text x and the target text y, we are computing the probability p(y|x) as follows:
- korean_translation: 소스 텍스트를 $x$, 타깃 텍스트를 $y$라고 한다면, 우리는 다음과 같이 조건부 확률 $p(y|x)$를 계산하게 됩니다.
- study_note: 소스 문장이 주어졌을 때 타깃 문장이 나타날 확률인 조건부 확률의 형태로 번역 문제를 정의합니다.

## Block 13
- type: structural_formula
- source_text: p(y|x) = p(y1|x)p(y2|y1, x)p(y3|y1, y2, x)...P(ym|y1, ..., ym−1, x) (11.10)
- korean_translation: p(y|x) = p(y1|x)p(y2|y1, x)p(y3|y1, y2, x)...P(ym|y1, ..., ym−1, x) (11.10)
- study_note: 모든 생성 단계에서 소스 문장 $x$의 정보가 조건으로 주어진다는 점을 보여주는 수식입니다.

## Block 14
- type: paragraph
- source_text: Fig. 11.4 shows the setup for a simplified version of the encoder-decoder model (we’ll see the full model, which requires attention, in the next section). Fig. 11.4 shows an English source text (“the green witch arrived”), a sentence separator token (<s>), and a Spanish target text (“llegó la bruja verde”). To translate a source text, we run it through the network performing forward inference to generate hidden states until we get to the end of the source. Then we begin autoregressive generation, asking for a word in the context of the hidden layer from the end of the source input as well as the end-of-sentence marker. Subsequent words are conditioned on the previous hidden state and the embedding for the last word generated.
- korean_translation: 그림 11.4는 단순화된 버전의 인코더-디코더 모델 설정을 보여줍니다(다음 절에서 어텐션(attention)이 필요한 전체 모델을 다룰 예정입니다). 그림 11.4는 영어 소스 텍스트("the green witch arrived"), 문장 구분 토큰(<s>), 그리고 스페인어 타깃 텍스트("llegó la bruja verde")를 보여줍니다. 소스 텍스트를 번역하기 위해, 우리는 소스의 끝에 도달할 때까지 순방향 추론을 수행하여 은닉 상태를 생성하며 네트워크를 통과시킵니다. 그런 다음, 소스 입력의 끝과 문장 끝 마커로부터 얻은 은닉층의 문맥에서 단어를 요청하며 자기 회귀 생성을 시작합니다. 그 이후의 단어들은 이전 은닉 상태와 마지막으로 생성된 단어의 임베딩에 따라 조건 지어집니다.
- study_note: 인코더를 통해 소스 문장의 정보를 요약한 후, 구분자 토큰을 기점으로 타깃 문장을 생성해 나가는 추론 과정을 단계별로 설명합니다.

## Block 15
- type: footnote
- source_text: 2 Later we’ll see how to use pairs of Transformers as well; it’s even possible to use separate architectures for the encoder and decoder.
- korean_translation: 2 나중에 한 쌍의 트랜스포머를 사용하는 방법도 배우게 될 것입니다. 인코더와 디코더에 서로 다른 아키텍처를 사용하는 것도 가능합니다.
- study_note: 인코더와 디코더가 반드시 같은 아키텍처일 필요는 없으며, 트랜스포머로도 구현 가능함을 명시하는 주석입니다.

# Page 8

## Block 1
- type: other
- source_text: 8 CHAPTER 11 • MACHINE TRANSLATION AND ENCODER-DECODER MODELS
- korean_translation: 8 제11장 • 기계 번역 및 인코더-디코더 모델
- study_note: 페이지 상단 헤더 영역입니다.

## Block 2
- type: figure
- source_text:
  [Figure 11.4 Diagram showing RNN-based translation process at inference time]
  Source Text: the green witch arrived
  Separator: <s>
  Target Text: llegó la bruja verde </s>
  (output of source is ignored)
- korean_translation:
  [그림 11.4 추론 시 RNN 기반 번역 과정 도식]
  소스 텍스트: the green witch arrived
  구분자: <s>
  타깃 텍스트: llegó la bruja verde </s>
  (소스의 출력은 무시됨)
- study_note: 소스 텍스트를 입력받아 은닉 상태를 축적한 뒤(Encoding), <s> 토큰을 기점으로 타깃 텍스트를 하나씩 생성하는(Decoding) 과정을 시각화한 그림입니다.

## Block 3
- type: figure_caption
- source_text: Figure 11.4 Translating a single sentence (inference time) in the basic RNN version of encoder-decoder approach to machine translation. Source and target sentences are concatenated with a separator token in between, and the decoder uses context information from the encoder’s last hidden state.
- korean_translation: 그림 11.4 기계 번역에 대한 인코더-디코더 접근법의 기본 RNN 버전에서의 단일 문장 번역(추론 시간). 소스 문장과 타깃 문장은 중간에 구분 토큰을 두고 연결되며, 디코더는 인코더의 마지막 은닉 상태로부터 문맥 정보를 사용합니다.
- study_note: 그림 11.4에 대한 캡션으로, 인코더의 마지막 은닉 상태가 문맥 전사의 핵심임을 설명합니다.

## Block 4
- type: paragraph
- source_text: Let’s formalize and generalize this model a bit in Fig. 11.5. (To help keep things straight, we’ll use the superscripts e and d where needed to distinguish the hidden states of the encoder and the decoder.) The elements of the network on the left process the input sequence x and comprise the encoder. While our simplified figure shows only a single network layer for the encoder, stacked architectures are the norm, where the output states from the top layer of the stack are taken as the final representation. A widely used encoder design makes use of stacked biLSTMs where the hidden states from top layers from the forward and backward passes are concatenated as described in Chapter 9 to provide the contextualized representations for each time step.
- korean_translation: 그림 11.5에서 이 모델을 좀 더 형식화하고 일반화해 봅시다. (혼동을 피하기 위해 인코더와 디코더의 은닉 상태를 구분할 필요가 있는 곳에 위첨자 $e$와 $d$를 사용하겠습니다.) 왼쪽의 네트워크 요소들은 입력 시퀀스 $x$를 처리하며 인코더(encoder)를 구성합니다. 단순화된 그림에서는 인코더 레이어가 하나뿐인 것으로 그려졌으나, 실제로는 여러 층을 쌓은(stacked) 아키텍처가 일반적이며, 이때 스택의 최상단 레이어에서 나온 출력 상태들이 최종 표현으로 간주됩니다. 널리 사용되는 인코더 설계는 9장에서 설명한 대로 순방향 및 역방향 패스의 최상단 레이어 은닉 상태들을 연결하여 각 시점의 문맥화된 표현을 제공하는 형태인 스택형 biLSTM을 활용합니다.
- study_note: 인코더-디코더 모델을 형식화하면서 전형적인 인코더 설계 방식(스택형 구조 및 biLSTM 활용)을 설명합니다.

## Block 5
- type: figure
- source_text:
  [Figure 11.5 A more formal encoder-decoder architecture]
  Encoder: x1, x2, x3, ..., xn -> h1e, h2e, h3e, ..., hne
  Bridge: hne = c = h0d
  Decoder: h0d -> y1, h1d -> y2, h2d -> y3, h3d -> y4, h4d -> </s>
  softmax over outputs
- korean_translation:
  [그림 11.5 더 형식적인 인코더-디코더 아키텍처]
  인코더: x1, x2, x3, ..., xn -> h1e, h2e, h3e, ..., hne
  가교: hne = c = h0d (인코더의 마지막 상태가 디코더의 초기 상태가 됨)
  디코더: h0d -> y1, h1d -> y2, h2d -> y3, h3d -> y4, h4d -> </s>
  출력에 대한 소프트맥스 적용
- study_note: 인코더의 마지막 은닉 상태($h_n^e$)가 문맥 벡터($c$)가 되어 디코더의 첫 번째 은닉 상태($h_0^d$)로 초기화되는 구조를 수식적으로 보여주는 도식입니다.

## Block 6
- type: figure_caption
- source_text: Figure 11.5 A more formal version of translating a sentence at inference time in the basic RNN-based encoder-decoder architecture. The final hidden state of the encoder RNN, hne , serves as the context for the decoder in its role as hd 0 in the decoder RNN.
- korean_translation: 그림 11.5 기본 RNN 기반 인코더-디코더 아키텍처에서 추론 시 문장을 번역하는 과정의 더 형식적인 버전. 인코더 RNN의 마지막 은닉 상태인 $h_n^e$는 디코더 RNN에서 $h_0^d$의 역할을 하며 디코더의 문맥으로 기능합니다.
- study_note: 그림 11.5에 대한 캡션입니다.

## Block 7
- type: paragraph
- source_text: The entire purpose of the encoder is to generate a contextualized representation of the input. This representation is embodied in the final hidden state of the encoder, hne . This representation, also called c for context, is then passed to the decoder.
- korean_translation: 인코더의 전체적인 목적은 입력에 대한 문맥화된 표현을 생성하는 것입니다. 이 표현은 인코더의 마지막 은닉 상태인 $h_n^e$에 구현되어 있습니다. 흔히 문맥이라 불리는 $c$로도 명칭되는 이 표현은 그 후 디코더로 전달됩니다.
- study_note: 인코더의 존재 이유와 문맥 벡터 생성 과정을 강조합니다.

## Block 8
- type: paragraph
- source_text: The decoder network on the right takes this state and uses it to initialize the first
- korean_translation: 오른쪽에 있는 디코더 네트워크는 이 상태를 받아 첫 번째 [은닉 상태를 초기화하는 데 사용합니다...]
- study_note: 디코더가 인코더로부터 정보를 넘겨받아 생성을 시작하는 첫 단계를 설명하는 부분입니다. (결말 미완성)
