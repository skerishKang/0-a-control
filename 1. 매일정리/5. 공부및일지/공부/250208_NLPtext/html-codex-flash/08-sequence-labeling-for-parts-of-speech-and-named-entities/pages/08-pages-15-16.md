# Page 15

## Block 1
- type: other
- source_text: 8.5 • CONDITIONAL RANDOM FIELDS (CRFS) 15
- korean_translation: 8.5 • 조건부 무작위장 (CRF) 15
- study_note: 8.5 섹션의 제목과 페이지 번호입니다.

## Block 2
- type: paragraph
- source_text: and the observation likelihood of the word Janet given the tag for that cell. Most of the cells in the column are zero since the word Janet cannot be any of those tags. The reader should find this in Fig. 8.14.
- korean_translation: [이전 페이지에서 계속] 그리고 해당 셀의 태그가 주어졌을 때 단어 'Janet'의 관측 우도입니다. 'Janet'이 해당 태그들 중 어느 것도 될 수 없으므로 열의 대부분의 셀은 0입니다. 독자는 그림 8.14에서 이를 확인할 수 있을 것입니다.
- study_note: 앞 페이지에서 이어진 비터비 알고리즘의 초기화 단계 설명을 마무리합니다.

## Block 3
- type: paragraph
- source_text: Next, each cell in the will column gets updated. For each state, we compute the value viterbi[s,t] by taking the maximum over the extensions of all the paths from the previous column that lead to the current cell according to Eq. 8.19. We have shown the values for the MD, VB, and NN cells. Each cell gets the max of the 7 values from the previous column, multiplied by the appropriate transition probability; as it happens in this case, most of them are zero from the previous column. The remaining value is multiplied by the relevant observation probability, and the (trivial) max is taken. In this case the final value, 2.772e-8, comes from the NNP state at the previous column. The reader should fill in the rest of the lattice in Fig. 8.14 and backtrace to see whether or not the Viterbi algorithm returns the gold state sequence NNP MD VB DT NN.
- korean_translation: 다음으로 'will' 열의 각 셀이 업데이트됩니다. 각 상태에 대해, 식 8.19에 따라 이전 열에서 현재 셀로 이어지는 모든 경로의 연장선상에서 최댓값을 취하여 *viterbi[s,t]* 값을 계산합니다. 우리는 MD, VB, NN 셀에 대한 값들을 보여 드렸습니다. 각 셀은 이전 열의 7개 값에 적절한 전이 확률을 곱한 것들 중 최댓값을 취합니다. 이 사례에서는 이전 열의 값들 대부분이 0입니다. 남은 값에 관련 관측 확률을 곱하고 (자명한) 최댓값을 취합니다. 이 경우 최종 값인 $2.772 \times 10^{-8}$은 이전 열의 NNP 상태로부터 온 것입니다. 독자는 그림 8.14의 나머지 격자를 채우고 역추적을 수행하여, 비터비 알고리즘이 정답 상태 시퀀스인 NNP MD VB DT NN을 반환하는지 확인해 보아야 합니다.
- study_note: 비터비 재귀 단계와 역추적을 통해 최종 정답을 도출하는 과정을 구체적인 수치와 함께 설명합니다.

## Block 4
- type: section_title
- source_text: 8.5 Conditional Random Fields (CRFs)
- korean_translation: 8.5 조건부 무작위장 (CRF)
- study_note: HMM의 대안이자 발전된 형태인 판별 모델 CRF를 소개하는 섹션입니다.

## Block 5
- type: margin_note
- source_text: unknown words
- korean_translation: 미등록어(unknown words)
- study_note: 학습 데이터에 포함되지 않아 모델이 알지 못하는 새로운 단어들입니다.

## Block 6
- type: paragraph
- source_text: While the HMM is a useful and powerful model, it turns out that HMMs need a number of augmentations to achieve high accuracy. For example, in POS tagging as in other tasks, we often run into unknown words: proper names and acronyms are created very often, and even new common nouns and verbs enter the language at a surprising rate. It would be great to have ways to add arbitrary features to help with this, perhaps based on capitalization or morphology (words starting with capital letters are likely to be proper nouns, words ending with -ed tend to be past tense (VBD or VBN), etc.) Or knowing the previous or following words might be a useful feature (if the previous word is the, the current tag is unlikely to be a verb).
- korean_translation: HMM은 유용하고 강력한 모델이지만, 높은 정확도를 얻기 위해서는 많은 보강이 필요하다는 사실이 밝혀졌습니다. 예를 들어 품사 태깅이나 다른 작업에서 우리는 종종 미등록어(unknown words) 문제에 직면합니다. 고유 명사나 약어는 매우 자주 만들어지며, 새로운 일반 명사와 동사도 놀라운 속도로 언어에 유입됩니다. 이를 돕기 위해 임의의 특징(features)을 추가할 수 있는 방법이 있다면 좋을 것입니다. 예를 들어 대문자 사용 여부나 형태소 정보(대문자로 시작하는 단어는 고유 명사일 가능성이 높고, *-ed*로 끝나는 단어는 과거 시제(VBD 혹은 VBN)일 가능성이 높은 등)를 활용하는 것입니다. 또는 이전이나 다음 단어를 아는 것도 유용한 특징이 될 수 있습니다(이전 단어가 'the'라면 현재 태그가 동사일 가능성은 낮습니다).
- study_note: HMM의 태생적 한계인 '특징 활용의 어려움'과 이를 해결하기 위한 지점들을 짚어봅니다. 풍부한 문맥 정보(특징)를 반영하는 것이 성능 향상의 핵심입니다.

## Block 7
- type: paragraph
- source_text: Although we could try to hack the HMM to find ways to incorporate some of these, in general it’s hard for generative models like HMMs to add arbitrary features directly into the model in a clean way. We’ve already seen a model for combining arbitrary features in a principled way: log-linear models like the logistic regression model of Chapter 5! But logistic regression isn’t a sequence model; it assigns a class to a single observation.
- korean_translation: 이러한 특징들 중 일부를 포함시키기 위해 HMM을 개조해 볼 수도 있겠지만, 일반적으로 HMM과 같은 생성 모델(generative models)에 임의의 특징들을 깔끔한 방식으로 직접 추가하기는 어렵습니다. 우리는 이미 원칙적인 방식으로 임의의 특징들을 결합하는 모델을 보았습니다. 바로 5장의 로지스틱 회귀 모델과 같은 로그 선형 모델(log-linear models)입니다! 하지만 로지스틱 회귀는 시퀀스 모델이 아니며, 단일 관측치에 대해 하나의 부류를 할당할 뿐입니다.
- study_note: HMM(생성 모델)과 로지스틱 회귀(로그 선형 모델)의 장단점을 비교하며, 두 장점을 결합한 시퀀스 모델의 필요성을 역설합니다.

## Block 8
- type: margin_note
- source_text: CRF
- korean_translation: CRF
- study_note: Conditional Random Field의 약자로, 시퀀스 레이블링을 위한 대표적인 판별 모델입니다.

## Block 9
- type: paragraph
- source_text: Luckily, there is a discriminative sequence model based on log-linear models: the conditional random field (CRF). We’ll describe here the linear chain CRF, the version of the CRF most commonly used for language processing, and the one whose conditioning closely matches the HMM.
- korean_translation: 다행히 로그 선형 모델에 기반한 판별 시퀀스 모델(discriminative sequence model)이 존재합니다. 바로 조건부 무작위장(CRF)입니다. 여기서는 언어 처리에서 가장 흔하게 사용되며 HMM의 조건 설정과 밀접하게 일치하는 버전인 선형 체인 CRF(linear chain CRF)에 대해 설명하겠습니다.
- study_note: CRF의 정의와 그중에서도 자연어 처리에서 널리 쓰이는 선형 체인 CRF를 소개합니다.

## Block 10
- type: paragraph
- source_text: Assuming we have a sequence of input words X = x_1 . . . x_n and want to compute a sequence of output tags Y = y_1 . . . y_n . In an HMM to compute the best tag sequence that maximizes P(Y|X) we rely on Bayes’ rule and the likelihood P(X|Y):
- korean_translation: 입력 단어들의 시퀀스 $X = x_1 \dots x_n$이 있고 출력 태그 시퀀스 $Y = y_1 \dots y_n$을 계산하고 싶다고 가정해 봅시다. HMM에서 $P(Y \mid X)$를 최대로 만드는 최적의 태그 시퀀스를 계산하기 위해 우리는 베이즈 정리와 우도 $P(X \mid Y)$에 의존했습니다.
- study_note: HMM의 접근 방식을 다시 상기시켜 CRF와의 차이점을 대비시키려 합니다.

## Block 11
- type: formula
- source_text:
  \hat{Y} = \underset{Y}{argmax} p(Y|X)
  = \underset{Y}{argmax} p(X|Y)p(Y)
  = \underset{Y}{argmax} \prod_{i} p(x_i | y_i) \prod_{i} p(y_i | y_{i-1}) (8.21)
- korean_translation:
  $\hat{Y} = \underset{Y}{\text{argmax }} p(Y \mid X)$
  $= \underset{Y}{\text{argmax }} p(X \mid Y)p(Y)$
  $= \underset{Y}{\text{argmax }} \prod_{i} p(x_i \mid y_i) \prod_{i} p(y_i \mid y_{i-1})$ (8.21)
- study_note: HMM의 목적 함수를 수식으로 정리한 것입니다. 베이즈 정리를 통해 우도와 사전 확률의 곱으로 표현됩니다.

## Block 12
- type: paragraph
- source_text: In a CRF, by contrast, we compute the posterior p(Y|X) directly, training the
- korean_translation: 이와 대조적으로 CRF에서는 사후 확률 $p(Y \mid X)$를 직접 계산하며, [다음 페이지에서 계속]
- study_note: HMM과 달리 사후 확률을 직접 모델링하는 판별 모델로서의 CRF의 특징을 강조합니다.

# Page 16

## Block 1
- type: other
- source_text: 16 CHAPTER 8 • SEQUENCE LABELING FOR PARTS OF SPEECH AND NAMED ENTITIES
- korean_translation: 16 제8장 • 품사 태깅과 개체명 인식을 위한 시퀀스 레이블링
- study_note: 페이지 상단 헤더입니다.

## Block 2
- type: paragraph
- source_text: CRF to discriminate among the possible tag sequences:
- korean_translation: [이전 페이지에서 계속] 가능한 태그 시퀀스들 사이를 판별하도록 CRF를 학습시킵니다.
- study_note: CRF의 판별적(discriminative) 학습 목적을 명시합니다.

## Block 3
- type: formula
- source_text: \hat{Y} = \underset{Y \in \mathcal{Y}}{argmax} P(Y|X) (8.22)
- korean_translation: $\hat{Y} = \underset{Y \in \mathcal{Y}}{\text{argmax }} P(Y \mid X)$ (8.22)
- study_note: 조건부 확률을 직접 최대화하는 CRF의 목적 함수입니다.

## Block 4
- type: paragraph
- source_text: However, the CRF does not compute a probability for each tag at each time step. Instead, at each time step the CRF computes log-linear functions over a set of relevant features, and these local features are aggregated and normalized to produce a global probability for the whole sequence.
- korean_translation: 하지만 CRF는 각 시간 단계마다 각 태그에 대한 확률을 계산하지 않습니다. 대신 CRF는 각 시간 단계에서 관련 특징들의 집합에 대해 로그 선형 함수를 계산하며, 이러한 지역적 특징(local features)들이 집계되고 정규화되어 전체 시퀀스에 대한 전역 확률(global probability)을 생성합니다.
- study_note: CRF가 시점별로 독립적 확률을 계산하는 것이 아니라, 전체 시퀀스의 에너지를 계산하고 이를 정규화하는 구조임을 설명합니다.

## Block 5
- type: paragraph
- source_text: Let’s introduce the CRF more formally, again using X and Y as the input and output sequences. A CRF is a log-linear model that assigns a probability to an entire output (tag) sequence Y, out of all possible sequences \mathcal{Y}, given the entire input (word) sequence X. We can think of a CRF as like a giant version of what multinomial logistic regression does for a single token. Recall that the feature function f in regular multinomial logistic regression can be viewed as a function of a tuple: a token x and a label y (page ??). In a CRF, the function F maps an entire input sequence X and an entire output sequence Y to a feature vector. Let’s assume we have K features, with a weight w_k for each feature F_k:
- korean_translation: 입력 및 출력 시퀀스로 다시 $X$와 $Y$를 사용하여 CRF를 좀 더 형식적으로 도입해 봅시다. CRF는 전체 입력(단어) 시퀀스 $X$가 주어졌을 때, 가능한 모든 시퀀스 집합 $\mathcal{Y}$ 중에서 전체 출력(태그) 시퀀스 $Y$에 확률을 할당하는 로그 선형 모델입니다. 우리는 CRF를 단일 토큰에 대해 다항 로지스틱 회귀가 수행하는 작업의 거대한 버전으로 생각할 수 있습니다. 일반적인 다항 로지스틱 회귀의 특징 함수 $f$가 토큰 $x$와 레이블 $y$의 튜플에 대한 함수로 간주될 수 있음을 상기하십시오. CRF에서 함수 $F$는 전체 입력 시퀀스 $X$와 전체 출력 시퀀스 $Y$를 특징 벡터로 매핑합니다. $K$개의 특징이 있고 각 특징 $F_k$에 대한 가중치 $w_k$가 있다고 가정해 보겠습니다.
- study_note: 다항 로지스틱 회귀(Softmax)를 시퀀스 단위로 확장한 것이 CRF라는 개념적 연결을 제공합니다.

## Block 6
- type: formula
- source_text:
  p(Y|X) = \frac{exp (\sum_{k=1}^K w_k F_k(X, Y))}{\sum_{Y' \in \mathcal{Y}} exp (\sum_{k=1}^K w_k F_k(X, Y'))} (8.23)
- korean_translation:
  $p(Y \mid X) = \frac{\exp \left(\sum_{k=1}^K w_k F_k(X, Y)\right)}{\sum_{Y' \in \mathcal{Y}} \exp \left(\sum_{k=1}^K w_k F_k(X, Y')\right)}$ (8.23)
- study_note: CRF의 기본 확률 정의식입니다. 분자는 특정 시퀀스 $Y$의 점수(score)의 지수승이며, 분모는 정규화를 위해 가능한 모든 시퀀스 조합에 대한 점수 합입니다.

## Block 7
- type: paragraph
- source_text: It’s common to also describe the same equation by pulling out the denominator into a function Z(X):
- korean_translation: 분모를 함수 $Z(X)$로 빼내어 동일한 식을 다음과 같이 기술하는 것이 일반적입니다.
- study_note: 수식의 가독성을 위해 정규화 상수(Partition Function) $Z(X)$를 정의하는 관습을 설명합니다.

## Block 8
- type: formula
- source_text:
  p(Y|X) = \frac{1}{Z(X)} exp (\sum_{k=1}^K w_k F_k(X, Y)) (8.24)
  Z(X) = \sum_{Y' \in \mathcal{Y}} exp (\sum_{k=1}^K w_k F_k(X, Y')) (8.25)
- korean_translation:
  $p(Y \mid X) = \frac{1}{Z(X)} \exp \left(\sum_{k=1}^K w_k F_k(X, Y)\right)$ (8.24)
  $Z(X) = \sum_{Y' \in \mathcal{Y}} \exp \left(\sum_{k=1}^K w_k F_k(X, Y')\right)$ (8.25)
- study_note: 정규화 상수 $Z(X)$를 사용한 CRF의 정형화된 표현식입니다.

## Block 9
- type: paragraph
- source_text: We’ll call these K functions F_k(X,Y) global features, since each one is a property of the entire input sequence X and output sequence Y. We compute them by decomposing into a sum of local features for each position i in Y:
- korean_translation: 우리는 이 $K$개의 함수 $F_k(X, Y)$를 전역 특징(global features)이라고 부를 것인데, 이는 각 함수가 전체 입력 시퀀스 $X$와 출력 시퀀스 $Y$의 속성이기 때문입니다. 우리는 이를 $Y$의 각 위치 $i$에 대한 지역 특징(local features)들의 합으로 분해하여 계산합니다.
- study_note: 전역 특징을 계산 가능한 시점별 지역 특징으로 분해하는 과정을 설명합니다.

## Block 10
- type: formula
- source_text: F_k(X, Y) = \sum_{i=1}^n f_k(y_{i−1}, y_i, X, i) (8.26)
- korean_translation: $F_k(X, Y) = \sum_{i=1}^n f_k(y_{i-1}, y_i, X, i)$ (8.26)
- study_note: 전역 특징 $F_k$가 각 시점($i$)의 지역 특징 $f_k$들의 단순 합으로 정의됨을 보여 줍니다. 여기서 지역 특징은 현재 태그, 이전 태그, 그리고 입력 전체를 참조할 수 있습니다.

## Block 11
- type: paragraph
- source_text: Each of these local features f_k in a linear-chain CRF is allowed to make use of the current output token y_i, the previous output token y_{i−1}, the entire input string X (or any subpart of it), and the current position i. This constraint to only depend on the current and previous output tokens y_i and y_{i−1} are what characterizes a linear chain CRF. As we will see, this limitation makes it possible to use versions of the efficient Viterbi and Forward-Backwards algorithms from the HMM. A general CRF, by contrast, allows a feature to make use of any output token, and are thus necessary for tasks in which the decision depend on distant output tokens, like y_{i−4}. General CRFs require more complex inference, and are less commonly used for language processing.
- korean_translation: 선형 체인 CRF에서 각 지역 특징 $f_k$는 현재 출력 토큰 $y_i$, 이전 출력 토큰 $y_{i-1}$, 전체 입력 문자열 $X$(또는 그 일부), 그리고 현재 위치 $i$를 활용할 수 있습니다. 오직 현재와 이전의 출력 토큰($y_i$와 $y_{i-1}$)에만 의존해야 한다는 이 제약조건이 바로 선형 체인 CRF(linear chain CRF)의 특징입니다. 나중에 보겠지만, 이러한 제약 덕분에 HMM에서 사용했던 효율적인 비터비 및 전진-후진(Forward-Backward) 알고리즘의 변형들을 사용할 수 있게 됩니다. 이와 대조적으로 일반적인 CRF(general CRF)는 특징이 어떠한 출력 토큰이든 활용할 수 있게 하므로, 의사결정이 $y_{i-4}$와 같이 멀리 떨어진 출력 토큰에 의존하는 작업에 필요합니다. 일반적인 CRF는 더 복잡한 추론을 필요로 하며 언어 처리에서는 상대적으로 덜 사용됩니다.
- study_note: 선형 체인 CRF의 정의와 핵심 제약 조건(오직 직전 상태만 참조)을 설명합니다. 이 제약이 효율적인 연산을 보장해 준다는 점이 중요합니다. 또한 일반 CRF와의 차이점도 명확히 합니다.

## Block 12
- type: margin_note
- source_text: linear chain CRF
- korean_translation: 선형 체인 CRF(linear chain CRF)
- study_note: 상태 간의 전이가 선형적인 구조(직전 상태에만 의존)를 갖는 특수한 형태의 CRF입니다.
