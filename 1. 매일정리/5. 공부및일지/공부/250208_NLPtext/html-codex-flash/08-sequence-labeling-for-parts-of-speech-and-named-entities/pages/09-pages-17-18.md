# Page 17

## Block 1
- type: other
- source_text: 8.5 • CONDITIONAL RANDOM FIELDS (CRFS) 17
- korean_translation: 8.5 • 조건부 무작위장 (CRF) 17
- study_note: 8.5 섹션의 제목과 페이지 번호입니다.

## Block 2
- type: subsection_title
- source_text: 8.5.1 Features in a CRF POS Tagger
- korean_translation: 8.5.1 CRF 품사 태거의 특징(Features)
- study_note: CRF 품사 태거에서 사용되는 다양한 특징(features)들의 구체적인 예시를 다루는 하위 섹션입니다.

## Block 3
- type: paragraph
- source_text: Let’s look at some of these features in detail, since the reason to use a discriminative sequence model is that it’s easy to incorporate a lot of features.²
- korean_translation: 판별 시퀀스 모델을 사용하는 이유는 많은 특징들을 포함시키기 쉽기 때문이므로, 이러한 특징들 중 일부를 자세히 살펴보겠습니다.²
- study_note: CRF의 가장 큰 장점인 '자유로운 특징 활용'의 측면에서 세부 내용을 살펴봅니다.

## Block 4
- type: paragraph
- source_text: Again, in a linear-chain CRF, each local feature f_k at position i can depend on any information from: (y_{i−1}, y_i, X, i). So some legal features representing common situations might be the following:
- korean_translation: 다시 말하지만, 선형 체인 CRF에서 각 시점 $i$의 지역 특징 $f_k$는 $(y_{i-1}, y_i, X, i)$로부터 얻은 어떠한 정보에도 의존할 수 있습니다. 따라서 일반적인 상황을 나타내는 몇 가지 적법한 특징들은 다음과 같을 수 있습니다.
- study_note: 지역 특징이 참조할 수 있는 정보의 범위(현재/이전 태그, 입력 전체, 현재 위치)를 명시합니다.

## Block 5
- type: other
- source_text:
  𝟙{x_i = the, y_i = DET}
  𝟙{y_i = PROPN, x_{i+1} = Street, y_{i−1} = NUM}
  𝟙{y_i = VERB, y_{i−1} = AUX}
- korean_translation:
  $\mathbb{1}\{x_i = \text{the}, y_i = \text{DET}\}$
  $\mathbb{1}\{y_i = \text{PROPN}, x_{i+1} = \text{Street}, y_{i-1} = \text{NUM}\}$
  $\mathbb{1}\{y_i = \text{VERB}, y_{i-1} = \text{AUX}\}$
- study_note: 지시 함수(indicator function)를 사용한 특징의 예입니다. 특정 조건(단어의 정체, 주변 단어, 주변 태그 등)이 만족되면 1, 아니면 0의 값을 갖습니다.

## Block 6
- type: paragraph
- source_text: For simplicity, we’ll assume all CRF features take on the value 1 or 0. Above, we explicitly use the notation 𝟙{x} to mean “1 if x is true, and 0 otherwise”. From now on, we’ll leave off the 𝟙 when we define features, but you can assume each feature has it there implicitly.
- korean_translation: 단순화를 위해 모든 CRF 특징이 1 또는 0의 값을 갖는다고 가정하겠습니다. 위에서는 $x$가 참이면 1이고 그렇지 않으면 0임을 의미하는 $\mathbb{1}\{x\}$ 표기법을 명시적으로 사용했습니다. 앞으로는 특징을 정의할 때 이 $\mathbb{1}$ 표기를 생략하겠지만, 각 특징에 암묵적으로 포함되어 있다고 가정하셔도 됩니다.
- study_note: 앞으로의 표기 규칙을 정립합니다.

## Block 7
- type: margin_note
- source_text: feature templates
- korean_translation: 특징 템플릿(feature templates)
- study_note: 데이터로부터 구체적인 특징들을 자동으로 생성해 내기 위한 일종의 설계도입니다.

## Block 8
- type: paragraph
- source_text: Although the idea of what features to use is done by the system designer by hand, the specific features are automatically populated by using feature templates as we briefly mentioned in Chapter 5. Here are some templates that only use information from y_{i−1}, y_i, X, i:
- korean_translation: 어떤 특징을 사용할지에 대한 아이디어는 시스템 설계자가 수동으로 결정하지만, 구체적인 특징들은 5장에서 잠시 언급했듯이 특징 템플릿(feature templates)을 사용하여 자동으로 채워집니다. 다음은 $y_{i-1}, y_i, X, i$의 정보만을 사용하는 몇 가지 템플릿입니다.
- study_note: 사람이 일일이 수만 개의 특징을 만드는 것이 아니라, 템플릿을 정의하면 데이터에서 자동으로 구체적인 특징 집합이 생성됨을 설명합니다.

## Block 9
- type: formula
- source_text: ⟨y_i, x_i⟩, ⟨y_i, y_{i−1}⟩, ⟨y_i, x_{i−1}, x_{i+2}⟩
- korean_translation: $\langle y_i, x_i \rangle, \langle y_i, y_{i-1} \rangle, \langle y_i, x_{i-1}, x_{i+2} \rangle$
- study_note: 템플릿의 형식적 예시입니다. 예를 들어 $\langle y_i, x_i \rangle$ 템플릿은 "현재 단어가 'the'이고 현재 태그가 'DET'인가?"와 같은 수많은 개별 특징들을 생성해 냅니다.

## Block 10
- type: paragraph
- source_text: These templates automatically populate the set of features from every instance in the training and test set. Thus for our example Janet/NNP will/MD back/VB the/DT bill/NN, when x_i is the word back, the following features would be generated and have the value 1 (we’ve assigned them arbitrary feature numbers):
- korean_translation: 이러한 템플릿들은 학습 및 테스트 세트의 모든 사례로부터 특징 집합을 자동으로 생성합니다. 따라서 우리의 예제 "Janet/NNP will/MD back/VB the/DT bill/NN"에서 $x_i$가 단어 'back'일 때, 다음과 같은 특징들이 생성되어 1의 값을 갖게 됩니다(임의의 특징 번호를 부여했습니다).
- study_note: 템플릿이 실제 문장에 적용되어 구체적인 특징 인스턴스로 변환되는 과정을 보여 줍니다.

## Block 11
- type: other
- source_text:
  f_{3743} : y_i = VB and x_i = back
  f_{156} : y_i = VB and y_{i−1} = MD
  f_{99732} : y_i = VB and x_{i−1} = will and x_{i+2} = bill
- korean_translation:
  $f_{3743} : y_i = \text{VB}$ 이고 $x_i = \text{back}$
  $f_{156} : y_i = \text{VB}$ 이고 $y_{i-1} = \text{MD}$
  $f_{99732} : y_i = \text{VB}$ 이고 $x_{i-1} = \text{will}$ 이고 $x_{i+2} = \text{bill}$
- study_note: 특정 문맥에서 활성화된(값 1을 갖는) 구체적인 특징들의 예시입니다.

## Block 12
- type: margin_note
- source_text: word shape
- korean_translation: 단어 형태(word shape)
- study_note: 단어의 구체적인 철자 대신 대/소문자, 숫자, 기호 등의 추상적 패턴을 나타내는 특징입니다.

## Block 13
- type: paragraph
- source_text: It’s also important to have features that help with unknown words. One of the most important is word shape features, which represent the abstract letter pattern of the word by mapping lower-case letters to ‘x’, upper-case to ‘X’, numbers to ‘d’, and retaining punctuation. Thus for example I.M.F would map to X.X.X. and DC10-30 would map to XXdd-dd. A second class of shorter word shape features is also used. In these features consecutive character types are removed, so words in all caps map to X, words with initial-caps map to Xx, DC10-30 would be mapped to Xd-d but I.M.F would still map to X.X.X. Prefix and suffix features are also useful. In summary, here are some sample feature templates that help with unknown words:
- korean_translation: 미등록어 문제를 돕는 특징들을 갖는 것도 중요합니다. 가장 중요한 것 중 하나는 단어 형태(word shape) 특징으로, 이는 소문자는 'x', 대문자는 'X', 숫자는 'd'로 매핑하고 문장 부호는 그대로 유지함으로써 단어의 추상적인 글자 패턴을 표현합니다. 예를 들어 'I.M.F'는 'X.X.X'로, 'DC10-30'은 'XXdd-dd'로 매핑됩니다. 두 번째로 더 짧은 형태의 단어 형태 특징도 사용됩니다. 이 특징에서는 연속된 동일 유형 글자가 제거되므로, 전체가 대문자인 단어는 'X'로, 첫 글자만 대문자인 단어는 'Xx'로 매핑되고, 'DC10-30'은 'Xd-d'로 매핑되지만 'I.M.F'는 여전히 'X.X.X'로 매핑됩니다. 접두사와 접미사 특징 또한 유용합니다. 요약하자면, 미등록어 처리에 도움이 되는 몇 가지 샘플 특징 템플릿은 다음과 같습니다.
- study_note: 미등록어의 품사를 추론하기 위해 쓰이는 기법들을 설명합니다. 단어의 구체적 의미를 몰라도 그 외형(대문자 여부, 접미사 등)을 통해 힌트를 얻을 수 있습니다.

## Block 14
- type: other
- source_text:
  x_i contains a particular prefix (perhaps from all prefixes of length ≤ 2)
  x_i contains a particular suffix (perhaps from all suffixes of length ≤ 2)
  x_i’s word shape
  x_i’s short word shape
- korean_translation:
  $x_i$가 특정 접두사를 포함함 (아마도 길이가 2 이하인 모든 접두사 중 일부)
  $x_i$가 특정 접미사를 포함함 (아마도 길이가 2 이하인 모든 접미사 중 일부)
  $x_i$의 단어 형태
  $x_i$의 짧은 단어 형태
- study_note: 미등록어 대응을 위한 대표적인 템플릿 종류입니다.

## Block 15
- type: other
- source_text: ² Because in HMMs all computation is based on the two probabilities P(tag|tag) and P(word|tag), if we want to include some source of knowledge into the tagging process, we must find a way to encode the knowledge into one of these two probabilities. Each time we add a feature we have to do a lot of complicated conditioning which gets harder and harder as we have more and more such features.
- korean_translation: ² HMM에서는 모든 계산이 $P(\text{태그} \mid \text{태그})$와 $P(\text{단어} \mid \text{태그})$라는 두 확률에 기반하기 때문에, 태깅 과정에 어떤 지식 정보원(knowledge source)을 포함시키고 싶다면 그 지식을 이 두 확률 중 하나에 인코딩할 방법을 찾아야만 합니다. 특징을 추가할 때마다 매우 복잡한 조건부 확률 설정을 해야 하며, 이러한 특징이 많아질수록 작업은 점점 더 어려워집니다.
- study_note: HMM에서 특징을 추가하는 것이 왜 어려운지를 확률론적 구조의 관점에서 설명한 각주입니다.

# Page 18

## Block 1
- type: other
- source_text: 18 CHAPTER 8 • SEQUENCE LABELING FOR PARTS OF SPEECH AND NAMED ENTITIES
- korean_translation: 18 제8장 • 품사 태깅과 개체명 인식을 위한 시퀀스 레이블링
- study_note: 페이지 상단 헤더입니다.

## Block 2
- type: other
- source_text:
  prefix(x_i) = w
  prefix(x_i) = we
  suffix(x_i) = ed
  suffix(x_i) = d
  word-shape(x_i) = xxxx-xxxxxxx
  short-word-shape(x_i) = x-x
- korean_translation:
  prefix($x_i$) = w
  prefix($x_i$) = we
  suffix($x_i$) = ed
  suffix($x_i$) = d
  word-shape($x_i$) = xxxx-xxxxxxx
  short-word-shape($x_i$) = x-x
- study_note: "well-dressed" 같은 단어에서 추출될 수 있는 특징 값의 예시입니다. 이를 통해 모델이 처음 보는 단어라도 '과거분사형 형용사'일 가능성을 추론할 수 있게 됩니다.

## Block 3
- type: paragraph
- source_text: The known-word templates are computed for every word seen in the training set; the unknown word features can also be computed for all words in training, or only on training words whose frequency is below some threshold. The result of the known-word templates and word-signature features is a very large set of features. Generally a feature cutoff is used in which features are thrown out if they have count < 5 in the training set.
- korean_translation: 기등록어(known-word) 템플릿은 학습 세트에서 관찰된 모든 단어에 대해 계산됩니다. 미등록어 특징 역시 학습 과정의 모든 단어에 대해 계산되거나, 혹은 빈도가 특정 임계값 이하인 학습 단어들에 대해서만 계산될 수 있습니다. 기등록어 템플릿과 단어 서명(word-signature) 특징의 결과로 매우 방대한 특징 집합이 만들어집니다. 일반적으로 학습 세트에서의 빈도가 5회 미만인 특징들을 제거하는 특징 차단(feature cutoff) 기법이 사용됩니다.
- study_note: 방대한 양의 자동 생성 특징들을 효율적으로 관리하기 위한 필터링 전략(특징 차단)을 설명합니다.

## Block 4
- type: paragraph
- source_text: Remember that in a CRF we don’t learn weights for each of these local features f_k. Instead, we first sum the values of each local feature (for example feature f_{3743}) over the entire sentence, to create each global feature (for example F_{3743}). It is those global features that will then be multiplied by weight w_{3743}. Thus for training and inference there is always a fixed set of K features with K weights, even though the length of each sentence is different.
- korean_translation: CRF에서 우리가 각각의 지역 특징 $f_k$에 대해 개별적인 가중치를 학습하는 것이 아니라는 점을 기억하십시오. 대신, 먼저 전체 문장에 걸쳐 각 지역 특징(예: 특징 $f_{3743}$)의 값들을 합산하여 각각의 전역 특징(예: $F_{3743}$)을 생성합니다. 그런 다음 가중치 $w_{3743}$이 곱해지는 것은 바로 이 전역 특징들입니다. 따라서 각 문장의 길이가 다르더라도 학습과 추론 과정에는 항상 $K$개의 가중치를 가진 고정된 $K$개의 특징 집합이 존재하게 됩니다.
- study_note: 시점별 지역 특징을 문장 전체의 전역 특징으로 합산하여 고정된 개수의 가중치를 학습하는 CRF의 아키텍처를 명확히 합니다.

## Block 5
- type: subsection_title
- source_text: 8.5.2 Features for CRF Named Entity Recognizers
- korean_translation: 8.5.2 CRF 개체명 인식기를 위한 특징(Features)
- study_note: 품사 태깅에 이어 개체명 인식(NER)에서 유용하게 쓰이는 특징들을 다룹니다.

## Block 6
- type: paragraph
- source_text: A CRF for NER makes use of very similar features to a POS tagger, as shown in Figure 8.15.
- korean_translation: NER을 위한 CRF는 그림 8.15에 나타난 것처럼 품사 태거와 매우 유사한 특징들을 활용합니다.
- study_note: NER과 POS 태깅이 특징 활용 측면에서 많은 부분을 공유함을 시사합니다.

## Block 7
- type: table
- source_text:
  | Feature |
  | :--- |
  | identity of w_i, identity of neighboring words |
  | embeddings for w_i, embeddings for neighboring words |
  | part of speech of w_i, part of speech of neighboring words |
  | presence of w_i in a **gazetteer** |
  | w_i contains a particular prefix (from all prefixes of length ≤ 4) |
  | w_i contains a particular suffix (from all suffixes of length ≤ 4) |
  | word shape of w_i, word shape of neighboring words |
  | short word shape of w_i, short word shape of neighboring words |
  | gazetteer features |
- korean_translation:
  | 특징 |
  | :--- |
  | $w_i$의 식별 정보, 주변 단어들의 식별 정보 |
  | $w_i$의 임베딩, 주변 단어들의 임베딩 |
  | $w_i$의 품사, 주변 단어들의 품사 |
  | $w_i$의 **지명 사전(gazetteer)** 포함 여부 |
  | $w_i$가 특정 접두사를 포함함 (길이가 4 이하인 모든 접두사 중 일부) |
  | $w_i$가 특정 접미사를 포함함 (길이가 4 이하인 모든 접미사 중 일부) |
  | $w_i$의 단어 형태, 주변 단어들의 단어 형태 |
  | $w_i$의 짧은 단어 형태, 주변 단어들의 짧은 단어 형태 |
  | 지명 사전 특징 |
- study_note: NER 시스템에서 전형적으로 사용되는 특징 목록입니다. 단어 자체뿐만 아니라 임베딩, 품사 정보, 그리고 지명 사전 같은 외부 지식이 추가된 것이 특징입니다.

## Block 8
- type: figure_caption
- source_text: Figure 8.15 Typical features for a feature-based NER system.
- korean_translation: 그림 8.15 특징 기반 NER 시스템을 위한 전형적인 특징들.
- study_note: 그림 8.15에 대한 캡션입니다.

## Block 9
- type: margin_note
- source_text: gazetteer
- korean_translation: 지명 사전(gazetteer)
- study_note: 지리적인 장소나 위치의 이름들을 모아놓은 목록 또는 사전입니다.

## Block 10
- type: paragraph
- source_text: One feature that is especially useful for locations is a gazetteer, a list of place names, often providing millions of entries for locations with detailed geographical and political information.³ This can be implemented as a binary feature indicating a phrase appears in the list. Other related resources like name-lists, for example from the United States Census Bureau⁴, can be used, as can other entity dictionaries like lists of corporations or products, although they may not be as helpful as a gazetteer (Mikheev et al., 1999).
- korean_translation: 장소 정보를 찾는 데 특히 유용한 특징 중 하나는 지명 사전(gazetteer)입니다. 이는 지리적, 정치적 정보가 담긴 수백만 개의 위치 정보를 제공하는 지명 목록입니다.³ 이는 해당 어구가 목록에 나타나는지 여부를 나타내는 이진 특징(binary feature)으로 구현될 수 있습니다. 미국 인구 조사국(United States Census Bureau)⁴ 등에서 제공하는 인명 목록과 같은 다른 관련 자원도 사용될 수 있으며, 기업이나 제품 목록과 같은 다른 개체 사전도 사용할 수 있지만 지명 사전만큼 도움이 되지는 않을 수 있습니다(Mikheev et al., 1999).
- study_note: NER 성능 향상을 위해 지명 사전이나 인명 리스트 같은 '사전식 외부 지식'을 특징으로 활용하는 방법을 설명합니다.

## Block 11
- type: paragraph
- source_text: The sample named entity token L’Occitane would generate the following non-zero valued feature values (assuming that L’Occitane is neither in the gazetteer nor the census).
- korean_translation: 예제 개체명 토큰인 'L’Occitane'은 (해당 단어가 지명 사전에나 인구 조사국 명단에 없다고 가정할 때) 다음과 같은 0이 아닌 특징 값들을 생성할 것입니다.
- study_note: 구체적인 NER 예시 단어를 통해 특징 추출 과정을 보여 주기 시작합니다.

## Block 12
- type: other
- source_text:
  ³ www.geonames.org
  ⁴ www.census.gov
- korean_translation:
  ³ www.geonames.org
  ⁴ www.census.gov
- study_note: 18페이지 하단의 각주 내용입니다. 각각 지명 사전과 미국 인구 조사국 웹사이트 주소를 안내합니다.
