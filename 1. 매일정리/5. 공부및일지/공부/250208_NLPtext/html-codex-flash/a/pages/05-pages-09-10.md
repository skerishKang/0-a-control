# Page 9
## Block 1
- type: paragraph
- source_text: Note that we represent the most probable path by taking the maximum over all possible previous state sequences max. Like other dynamic programming algorithms, Viterbi fills each cell recursively. Given that we had already computed the probability of being in every state at time t-1, we compute the Viterbi probability by taking the most probable of the extensions of the paths that lead to the current cell.
- korean_translation: 모든 가능한 이전 상태 시퀀스 중 최대값을 취함으로써 가장 확률이 높은 경로를 표현한다는 점에 유의하십시오. 다른 동적 프로그래밍 알고리즘과 마찬가지로, 비터비는 각 셀을 재귀적으로 채웁니다. 시간 t-1에서 모든 상태에 있을 확률을 이미 계산했다고 가정하면, 현재 셀로 이어지는 경로 확장 중 가장 확률이 높은 것을 선택하여 비터비 확률을 계산합니다.
- study_note: Viterbi algorithm uses dynamic programming to efficiently find the most likely sequence of hidden states.

## Block 2
- type: formula
- source_text: vt(j) = max P(q1...qt-1; o1,o2...ot; qt = j | λ) (A.13)
- korean_translation: vt(j) = max P(q1...qt-1; o1,o2...ot; qt = j | λ) (A.13)
- study_note: Formal definition of the Viterbi probability for state j at time t.

## Block 3
- type: paragraph
- source_text: For a given state qj at time t, the value vt(j) is computed as
- korean_translation: 시간 t에서 주어진 상태 qj에 대해, vt(j) 값은 다음과 같이 계산됩니다.
- study_note: Introduction to the recursive step formula.

## Block 4
- type: formula
- source_text: vt(j) = max_{i=1}^{N} vt-1(i) aij bj(ot) (A.14)
- korean_translation: vt(j) = max_{i=1}^{N} vt-1(i) aij bj(ot) (A.14)
- study_note: The core recursive formula for updating Viterbi probabilities.

## Block 5
- type: paragraph
- source_text: The three factors that are multiplied in Eq. A.14 for extending the previous paths to compute the Viterbi probability at time t are: vt-1(i) the previous Viterbi path probability from the previous time step; aij the transition probability from previous state qi to current state qj; bj(ot) the state observation likelihood of the observation symbol ot given the current state j.
- korean_translation: 시간 t에서 비터비 확률을 계산하기 위해 이전 경로를 확장할 때 식 A.14에서 곱해지는 세 가지 요소는 다음과 같습니다: vt-1(i)는 이전 시간 단계에서의 이전 비터비 경로 확률; aij는 이전 상태 qi에서 현재 상태 qj로의 전이 확률; bj(ot)는 현재 상태 j가 주어졌을 때 관측 기호 ot가 나타날 상태 관측 우도입니다.
- study_note: Breakdown of the components in the Viterbi update equation.

## Block 6
- type: code
- source_text: |
    function VITERBI(observations of len T, state-graph of len N) returns best-path, path-prob
      create a path probability matrix viterbi[N,T]
      for each state s from 1 to N do ; initialization step
        viterbi[s,1] <- pi_s * bs(o1)
        backpointer[s,1] <- 0
      for each time step t from 2 to T do ; recursion step
        for each state s from 1 to N do
          viterbi[s,t] <- max_{s'=1}^{N} viterbi[s',t-1] * as',s * bs(ot)
          backpointer[s,t] <- argmax_{s'=1}^{N} viterbi[s',t-1] * as',s * bs(ot)
      bestpathprob <- max_{s=1}^{N} viterbi[s,T] ; termination step
      bestpathpointer <- argmax_{s=1}^{N} viterbi[s,T] ; termination step
      bestpath <- the path starting at state bestpathpointer, that follows backpointer[] to states back in time
      return bestpath, bestpathprob
- korean_translation: |
    function VITERBI(길이 T의 관측치, 길이 N의 상태 그래프) returns 최적 경로, 경로 확률
      경로 확률 행렬 viterbi[N,T] 생성
      각 상태 s (1부터 N까지)에 대해 ; 초기화 단계
        viterbi[s,1] <- pi_s * bs(o1)
        backpointer[s,1] <- 0
      각 시간 단계 t (2부터 T까지)에 대해 ; 재귀 단계
        각 상태 s (1부터 N까지)에 대해
          viterbi[s,t] <- max_{s'=1}^{N} viterbi[s',t-1] * as',s * bs(ot)
          backpointer[s,t] <- argmax_{s'=1}^{N} viterbi[s',t-1] * as',s * bs(ot)
      bestpathprob <- max_{s=1}^{N} viterbi[s,T] ; 종료 단계
      bestpathpointer <- argmax_{s=1}^{N} viterbi[s,T] ; 종료 단계
      bestpath <- bestpathpointer 상태에서 시작하여 backpointer[]를 따라 시간을 거슬러 올라가는 경로
      return 최적 경로, 경로 확률
- study_note: Pseudocode for the complete Viterbi algorithm.

## Block 7
- type: paragraph
- source_text: Figure A.9 shows pseudocode for the Viterbi algorithm. Note that the Viterbi algorithm is identical to the forward algorithm except that it takes the max over the previous path probabilities whereas the forward algorithm takes the sum.
- korean_translation: 그림 A.9는 비터비 알고리즘의 의사코드를 보여줍니다. 비터비 알고리즘은 전방 알고리즘(forward algorithm)이 이전 경로 확률들의 합을 구하는 것과 달리 최대값을 취한다는 점을 제외하면 전방 알고리즘과 동일하다는 점에 유의하십시오.
- study_note: Key difference between Viterbi (decoding) and Forward (likelihood) algorithms.

# Page 10
## Block 1
- type: paragraph
- source_text: Note also that the Viterbi algorithm has one component that the forward algorithm doesn’t have: backpointers. The reason is that while the forward algorithm needs to produce an observation likelihood, the Viterbi algorithm must produce a probability and also the most likely state sequence. We compute this best state sequence by keeping track of the path of hidden states that led to each state, and then at the end backtracing the best path to the beginning (the Viterbi backtrace).
- korean_translation: 또한 비터비 알고리즘에는 전방 알고리즘에 없는 한 가지 구성 요소인 역추적 포인터(backpointers)가 있습니다. 그 이유는 전방 알고리즘은 관측 우도를 생성해야 하는 반면, 비터비 알고리즘은 확률과 함께 가장 가능성 있는 상태 시퀀스를 생성해야 하기 때문입니다. 우리는 각 상태로 이어진 은닉 상태의 경로를 추적하고, 마지막에 시작 부분까지 최적 경로를 역추적(비터비 역추적)함으로써 이 최적 상태 시퀀스를 계산합니다.
- study_note: Explanation of why backpointers are necessary for decoding.

## Block 2
- type: figure_caption
- source_text: Figure A.10 The Viterbi backtrace: we keep a backpointer (shown with broken lines) to the best path that led us to this state.
- korean_translation: 그림 A.10 비터비 역추적: 이 상태로 이어진 최적 경로에 대한 역추적 포인터(점선으로 표시됨)를 유지합니다.
- study_note: Visualizing how the best path is recovered.

## Block 3
- type: other
- source_text: |
    Finally, we can give a formal definition of the Viterbi recursion as follows:
    1. Initialization:
       v1(j) = pi_j * bj(o1)  1 <= j <= N
       bt1(j) = 0             1 <= j <= N
- korean_translation: |
    마지막으로, 비터비 재귀의 공식적인 정의는 다음과 같습니다:
    1. 초기화:
       v1(j) = pi_j * bj(o1)  1 <= j <= N
       bt1(j) = 0             1 <= j <= N
- study_note: Formal mathematical definition of the Viterbi algorithm steps.

## Block 4
- type: formula
- source_text: |
    2. Recursion
       vt(j) = max_{i=1}^{N} vt-1(i) aij bj(ot);  1 <= j <= N; 1 < t <= T
       btt(j) = argmax_{i=1}^{N} vt-1(i) aij bj(ot);  1 <= j <= N; 1 < t <= T
- korean_translation: |
    2. 재귀
       vt(j) = max_{i=1}^{N} vt-1(i) aij bj(ot);  1 <= j <= N; 1 < t <= T
       btt(j) = argmax_{i=1}^{N} vt-1(i) aij bj(ot);  1 <= j <= N; 1 < t <= T
- study_note: The recursive step including the argmax for backpointers.

## Block 5
- type: formula
- source_text: |
    3. Termination:
       The best score: P* = max_{i=1}^{N} vT(i)
       The start of backtrace: qT* = argmax_{i=1}^{N} vT(i)
- korean_translation: |
    3. 종료:
       최적 점수: P* = max_{i=1}^{N} vT(i)
       역추적 시작점: qT* = argmax_{i=1}^{N} vT(i)
- study_note: Final steps to get the probability and start the backtrace.

## Block 6
- type: section_title
- source_text: A.5 HMM Training: The Forward-Backward Algorithm
- korean_translation: A.5 HMM 훈련: 전방-후방 알고리즘
- study_note: Introduction to HMM parameter learning.

## Block 7
- type: paragraph
- source_text: We turn to the third problem for HMMs: learning the parameters of an HMM, that is, the A and B matrices. Formally, Learning: Given an observation sequence O and the set of possible states in the HMM, learn the HMM parameters A and B.
- korean_translation: HMM의 세 번째 문제인 HMM 매개변수(즉, A 및 B 행렬) 학습으로 넘어갑니다. 공식적으로 학습이란: 관측 시퀀스 O와 HMM의 가능한 상태 집합이 주어졌을 때, HMM 매개변수 A와 B를 학습하는 것입니다.
- study_note: Definition of the learning task in HMMs.
