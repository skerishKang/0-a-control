# Page 23

## Block 1
- type: other
- source_text: 7.7 • TRAINING THE NEURAL LANGUAGE MODEL 23
- korean_translation: 7.7 • 신경망 언어 모델 학습시키기 23
- study_note: 페이지 상단 헤더 및 페이지 번호입니다.

## Block 2
- type: paragraph
- source_text: and TensorFlow (Abadi et al., 2015) are two of the most popular. The interested reader should consult a neural network textbook for further details; some suggestions are at the end of the chapter.
- korean_translation: [이전 페이지에서 계속] 그리고 TensorFlow(Abadi 등, 2015)가 가장 인기 있는 두 프레임워크입니다. 관심 있는 독자들은 더 자세한 내용을 위해 신경망 교과서를 참고하시기 바랍니다. 일부 추천 도서는 이 장의 끝에 있습니다.
- study_note: 대중적인 딥러닝 프레임워크들과 추가 학습 자료를 안내합니다.

## Block 3
- type: section_title
- source_text: 7.7 Training the neural language model
- korean_translation: 7.7 신경망 언어 모델 학습시키기
- study_note: 신경망 언어 모델의 파라미터($E, W, U, b$)를 학습하는 구체적인 과정을 설명합니다.

## Block 4
- type: paragraph
- source_text: Now that we’ve seen how to train a generic neural net, let’s talk about the architecture for training a neural language model, setting the parameters \theta = E, W, U, b.
- korean_translation: 이제 일반적인 신경망을 학습시키는 방법을 살펴보았으니, 파라미터 $\theta = \{ \mathbf{E, W, U, b} \}$를 설정하여 신경망 언어 모델을 학습시키기 위한 구조에 대해 이야기해 봅시다.
- study_note: 언어 모델 학습에서 관리해야 할 파라미터 집합을 정의합니다.

## Block 5
- type: paragraph
- source_text: For some tasks, it’s ok to freeze the embedding layer E with initial word2vec values. Freezing means we use word2vec or some other pretraining algorithm to compute the initial embedding matrix E, and then hold it constant while we only modify W, U, and b, i.e., we don’t update E during language model training. However, often we’d like to learn the embeddings simultaneously with training the network. This is useful when the task the network is designed for (sentiment classification, or translation, or parsing) places strong constraints on what makes a good representation for words.
- korean_translation: 어떤 작업들에서는 초기 word2vec 값으로 임베딩 층 $\mathbf{E}$를 '고정(freeze)'해도 괜찮습니다. 고정한다는 것은 word2vec이나 다른 사전 학습 알고리즘을 사용하여 초기 임베딩 행렬 $\mathbf{E}$를 계산한 다음, 언어 모델 학습 동안 $\mathbf{E}$를 업데이트하지 않고 $\mathbf{W, U, b}$만 수정하며 일정하게 유지하는 것을 의미합니다. 하지만 종종 네트워크 학습과 동시에 임베딩을 학습시키고 싶을 때가 있습니다. 이는 네트워크가 설계된 작업(감성 분류, 번역, 구문 분석 등)이 '좋은 단어 표현'이 무엇인지에 대해 강력한 제약을 부여할 때 유용합니다.
- study_note: 임베딩 층의 고정(freezing)과 미세 조정(fine-tuning/learning)의 개념 및 각각의 활용 사례를 설명합니다.

## Block 6
- type: margin_note
- source_text: freeze
- korean_translation: 고정(freeze)
- study_note: 학습 과정에서 특정 층의 파라미터가 업데이트되지 않도록 고정하는 기술입니다.

## Block 7
- type: paragraph
- source_text: Let’s see how to train the entire model including E, i.e. to set all the parameters \theta = E, W, U, b. We’ll do this via gradient descent (Fig. ??), using error backpropagation on the computation graph to compute the gradient. Training thus not only sets the weights W and U of the network, but also as we’re predicting upcoming words, we’re learning the embeddings E for each word that best predict upcoming words.
- korean_translation: $\mathbf{E}$를 포함한 전체 모델을 학습시키는 방법, 즉 모든 파라미터 $\theta = \{ \mathbf{E, W, U, b} \}$를 설정하는 방법을 알아봅시다. 우리는 계산 그래프에서 오차 역전파를 사용하여 그래디언트를 계산하고, 이를 경사 하강법을 통해 수행할 것입니다. 따라서 학습은 네트워크의 가중치 $\mathbf{W}$와 $\mathbf{U}$를 설정할 뿐만 아니라, 다음에 올 단어를 예측함에 따라 그 예측을 가장 잘 수행하게 하는 각 단어의 임베딩 $\mathbf{E}$도 함께 학습하게 됩니다.
- study_note: 역전파를 통해 임베딩 층까지 그래디언트를 전달하여 단어 벡터 자체를 최적화하는 과정을 설명합니다.

## Block 8
- type: image
- source_text: [Figure 7.18 Learning all the way back to embeddings. Again, the embedding matrix E is shared among the 3 context words.]
- korean_translation: [그림 7.18 임베딩까지 거슬러 올라가며 학습하는 과정. 여기서도 임베딩 행렬 $\mathbf{E}$는 3개의 문맥 단어 사이에서 공유됩니다.]
- study_note: 그림 7.18. 손실 함부로부터 입력 임베딩 층까지 역전파 화살표(빨간 점선)가 이어지는 모습을 통해 전체 파라미터 학습 과정을 보여줍니다.

## Block 9
- type: figure_caption
- source_text: Figure 7.18 Learning all the way back to embeddings. Again, the embedding matrix E is shared among the 3 context words.
- korean_translation: 그림 7.18 임베딩까지 거슬러 올라가며 학습하는 과정. 여기서도 임베딩 행렬 $\mathbf{E}$는 3개의 문맥 단어 사이에서 공유됩니다.
- study_note: 그림 7.18 캡션입니다.

## Block 10
- type: paragraph
- source_text: Fig. 7.18 shows the set up for a window size of N=3 context words. The input x consists of 3 one-hot vectors, fully connected to the embedding layer via 3 instantiations of the embedding matrix E. We don’t want to learn separate weight matrices
- korean_translation: 그림 7.18은 윈도우 크기가 $N=3$인 문맥 단어들에 대한 설정을 보여줍니다. 입력 $\mathbf{x}$는 3개의 원-핫 벡터로 구성되며, 임베딩 행렬 $\mathbf{E}$의 3개 인스턴스를 통해 임베딩 층에 전결합됩니다. 우리는 [다음 페이지에서 계속]
- study_note: 공유 네트워크 구조를 통한 임베딩 학습 설정을 설명합니다.

# Page 24

## Block 1
- type: other
- source_text: 24 CHAPTER 7 • NEURAL NETWORKS AND NEURAL LANGUAGE MODELS
- korean_translation: 24 제7장 • 신경망과 신경망 언어 모델
- study_note: 페이지 상단 헤더 및 페이지 번호입니다.

## Block 2
- type: paragraph
- source_text: for mapping each of the 3 previous words to the projection layer. We want one single embedding dictionary E that’s shared among these three. That’s because over time, many different words will appear as w_{t-2} or w_{t-1}, and we’d like to just represent each word with one vector, whichever context position it appears in. Recall that the embedding weight matrix E has a column for each word, each a column vector of d dimensions, and hence has dimensionality d × |V|.
- korean_translation: [이전 페이지에서 계속] 이전 3개 단어 각각을 프로젝션 층(projection layer)으로 매핑하기 위해 별도의 가중치 행렬을 학습시키고 싶지는 않습니다. 우리는 이 세 위치 사이에서 공유되는 단 하나의 임베딩 사전 $\mathbf{E}$를 원합니다. 왜냐하면 시간이 지남에 따라 많은 다양한 단어들이 $w_{t-2}$나 $w_{t-1}$ 위치에 나타날 것이고, 우리는 각 단어가 어떤 문맥 위치에 나타나든 단 하나의 벡터로 표현되기를 바라기 때문입니다. 임베딩 가중치 행렬 $\mathbf{E}$는 각 단어마다 하나의 열을 가지며 각각이 $d$차원의 열 벡터이므로, $d \times |V|$의 차원을 가진다는 점을 상기하십시오.
- study_note: 문맥 내 위치와 상관없이 동일한 단어는 동일한 벡터로 표현되어야 하므로 임베딩 행렬을 공유(parameter sharing)해야 함을 역설합니다.

## Block 3
- type: paragraph
- source_text: Generally training proceeds by taking as input a very long text, concatenating all the sentences, starting with random weights, and then iteratively moving through the text predicting each word w_t. At each word w_t, we use the cross-entropy (negative log likelihood) loss. Recall that the general form for this (repeated from Eq. 7.26) is:
- korean_translation: 일반적으로 학습은 아주 긴 텍스트를 입력으로 받아 모든 문장을 연결하고, 무작위 가중치로 시작하여 텍스트를 반복적으로 훑으며 각 단어 $w_t$를 예측하는 방식으로 진행됩니다. 각 단어 $w_t$에서 우리는 교차 엔트로피(음의 로그 우도) 손실을 사용합니다. 이의 일반적인 형태(식 7.26에서 반복됨)는 다음과 같습니다.
- study_note: 언어 모델의 일반적인 학습 절차와 손실 함수 적용 방식을 설명합니다.

## Block 4
- type: formula
- source_text: L_{CE}(\hat{y}, y) = -\log \hat{y}_i, \quad \text{(where } i \text{ is the correct class)} (7.43)
- korean_translation: L_{CE}(\hat{y}, y) = -\log \hat{y}_i, \quad \text{(} i \text{가 정답 클래스인 경우)} (7.43)
- study_note: 식 (7.43). 정답 단어에 대한 음의 로그 확률로 정의되는 손실 함수입니다.

## Block 5
- type: paragraph
- source_text: For language modeling, the classes are the words in the vocabulary, so \hat{y}_i here means the probability that the model assigns to the correct next word w_t:
- korean_translation: 언어 모델링에서 클래스는 어휘 사전의 단어들이므로, 여기서 $\hat{y}_i$는 모델이 정답인 다음 단어 $w_t$에 할당하는 확률을 의미합니다.
- study_note: 분류 문제로서의 언어 모델링에서 출력값의 의미를 구체화합니다.

## Block 6
- type: formula
- source_text: L_{CE} = -\log p(w_t | w_{t-1}, \dots, w_{t-n+1}) (7.44)
- korean_translation: L_{CE} = -\log p(w_t | w_{t-1}, \dots, w_{t-n+1}) (7.44)
- study_note: 식 (7.44). 언어 모델링 문맥에서의 교차 엔트로피 손실 수식입니다.

## Block 7
- type: paragraph
- source_text: The parameter update for stochastic gradient descent for this loss from step s to s + 1 is then:
- korean_translation: 이 손실에 대해 단계 $s$에서 $s+1$로 넘어가는 확률적 경사 하강법(SGD)의 파라미터 업데이트 식은 다음과 같습니다.
- study_note: 파라미터 업데이트를 위한 SGD 공식입니다.

## Block 8
- type: formula
- source_text: \theta^{s+1} = \theta^s - \eta \frac{\partial [-\log p(w_t | w_{t-1}, \dots, w_{t-n+1})]}{\partial \theta} (7.45)
- korean_translation: \theta^{s+1} = \theta^s - \eta \frac{\partial [-\log p(w_t | w_{t-1}, \dots, w_{t-n+1})]}{\partial \theta} (7.45)
- study_note: 식 (7.45). 학습률 $\eta$를 곱한 그래디언트를 현재 파라미터에서 빼서 업데이트하는 기본 SGD 공식입니다.

## Block 9
- type: paragraph
- source_text: This gradient can be computed in any standard neural network framework which will then backpropagate through \theta = E, W, U, b.
- korean_translation: 이 그래디언트는 어떤 표준 신경망 프레임워크에서도 계산될 수 있으며, 프레임워크는 $\theta = \{ \mathbf{E, W, U, b} \}$ 전체를 통해 역전파를 수행할 것입니다.
- study_note: 현대적 프레임워크들이 자동 미분을 통해 모든 파라미터를 학습시킴을 언급합니다.

## Block 10
- type: paragraph
- source_text: Training the parameters to minimize loss will result both in an algorithm for language modeling (a word predictor) but also a new set of embeddings E that can be used as word representations for other tasks.
- korean_translation: 손실을 최소화하도록 파라미터를 학습시키면 언어 모델링 알고리즘(단어 예측기)이 만들어질 뿐만 아니라, 다른 작업에서 단어 표현으로 사용할 수 있는 새로운 임베딩 세트 $\mathbf{E}$도 결과물로 얻게 됩니다.
- study_note: 언어 모델 학습의 부수적 결과물인 범용적 단어 임베딩의 가치를 강조합니다.

## Block 11
- type: section_title
- source_text: 7.8 Summary
- korean_translation: 7.8 요약
- study_note: 7장에서 다룬 핵심 개념들을 정리하는 요약 섹션입니다.

## Block 12
- type: list
- source_text:
  • Neural networks are built out of neural units, originally inspired by human neurons but now simply an abstract computational device.
  • Each neural unit multiplies input values by a weight vector, adds a bias, and then applies a non-linear activation function like sigmoid, tanh, or rectified linear unit.
  • In a fully-connected, feedforward network, each unit in layer i is connected to each unit in layer i + 1, and there are no cycles.
  • The power of neural networks comes from the ability of early layers to learn representations that can be utilized by later layers in the network.
  • Neural networks are trained by optimization algorithms like gradient descent.
  • Error backpropagation, backward differentiation on a computation graph, is used to compute the gradients of the loss function for a network.
  • Neural language models use a neural network as a probabilistic classifier, to compute the probability of the next word given the previous n words.
  • Neural language models can use pretrained embeddings, or can learn embeddings from scratch in the process of language modeling.
- korean_translation:
  • 신경망은 인간의 뉴런에서 영감을 받았으나 현재는 추상적인 계산 장치인 '신경망 유닛(neural units)'으로 구축됩니다.
  • 각 신경망 유닛은 입력값에 가중치 벡터를 곱하고, 바이어스를 더한 뒤, 시그모이드, tanh, 또는 ReLU(rectified linear unit)와 같은 비선형 활성화 함수를 적용합니다.
  • 전결합(fully-connected) 피드포워드 네트워크에서는 $i$층의 각 유닛이 $i+1$층의 모든 유닛과 연결되며, 순환 구조는 존재하지 않습니다.
  • 신경망의 강력함은 초기 층들이 네트워크의 후기 층에서 활용될 수 있는 '표현(representations)'을 학습하는 능력에서 나옵니다.
  • 신경망은 경사 하강법과 같은 최적화 알고리즘에 의해 학습됩니다.
  • 네트워크 손실 함수의 그래디언트를 계산하기 위해 계산 그래프에서의 후향 미분인 '오차 역전파(error backpropagation)'가 사용됩니다.
  • 신경망 언어 모델은 신경망을 확률적 분류기로 사용하여, 이전 $n$개의 단어가 주어졌을 때 다음 단어의 확률을 계산합니다.
  • 신경망 언어 모델은 사전 학습된 임베딩을 사용하거나, 언어 모델링 과정에서 처음부터 임베딩을 학습할 수 있습니다.
- study_note: 7장의 핵심 학습 포인트들을 조목조목 정리하여 복습을 돕습니다.
