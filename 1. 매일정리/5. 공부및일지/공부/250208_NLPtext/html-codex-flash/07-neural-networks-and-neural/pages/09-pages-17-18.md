# Page 17

## Block 1
- type: other
- source_text: 7.6 • TRAINING NEURAL NETS 17
- korean_translation: 7.6 • 신경망 학습시키기 17
- study_note: 페이지 상단 헤더 및 페이지 번호입니다.

## Block 2
- type: paragraph
- source_text: What about if the neural network is being used as a multinomial classifier? Let y be a vector over the C classes representing the true output probability distribution. The cross-entropy loss here is
- korean_translation: 신경망이 다중 분류기(multinomial classifier)로 사용되는 경우는 어떨까요? $C$개의 클래스에 대한 실제 출력 확률 분포를 나타내는 벡터를 $\mathbf{y}$라고 합시다. 이때의 교차 엔트로피 손실은 다음과 같습니다.
- study_note: 다중 클래스 분류 상황에서의 손실 함수 정의를 도입합니다.

## Block 3
- type: formula
- source_text: L_{CE}(\hat{\mathbf{y}}, \mathbf{y}) = -\sum_{i=1}^C y_i \log \hat{y}_i (7.24)
- korean_translation: L_{CE}(\hat{\mathbf{y}}, \mathbf{y}) = -\sum_{i=1}^C y_i \log \hat{y}_i (7.24)
- study_note: 식 (7.24). 다중 클래스 교차 엔트로피 손실의 일반식입니다.

## Block 4
- type: paragraph
- source_text: We can simplify this equation further. Assume this is a hard classification task, meaning that only one class is the correct one, and that there is one output unit in y for each class. If the true class is i, then y is a vector where y_i = 1 and y_j = 0 ∀j ≠ i. A vector like this, with one value=1 and the rest 0, is called a one-hot vector. The terms in the sum in Eq. 7.24 will be 0 except for the term corresponding to the true class, i.e.:
- korean_translation: 이 식을 더 단순화할 수 있습니다. 단 하나의 클래스만이 정답이고 각 클래스마다 하나의 출력 유닛이 대응되는 '하드 분류(hard classification)' 작업이라고 가정해 봅시다. 만약 실제 정답 클래스가 $i$라면, $\mathbf{y}$는 $y_i = 1$이고 $j \neq i$인 모든 $j$에 대해 $y_j = 0$인 벡터가 됩니다. 이처럼 하나의 값만 1이고 나머지는 모두 0인 벡터를 '원-핫 벡터(one-hot vector)'라고 부릅니다. 이 경우 식 7.24의 합계 항들은 정답 클래스에 해당하는 항을 제외하고는 모두 0이 됩니다. 즉:
- study_note: 정답이 하나인 경우(원-핫 타겟) 교차 엔트로피 식이 어떻게 단순화되는지 설명합니다.

## Block 5
- type: formula
- source_text:
  L_{CE}(\hat{\mathbf{y}}, \mathbf{y}) = -\sum_{k=1}^K \mathbb{1}\{y = k\} \log \hat{y}_k \\
  = -\sum_{k=1}^K \mathbb{1}\{y = k\} \log \hat{p}(y = k | \mathbf{x}) \\
  = -\sum_{k=1}^K \mathbb{1}\{y = k\} \log \frac{\exp(\mathbf{z}_k)}{\sum_{j=1}^K \exp(\mathbf{z}_j)} (7.25)
- korean_translation:
  L_{CE}(\hat{\mathbf{y}}, \mathbf{y}) = -\sum_{k=1}^K \mathbb{1}\{y = k\} \log \hat{y}_k \\
  = -\sum_{k=1}^K \mathbb{1}\{y = k\} \log \hat{p}(y = k | \mathbf{x}) \\
  = -\sum_{k=1}^K \mathbb{1}\{y = k\} \log \frac{\exp(\mathbf{z}_k)}{\sum_{j=1}^K \exp(\mathbf{z}_j)} (7.25)
- study_note: 식 (7.25). 지시 함수(indicator function)와 소프트맥스를 사용하여 손실 함수를 구체화한 형태입니다.

## Block 6
- type: paragraph
- source_text: Hence the cross-entropy loss is simply the log of the output probability corresponding to the correct class, and we therefore also call this the negative log likelihood loss:
- korean_translation: 따라서 교차 엔트로피 손실은 간단히 정답 클래스에 해당하는 출력 확률의 로그값이 되며, 이를 '음의 로그 우도 손실(negative log likelihood loss, NLL)'이라고도 부릅니다.
- study_note: 교차 엔트로피 손실의 다른 명칭과 그 의미를 설명합니다.

## Block 7
- type: margin_note
- source_text:
  negative log likelihood loss
- korean_translation:
  음의 로그 우도 손실(negative log likelihood loss)
- study_note: 정답 클래스의 확률을 최대화하는 것과 동일한 의미를 갖는 손실 함수입니다.

## Block 8
- type: formula
- source_text: L_{CE}(\hat{\mathbf{y}}, \mathbf{y}) = -\log \hat{y}_i \quad \text{(where } i \text{ is the correct class)} (7.26)
- korean_translation: L_{CE}(\hat{\mathbf{y}}, \mathbf{y}) = -\log \hat{y}_i \quad \text{(} i \text{가 정답 클래스인 경우)} (7.26)
- study_note: 식 (7.26). 단일 정답에 대한 NLL 손실의 간략한 수식입니다.

## Block 9
- type: paragraph
- source_text: Plugging in the softmax formula from Eq. 7.9, and with K the number of classes:
- korean_translation: 식 7.9의 소프트맥스 공식을 대입하고 $K$를 클래스의 수라고 하면 다음과 같습니다.
- study_note: 소프트맥스가 포함된 최종 손실 함수 식입니다.

## Block 10
- type: formula
- source_text: L_{CE}(\hat{\mathbf{y}}, \mathbf{y}) = -\log \frac{\exp(\mathbf{z}_i)}{\sum_{j=1}^K \exp(\mathbf{z}_j)} \quad \text{(where } i \text{ is the correct class)} (7.27)
- korean_translation: L_{CE}(\hat{\mathbf{y}}, \mathbf{y}) = -\log \frac{\exp(\mathbf{z}_i)}{\sum_{j=1}^K \exp(\mathbf{z}_j)} \quad \text{(} i \text{가 정답 클래스인 경우)} (7.27)
- study_note: 식 (7.27). 출력 노드 값(z)을 이용한 최종 NLL 손실 공식입니다.

## Block 11
- type: section_title
- source_text: 7.6.2 Computing the Gradient
- korean_translation: 7.6.2 그래디언트 계산하기
- study_note: 손실을 최소화하기 위해 파라미터를 어떻게 업데이트할지 결정하는 그래디언트 계산법을 다룹니다.

## Block 12
- type: paragraph
- source_text: How do we compute the gradient of this loss function? Computing the gradient requires the partial derivative of the loss function with respect to each parameter. For a network with one weight layer and sigmoid output (which is what logistic regression is), we could simply use the derivative of the loss that we used for logistic regression in Eq. 7.28 (and derived in Section ??):
- korean_translation: 이 손실 함수의 그래디언트를 어떻게 계산할까요? 그래디언트를 계산하려면 각 파라미터에 대한 손실 함수의 편미분값을 구해야 합니다. (로지스틱 회귀와 같이) 하나의 가중치 층과 시그모이드 출력을 가진 네트워크의 경우, 5장에서 유도했던 식 7.28의 손실 함수 미분값을 그대로 사용할 수 있습니다.
- study_note: 단층 네트워크(로지스틱 회귀)에서의 미분 공식을 복습합니다.

## Block 13
- type: formula
- source_text:
  \frac{\partial L_{CE}(w, b)}{\partial \mathbf{w}_j} = (\hat{y} - y)\mathbf{x}_j \\
  = (\sigma(\mathbf{w} \cdot \mathbf{x} + b) - y)\mathbf{x}_j (7.28)
- korean_translation:
  \frac{\partial L_{CE}(w, b)}{\partial \mathbf{w}_j} = (\hat{y} - y)\mathbf{x}_j \\
  = (\sigma(\mathbf{w} \cdot \mathbf{x} + b) - y)\mathbf{x}_j (7.28)
- study_note: 식 (7.28). 가중치 w_j에 대한 오차의 편미분값입니다.

## Block 14
- type: paragraph
- source_text: Or for a network with one hidden layer and softmax output, we could use the derivative of the softmax loss from Eq. ??:
- korean_translation: 혹은 하나의 은닉층과 소프트맥스 출력을 가진 네트워크의 경우, 식 ??에서 보았던 소프트맥스 손실의 미분값을 사용할 수 있습니다.
- study_note: 소프트맥스 출력층에 대한 미분 공식입니다.

## Block 15
- type: formula
- source_text:
  \frac{\partial L_{CE}}{\partial \mathbf{w}_k} = -(\mathbb{1}\{y = k\} - p(y = k | \mathbf{x}))\mathbf{x}_k \\
  = -\left(\mathbb{1}\{y = k\} - \frac{\exp(\mathbf{w}_k \cdot \mathbf{x} + \mathbf{b}_k)}{\sum_{j=1}^K \exp(\mathbf{w}_j \cdot \mathbf{x} + \mathbf{b}_j)}\right)\mathbf{x}_k (7.29)
- korean_translation:
  \frac{\partial L_{CE}}{\partial \mathbf{w}_k} = -(\mathbb{1}\{y = k\} - p(y = k | \mathbf{x}))\mathbf{x}_k \\
  = -\left(\mathbb{1}\{y = k\} - \frac{\exp(\mathbf{w}_k \cdot \mathbf{x} + \mathbf{b}_k)}{\sum_{j=1}^K \exp(\mathbf{w}_j \cdot \mathbf{x} + \mathbf{b}_j)}\right)\mathbf{x}_k (7.29)
- study_note: 식 (7.29). 다중 클래스 분류에서의 가중치 업데이트를 위한 미분 공식입니다.

# Page 18

## Block 1
- type: other
- source_text: 18 CHAPTER 7 • NEURAL NETWORKS AND NEURAL LANGUAGE MODELS
- korean_translation: 18 제7장 • 신경망과 신경망 언어 모델
- study_note: 페이지 상단 헤더 및 페이지 번호입니다.

## Block 2
- type: paragraph
- source_text: But these derivatives only give correct updates for one weight layer: the last one! For deep networks, computing the gradients for each weight is much more complex, since we are computing the derivative with respect to weight parameters that appear all the way back in the very early layers of the network, even though the loss is computed only at the very end of the network.
- korean_translation: 하지만 이 미분값들은 단 하나의 가중치 층, 즉 '마지막 층'에 대해서만 올바른 업데이트를 제공할 뿐입니다! 심층 네트워크의 경우, 각 가중치에 대한 그래디언트를 계산하는 것은 훨씬 더 복잡합니다. 손실은 네트워크의 맨 끝에서만 계산되는데, 우리는 네트워크의 아주 초기 층에 위치한 가중치 파라미터들에 대한 미분값을 계산해야 하기 때문입니다.
- study_note: 심층 신경망에서 초기 층의 가중치 미분을 구하는 것의 난해함을 지적합니다.

## Block 3
- type: paragraph
- source_text: The solution to computing this gradient is an algorithm called error backpropagation or backprop (Rumelhart et al., 1986). While backprop was invented specially for neural networks, it turns out to be the same as a more general procedure called backward differentiation, which depends on the notion of computation graphs. Let’s see how that works in the next subsection.
- korean_translation: 이 그래디언트를 계산하기 위한 해결책은 '오차 역전파(error backpropagation)' 또는 '백프롭(backprop)'이라 불리는 알고리즘입니다(Rumelhart 등, 1986). 백프롭은 신경망을 위해 특별히 발명되었지만, 결과적으로는 '계산 그래프(computation graphs)' 개념에 의존하는 '후향 미분(backward differentiation)'이라는 더 일반적인 절차와 동일한 것으로 밝혀졌습니다. 다음 서브섹션에서 이것이 어떻게 작동하는지 살펴봅시다.
- study_note: 역전파 알고리즘의 유래와 계산 그래프 기반의 후향 미분과의 관계를 설명합니다.

## Block 4
- type: margin_note
- source_text: error backpropagation
- korean_translation: 오차 역전파(error backpropagation)
- study_note: 출력에서 발생한 오차를 입력 방향으로 되돌리며 각 가중치를 조정하는 핵심 학습 알고리즘입니다.

## Block 5
- type: section_title
- source_text: 7.6.3 Computation Graphs
- korean_translation: 7.6.3 계산 그래프(Computation Graphs)
- study_note: 복잡한 수학적 연산을 노드와 에지로 시각화하여 미분을 효율적으로 수행하게 돕는 계산 그래프를 소개합니다.

## Block 6
- type: paragraph
- source_text: A computation graph is a representation of the process of computing a mathematical expression, in which the computation is broken down into separate operations, each of which is modeled as a node in a graph.
- korean_translation: 계산 그래프는 수학적 표현을 계산하는 과정을 나타낸 것으로, 계산을 개별적인 연산들로 쪼개고 각각을 그래프의 '노드'로 모델링합니다.
- study_note: 계산 그래프의 정의를 설명합니다.

## Block 7
- type: paragraph
- source_text: Consider computing the function L(a, b, c) = c(a + 2b). If we make each of the component addition and multiplication operations explicit, and add names (d and e) for the intermediate outputs, the resulting series of computations is:
- korean_translation: 함수 $L(a, b, c) = c(a + 2b)$를 계산하는 경우를 생각해 봅시다. 각 구성 요소인 덧셈과 곱셈 연산을 명시적으로 만들고, 중간 출력들에 이름($d$와 $e$)을 붙이면 일련의 계산 과정은 다음과 같습니다.
- study_note: 함수를 연산 단위로 분해하는 예제입니다.

## Block 8
- type: formula
- source_text:
  d = 2 * b \\
  e = a + d \\
  L = c * e
- korean_translation:
  d = 2 * b \\
  e = a + d \\
  L = c * e
- study_note: 예제 함수의 단계별 연산 과정입니다.

## Block 9
- type: paragraph
- source_text: We can now represent this as a graph, with nodes for each operation, and directed edges showing the outputs from each operation as the inputs to the next, as in Fig. 7.14. The simplest use of computation graphs is to compute the value of the function with some given inputs. In the figure, we’ve assumed the inputs a = 3, b = 1, c = -2, and we’ve shown the result of the forward pass to compute the result L(3, 1, -2) = -10. In the forward pass of a computation graph, we apply each operation left to right, passing the outputs of each computation as the input to the next node.
- korean_translation: 이제 이를 그림 7.14와 같이 각 연산을 위한 노드와, 한 연산의 출력이 다음 연산의 입력이 됨을 보여주는 유향 에지(directed edges)를 가진 그래프로 표현할 수 있습니다. 계산 그래프의 가장 단순한 용도는 주어진 입력값들에 대해 함수의 값을 계산하는 것입니다. 그림에서 우리는 입력 $a = 3, b = 1, c = -2$를 가정했으며, $L(3, 1, -2) = -10$을 계산하기 위한 '순방향 패스(forward pass)'의 결과를 보여주었습니다. 계산 그래프의 순방향 패스에서는 각 연산을 왼쪽에서 오른쪽으로 적용하며, 각 계산의 출력을 다음 노드의 입력으로 전달합니다.
- study_note: 분해된 연산들을 그래프로 시각화하고 입력값을 이용해 최종 결과값을 도출하는 순방향 패스 과정을 설명합니다.

## Block 10
- type: image
- source_text: [Figure 7.14 Computation graph for the function L(a, b, c) = c(a + 2b), with values for input nodes a = 3, b = 1, c = -2, showing the forward pass computation of L.]
- korean_translation: [그림 7.14 함수 $L(a, b, c) = c(a + 2b)$에 대한 계산 그래프. 입력 노드 $a = 3, b = 1, c = -2$일 때 $L$을 계산하기 위한 순방향 패스 과정을 보여줍니다.]
- study_note: 그림 7.14. 예제 함수의 계산 그래프와 순방향 연산 수치들을 보여줍니다.

## Block 11
- type: figure_caption
- source_text: Figure 7.14 Computation graph for the function L(a, b, c) = c(a + 2b), with values for input nodes a = 3, b = 1, c = -2, showing the forward pass computation of L.
- korean_translation: 그림 7.14 함수 $L(a, b, c) = c(a + 2b)$에 대한 계산 그래프. 입력 노드 $a = 3, b = 1, c = -2$일 때 $L$을 계산하기 위한 순방향 패스 과정을 보여줍니다.
- study_note: 그림 7.14 캡션입니다.

## Block 12
- type: section_title
- source_text: 7.6.4 Backward differentiation on computation graphs
- korean_translation: 7.6.4 계산 그래프에서의 후향 미분
- study_note: 계산 그래프를 거꾸로 거슬러 올라가며 미분값(그래디언트)을 효율적으로 구하는 역방향 패스 과정을 다룹니다.

## Block 13
- type: paragraph
- source_text: The importance of the computation graph comes from the backward pass, which is used to compute the derivatives that we’ll need for the weight update. In this example our goal is to compute the derivative of the output function L with respect
- korean_translation: 계산 그래프의 중요성은 가중치 업데이트에 필요한 미분값들을 계산하는 데 사용되는 '역방향 패스(backward pass)'에서 나옵니다. 이 예제에서 우리의 목표는 출력 함수 $L$을 [다음 페이지에서 계속]
- study_note: 역방향 패스를 통한 미분 계산의 목적과 그 중요성을 강조합니다.
