# Page 11

## Block 1
- type: other
- source_text: 7.4 • FEEDFORWARD NETWORKS FOR NLP: CLASSIFICATION 11
- korean_translation: 7.4 • NLP를 위한 피드포워드 네트워크: 분류 11
- study_note: 페이지 상단 헤더 및 페이지 번호입니다.

## Block 2
- type: paragraph
- source_text: Replacing the bias unit In describing networks, we will often use a slightly simplified notation that represents exactly the same function without referring to an explicit bias node b. Instead, we add a dummy node a_0 to each layer whose value will always be 1. Thus layer 0, the input layer, will have a dummy node a^[0]_0 = 1, layer 1 will have a^[1]_0 = 1, and so on. This dummy node still has an associated weight, and that weight represents the bias value b. For example instead of an equation like
- korean_translation: 바이어스 유닛의 대체: 네트워크를 설명할 때, 우리는 명시적인 바이어스 노드 $b$를 언급하지 않고도 정확히 동일한 함수를 표현하는 약간 단순화된 표기법을 자주 사용할 것입니다. 대신 각 층에 값이 항상 1인 더미 노드(dummy node) $a_0$를 추가합니다. 따라서 입력층인 0번 층은 더미 노드 $a^{[0]}_0 = 1$을 갖고, 1번 층은 $a^{[1]}_0 = 1$을 갖는 식입니다. 이 더미 노드는 여전히 연관된 가중치를 가지며, 그 가중치가 바로 바이어스 값 $b$를 나타냅니다. 예를 들어 다음과 같은 식 대신,
- study_note: 바이어스 항 b를 입력에 1을 추가하고 가중치에 포함시키는 방식으로 단순화하는 표기법을 설명합니다.

## Block 3
- type: formula
- source_text: h = \sigma(\mathbf{Wx} + \mathbf{b}) (7.15)
- korean_translation: h = \sigma(\mathbf{Wx} + \mathbf{b}) (7.15)
- study_note: 식 (7.15). 명시적인 바이어스 벡터 b를 포함한 형태입니다.

## Block 4
- type: paragraph
- source_text: we’ll use:
- korean_translation: 다음 식을 사용하게 됩니다.
- study_note: 바이어스를 가중치 행렬에 통합한 단순화된 식입니다.

## Block 5
- type: formula
- source_text: h = \sigma(\mathbf{Wx}) (7.16)
- korean_translation: h = \sigma(\mathbf{Wx}) (7.16)
- study_note: 식 (7.16). 바이어스가 통합된 단순화 표기법입니다.

## Block 6
- type: paragraph
- source_text: But now instead of our vector x having n values: x = x_1, . . . , x_n, it will have n + 1 values, with a new 0th dummy value x_0 = 1: x = x_0, . . . , x_n. And instead of computing each h_j as follows:
- korean_translation: 그러나 이제 $n$개의 값을 가진 벡터 $\mathbf{x} = x_1, \dots, x_n$ 대신, 0번째 더미 값 $x_0 = 1$이 추가된 $n+1$개의 값을 가진 벡터 $\mathbf{x} = x_0, \dots, x_n$을 사용하게 됩니다. 그리고 각 $h_j$를 다음과 같이 계산하는 대신,
- study_note: 입력 벡터에 x0 = 1을 추가하여 연산 범위를 확장하는 방식을 설명합니다.

## Block 7
- type: formula
- source_text: h_j = \sigma \left( \sum_{i=1}^{n_0} W_{ji} x_i + b_j \right), (7.17)
- korean_translation: h_j = \sigma \left( \sum_{i=1}^{n_0} W_{ji} x_i + b_j \right), (7.17)
- study_note: 식 (7.17). 인덱스가 1부터 시작하며 별도의 바이어스 항을 가진 기존 방식입니다.

## Block 8
- type: paragraph
- source_text: we’ll instead use:
- korean_translation: 대신 다음 식을 사용합니다.
- study_note: 인덱스 0부터 시작하는 통합 계산 방식입니다.

## Block 9
- type: formula
- source_text: \sigma \left( \sum_{i=0}^{n_0} W_{ji} x_i \right), (7.18)
- korean_translation: \sigma \left( \sum_{i=0}^{n_0} W_{ji} x_i \right), (7.18)
- study_note: 식 (7.18). i=0(더미 노드)을 포함하여 바이어스 계산까지 한 번에 처리하는 식입니다.

## Block 10
- type: paragraph
- source_text: where the value W_j0 replaces what had been b_j. Fig. 7.9 shows a visualization.
- korean_translation: 여기서 값 $W_{j0}$는 기존의 $b_j$를 대체합니다. 그림 7.9는 이에 대한 시각화를 보여줍니다.
- study_note: 가중치 행렬의 0번 열이 바이어스 역할을 수행함을 명시합니다.

## Block 11
- type: image
- source_text: [Figure 7.9 Replacing the bias node (shown in a) with x0 (b).]
- korean_translation: [그림 7.9 (a)에 표시된 바이어스 노드를 (b)의 $x_0$로 교체하는 과정 시각화.]
- study_note: 그림 7.9. 바이어스 노드 처리를 위한 두 가지 상이한 도식적 표현법을 비교합니다.

## Block 12
- type: figure_caption
- source_text: Figure 7.9 Replacing the bias node (shown in a) with x0 (b).
- korean_translation: 그림 7.9 바이어스 노드(a에 표시됨)를 $x_0$(b)로 교체하는 모습.
- study_note: 그림 7.9 캡션입니다.

## Block 13
- type: paragraph
- source_text: We’ll continue showing the bias as b when we go over the learning algorithm in Section 7.6, but then we’ll switch to this simplified notation without explicit bias terms for the rest of the book.
- korean_translation: 7.6절의 학습 알고리즘을 살펴볼 때는 바이어스를 $b$로 계속 표시하겠지만, 책의 나머지 부분에서는 명시적인 바이어스 항이 없는 이 단순화된 표기법으로 전환할 것입니다.
- study_note: 향후 설명 과정에서의 표기법 사용 계획을 안내합니다.

## Block 14
- type: section_title
- source_text: 7.4 Feedforward networks for NLP: Classification
- korean_translation: 7.4 NLP를 위한 피드포워드 네트워크: 분류
- study_note: 자연어 처리(NLP) 분야에서 피드포워드 네트워크를 이용한 분류 작업 적용 사례를 다룹니다.

## Block 15
- type: paragraph
- source_text: Let’s see how to apply feedforward networks to NLP tasks! In this section we’ll look at classification tasks like sentiment analysis; in the next section we’ll introduce neural language modeling.
- korean_translation: 피드포워드 네트워크를 NLP 작업에 어떻게 적용하는지 살펴봅시다! 이 섹션에서는 감성 분석과 같은 분류 작업을 살펴볼 것이며, 다음 섹션에서는 신경망 언어 모델링을 소개할 것입니다.
- study_note: 다룰 구체적인 NLP 작업들(감성 분석 등)을 예고합니다.

# Page 12

## Block 1
- type: other
- source_text: 12 CHAPTER 7 • NEURAL NETWORKS AND NEURAL LANGUAGE MODELS
- korean_translation: 12 제7장 • 신경망과 신경망 언어 모델
- study_note: 페이지 상단 헤더 및 페이지 번호입니다.

## Block 2
- type: paragraph
- source_text: Let’s begin with a simple two-layer sentiment classifier. You might imagine taking our logistic regression classifier of Chapter 5, which corresponds to a 1-layer network, and just adding a hidden layer. The input element x_i could be scalar features like those in Fig. ??, e.g., x_1 = count(words \in doc), x_2 = count(positive lexicon words \in doc), x_3 = 1 if “no” \in doc, and so on. And the output layer y could have two nodes (one each for positive and negative), or 3 nodes (positive, negative, neutral), in which case y_1 would be the estimated probability of positive sentiment, y_2 the probability of negative and y_3 the probability of neutral. The resulting equations would be just what we saw above for a two-layer network (as sketched in Fig. 7.10):
- korean_translation: 단순한 2층 감성 분류기(sentiment classifier)부터 시작해 봅시다. 5장의 로지스틱 회귀 분류기(1층 네트워크에 해당함)에 은닉층 하나를 단순히 추가한다고 상상해 볼 수 있습니다. 입력 요소 $x_i$는 그림 ??에 나온 것과 같은 스칼라 특징들일 수 있습니다. 예를 들어 $x_1$은 문서 내 단어 수, $x_2$는 문서 내 긍정 어휘 수, $x_3$은 문서에 "no"가 포함되면 1인 식입니다. 그리고 출력층 $\mathbf{y}$는 두 개의 노드(긍정과 부정 각각 하나씩)를 갖거나 세 개의 노드(긍정, 부정, 중립)를 가질 수 있습니다. 이 경우 $y_1$은 긍정 감성의 추정 확률, $y_2$는 부정의 확률, $y_3$는 중립의 확률이 될 것입니다. 결과적인 공식들은 (그림 7.10에 묘사된 것과 같은) 2층 네트워크에 대해 위에서 보았던 것과 정확히 일치할 것입니다.
- study_note: 수작업으로 설계된 특징들을 이용한 감성 분류기 구조를 2층 네트워크로 설명합니다.

## Block 3
- type: formula
- source_text:
  \mathbf{x} = \text{vector of hand-designed features} \\
  \mathbf{h} = \sigma(\mathbf{Wx} + \mathbf{b}) \\
  \mathbf{z} = \mathbf{Uh} \\
  \mathbf{y} = \text{softmax}(\mathbf{z}) (7.19)
- korean_translation:
  \mathbf{x} = \text{수작업으로 설계된 특징들의 벡터} \\
  \mathbf{h} = \sigma(\mathbf{Wx} + \mathbf{b}) \\
  \mathbf{z} = \mathbf{Uh} \\
  \mathbf{y} = \text{softmax}(\mathbf{z}) (7.19)
- study_note: 식 (7.19). 수작업 특징 입력 방식의 감성 분류 연산 과정입니다.

## Block 4
- type: paragraph
- source_text: As we mentioned earlier, adding this hidden layer to our logistic regression classifier allows the network to represent the non-linear interactions between features. This alone might give us a better sentiment classifier.
- korean_translation: 앞서 언급했듯이, 로지스틱 회귀 분류기에 이 은닉층을 추가하면 네트워크가 특징들 사이의 비선형적인 상호작용을 표현할 수 있게 됩니다. 이것만으로도 더 나은 성능의 감성 분류기를 얻을 수 있을지 모릅니다.
- study_note: 은닉층 추가를 통한 비선형적 상호작용 포착 능력이 가져다주는 성능 향상 효과를 설명합니다.

## Block 5
- type: paragraph
- source_text: Most neural NLP applications do something different, however. Instead of using hand-built human-engineered features as the input to our classifier, we draw on deep learning’s ability to learn features from the data by representing words as word2vec or GloVe embeddings (Chapter 6). For a text with n input words/tokens w_1, . . . , w_n, the input vector will be the concatenated embeddings of the n words: [e_{w_1}; e_{w_2}; e_{w_3}; \dots ; e_{w_n}]. If we use the semicolon ‘;’ to mean concatenation of vectors, the equation for our sentiment classifier will be (as sketched in Fig. 7.11):
- korean_translation: 하지만 대부분의 신경망 NLP 애플리케이션은 이와 다르게 동작합니다. 사람에 의해 설계된 수작업 특징들을 분류기의 입력으로 사용하는 대신, 6장에서 다룬 word2vec이나 GloVe 임베딩으로 단어를 표현함으로써 데이터로부터 특징을 학습하는 딥러닝의 능력을 활용합니다. $n$개의 입력 단어/토큰 $w_1, \dots, w_n$으로 이루어진 텍스트의 경우, 입력 벡터는 해당 $n$개 단어 임베딩의 연결(concatenation)이 될 것입니다: $[e_{w_1}; e_{w_2}; e_{w_3}; \dots ; e_{w_n}]$. 세미콜론 ';'을 벡터의 연결로 사용한다면, 감성 분류기를 위한 공식은 다음과 같을 것입니다(그림 7.11에 묘사된 형태).
- study_note: 수작업 특징 대신 단어 임베딩 벡터들을 연결(concatenation)하여 입력을 구성하는 현대적 방식을 소개합니다.

## Block 6
- type: formula
- source_text:
  \mathbf{x} = [e_{w_1}; e_{w_2}; e_{w_3}; \dots ; e_{w_n}] \\
  \mathbf{h} = \sigma(\mathbf{Wx} + \mathbf{b}) \\
  \mathbf{z} = \mathbf{Uh} \\
  \mathbf{y} = \text{softmax}(\mathbf{z}) (7.20)
- korean_translation:
  \mathbf{x} = [e_{w_1}; e_{w_2}; e_{w_3}; \dots ; e_{w_n}] \\
  \mathbf{h} = \sigma(\mathbf{Wx} + \mathbf{b}) \\
  \mathbf{z} = \mathbf{Uh} \\
  \mathbf{y} = \text{softmax}(\mathbf{z}) (7.20)
- study_note: 식 (7.2). 임베딩 입력을 사용하는 감성 분류 연산 과정입니다.

## Block 7
- type: paragraph
- source_text: The idea of using word2vec or GloVe embeddings as our input representation — and more generally the idea of relying on another algorithm to have already learned an embedding representation for our input words — is called pretraining. Using pretrained embedding representations, whether simple static word embeddings like word2vec or the more powerful contextual embeddings we’ll introduce in Chapter 11, is one of the central ideas of deep learning. (It’s also, however, possible to train the word embeddings as part of an NLP task; we’ll talk about how to do this Section 7.7 in the context of the neural language modeling task.)
- korean_translation: word2vec이나 GloVe 임베딩을 입력 표현으로 사용하는 아이디어, 즉 더 일반적으로는 우리 입력 단어들에 대해 다른 알고리즘이 이미 임베딩 표현을 학습해 두었을 것이라고 믿고 이에 의존하는 아이디어를 '사전 학습(pretraining)'이라고 부릅니다. word2vec 같은 단순한 정적 단어 임베딩이든 11장에서 소개할 더 강력한 문맥적 임베딩이든, 사전 학습된 임베딩 표현을 활용하는 것은 딥러닝의 핵심적인 아이디어 중 하나입니다. (하지만 NLP 작업의 일부로서 단어 임베딩 자체를 학습시키는 것도 가능합니다. 7.7절에서 신경망 언어 모델링 작업과 관련하여 이를 어떻게 수행하는지 다룰 예정입니다.)
- study_note: 사전 학습(pretraining)의 개념과 그 중요성, 그리고 전이 학습적 측면을 설명합니다.

## Block 8
- type: margin_note
- source_text: pretraining
- korean_translation: 사전 학습(pretraining)
- study_note: 대량의 데이터로 미리 학습된 파라미터(임베딩 등)를 가져와 사용함으로써 학습 경험을 전이하는 기술입니다.

## Block 9
- type: section_title
- source_text: 7.5 Feedforward Neural Language Modeling
- korean_translation: 7.5 피드포워드 신경망 언어 모델링
- study_note: 피드포워드 네트워크를 언어 모델링(다음 단어 예측) 작업에 적용하는 구체적인 모델을 소개합니다.

## Block 10
- type: paragraph
- source_text: As our second application of feedforward networks, let’s consider language modeling: predicting upcoming words from prior word context. Neural language modeling is an important NLP task in itself, and it plays a role in many important algorithms for tasks like machine translation, summarization, speech recognition, grammar correction, and dialogue. We’ll describe simple feedforward neural language models, first introduced by Bengio et al. (2003). While modern neural language models use
- korean_translation: 피드포워드 네트워크의 두 번째 적용 사례로 언어 모델링을 생각해 봅시다. 이는 이전 단어 문맥으로부터 다음에 올 단어를 예측하는 것입니다. 신경망 언어 모델링(Neural language modeling)은 그 자체로 중요한 NLP 작업이며, 기계 번역, 요약, 음성 인식, 문법 교정, 대화문 생성 등 많은 주요 알고리즘에서 핵심적인 역할을 합니다. 우리는 Bengio 등(2003)에 의해 처음 소개된 간단한 피드포워드 신경망 언어 모델을 설명할 것입니다. 비록 현대적인 신경망 언어 모델은 [다음 페이지에서 계속]
- study_note: 신경망 언어 모델링의 정의와 중요성, 그리고 선구적인 연구인 Bengio 등(2003)의 모델을 언급합니다.
