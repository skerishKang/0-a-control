# Page 11

## Block 1
- type: other
- source_text: 4. • CTC 11
- korean_translation: 4. • CTC 11
- study_note: 페이지 상단 헤더입니다.

## Block 2
- type: subsection_title
- source_text: 26.4.1 CTC Inference
- korean_translation: 26.4.1 CTC 추론
- study_note: CTC 모델을 이용한 추론(Inference) 과정을 다루는 섹션입니다.

## Block 3
- type: paragraph
- source_text: Before we see how to compute P(Y|X) let’s first see how CTC assigns a probability to one particular alignment A = {a1, ..., an}. CTC makes a strong conditional independence assumption: it assumes that, given the input X, the CTC model output at at time t is independent of the output labels at any other time ai. Thus:
- korean_translation: P(Y|X)를 계산하는 방법을 알아보기 전에, 먼저 CTC가 하나의 특정 정렬(alignment) A = {a1, ..., an}에 확률을 어떻게 할당하는지 살펴봅시다. CTC는 강력한 조건부 독립성 가정을 합니다. 즉, 입력 X가 주어졌을 때 시간 t에서의 CTC 모델 출력은 다른 시간 ai에서의 출력 레이블과 독립적이라고 가정합니다. 따라서:
- study_note: CTC의 핵심 가정인 각 타임스텝의 출력 독립성에 대해 설명합니다.

## Block 4
- type: formula
- source_text: P_CTC(A|X) = prod_{t=1}^T p(at|X) (26.13)
- korean_translation: P_CTC(A|X) = prod_{t=1}^T p(at|X) (26.13)
- study_note: 특정 정렬 A에 대한 확률을 각 타임스텝의 확률의 곱으로 나타내는 공식입니다.

## Block 5
- type: paragraph
- source_text: Thus to find the best alignment A = {a1, ..., aT} we can greedily choose the character with the max probability at each time step t:
- korean_translation: 따라서 최적의 정렬 A = {a1, ..., aT}를 찾기 위해 각 타임스텝 t에서 확률이 최대인 문자를 탐욕적으로(greedily) 선택할 수 있습니다.
- study_note: 가장 단순한 추론 방식인 그리디 탐색(Greedy Search)을 설명합니다.

## Block 6
- type: formula
- source_text: a_hat_t = argmax_{c \in C} p_t(c|X) (26.14)
- korean_translation: a_hat_t = argmax_{c \in C} p_t(c|X) (26.14)
- study_note: 각 시점 t에서 가장 높은 확률을 가진 문자를 선택하는 수식입니다.

## Block 7
- type: paragraph
- source_text: We then pass the resulting sequence A to the CTC collapsing function B to get the output sequence Y.
- korean_translation: 그런 다음 결과 시퀀스 A를 CTC 축소 함수 B에 통과시켜 출력 시퀀스 Y를 얻습니다.
- study_note: 정렬된 시퀀스에서 중복을 제거하고 빈 토큰을 없애 최종 문장을 만드는 과정을 언급합니다.

## Block 8
- type: paragraph
- source_text: Let’s talk about how this simple inference algorithm for finding the best alignment A would be implemented. Because we are making a decision at each time point, we can treat CTC as a sequence-modeling task, where we output one letter y_hat_t at time t corresponding to each input token xt, eliminating the need for a full decoder. Fig. 26.10 sketches this architecture, where we take an encoder, produce a hidden state ht at each timestep, and decode by taking a softmax over the character vocabulary at each time step.
- korean_translation: 최적의 정렬 A를 찾기 위한 이 단순한 추론 알고리즘이 어떻게 구현되는지 이야기해 봅시다. 각 시점마다 결정을 내리기 때문에, CTC를 각 입력 토큰 xt에 대응하는 시간 t에서 하나의 문자 y_hat_t를 출력하는 시퀀스 모델링 작업으로 취급할 수 있으며, 이로 인해 전체 디코더가 필요하지 않게 됩니다. 그림 26.10은 인코더를 취해 각 타임스텝에서 은닉 상태 ht를 생성하고, 각 타임스텝에서 문자 어휘에 대해 소프트맥스를 취하여 디코딩하는 이 아키텍처의 개요를 보여줍니다.
- study_note: CTC 추론이 별도의 복잡한 디코더 없이 인코더의 출력만으로도 가능함을 설명합니다.

## Block 9
- type: figure_caption
- source_text: Figure 26.10 ... simple softmaxes over the hidden state ht at each output step.
- korean_translation: 그림 26.10 ... 각 출력 단계에서 은닉 상태 ht에 대해 단순한 소프트맥스를 적용함.
- study_note: CTC 아키텍처의 단순화된 출력 과정을 설명하는 그림의 캡션입니다.

## Block 10
- type: paragraph
- source_text: Alas, there is a potential flaw with the inference algorithm sketched in (Eq. 26.14) and Fig. 26.9. The problem is that we chose the most likely alignment A, but the most likely alignment may not correspond to the most likely final collapsed output string Y. That’s because there are many possible alignments that lead to the same output string, and hence the most likely output string might correspond to the most probable alignment. For example, imagine the most probable alignment A for an input X=[x1, x2, x3] is the string [a, b, epsilon] but the next two most probable alignments are [b, epsilon, b] and [epsilon, b, b]. The output Y= [b, b], summing over those two alignments, might be more probable than Y= [a, b].
- korean_translation: 안타깝게도 (식 26.14)와 그림 26.9에 묘사된 추론 알고리즘에는 잠재적인 결함이 있습니다. 문제는 우리가 가장 가능성 높은 '정렬 A'를 선택했지만, 가장 가능성 높은 정렬이 반드시 가장 가능성 높은 최종 축소 출력 문자열 Y에 대응하지는 않을 수 있다는 점입니다. 이는 동일한 출력 문자열로 이어지는 가능한 정렬이 많기 때문이며, 따라서 가장 가능성 높은 출력 문자열은 여러 정렬의 확률 합에 의해 결정되어야 합니다. 예를 들어, 입력 X=[x1, x2, x3]에 대해 가장 확률이 높은 정렬 A가 [a, b, ]이지만, 그다음으로 확률이 높은 두 정렬이 [b, , b]와 [, b, b]라고 상상해 보십시오. 이 두 정렬을 합친 출력 Y=[b, b]가 Y=[a, b]보다 더 확률이 높을 수 있습니다.
- study_note: 단일 최적 정렬을 찾는 것보다 여러 정렬의 확률을 합산하여 최종 문자열을 결정하는 것이 더 정확함을 강조합니다.

## Block 11
- type: paragraph
- source_text: For this reason, the most probable output sequence Y is the one that has, not the single best CTC alignment, but the highest sum over the probability of all its possible alignments:
- korean_translation: 이러한 이유로, 가장 확률이 높은 출력 시퀀스 Y는 단일 최적 CTC 정렬을 가진 것이 아니라, 가능한 모든 정렬의 확률 합이 가장 높은 것입니다.
- study_note: 최종 출력 결정 방식이 개별 정렬의 최대 확률이 아닌, 정렬들의 확률 합(marginal probability)에 기반해야 함을 설명합니다.

# Page 12

## Block 1
- type: other
- source_text: 12 CHAPTER 26 • AUTOMATIC SPEECH RECOGNITION AND TEXT-TO-SPEECH
- korean_translation: 12 제26장 • 자동 음성 인식 및 텍스트 음성 변환
- study_note: 페이지 상단 헤더입니다.

## Block 2
- type: formula
- source_text: P_CTC(Y|X) = sum_{A \in B^{-1}(Y)} P(A|X) = sum_{A \in B^{-1}(Y)} prod_{t=1}^T p(at|ht) (26.15)
- korean_translation: P_CTC(Y|X) = sum_{A \in B^{-1}(Y)} P(A|X) = sum_{A \in B^{-1}(Y)} prod_{t=1}^T p(at|ht) (26.15)
- study_note: 특정 출력 Y에 대해 가능한 모든 정렬 A의 확률을 합산하여 최종 확률을 구하는 공식입니다.

## Block 3
- type: formula
- source_text: Y_hat = argmax_Y P_CTC(Y|X)
- korean_translation: Y_hat = argmax_Y P_CTC(Y|X)
- study_note: 합산된 확률이 최대가 되는 최적의 시퀀스 Y를 찾는 식입니다.

## Block 4
- type: paragraph
- source_text: Alas, summing over all alignments is very expensive (there are a lot of alignments), so we approximate this sum by using a version of Viterbi beam search that cleverly keeps in the beam the high-probability alignments that map to the same output string, and sums those as an approximation of (Eq. 26.15). See Hannun (2017) for a clear explanation of this extension of beam search for CTC.
- korean_translation: 안타깝게도 모든 정렬에 대해 합산하는 것은 매우 비용이 많이 듭니다(정렬의 수가 매우 많기 때문입니다). 따라서 우리는 동일한 출력 문자열로 매핑되는 고확률 정렬들을 빔 안에 영리하게 유지하고 이들을 합산하는 방식의 비터비 빔 서치(Viterbi beam search) 버전을 사용하여 (식 26.15)를 근사합니다. CTC를 위한 이 빔 서치 확장에 대한 명확한 설명은 Hannun(2017)을 참조하십시오.
- study_note: 모든 경우의 수를 계산하기 힘든 정렬 합산 문제를 해결하기 위해 빔 서치를 통한 근사 방식을 사용함을 설명합니다.

## Block 5
- type: paragraph
- source_text: Because of the strong conditional independence assumption mentioned earlier (that the output at time t is independent of the output at time t-1, given the input), CTC does not implicitly learn a language model over the data (unlike the attention-based encoder-decoder architectures). It is therefore essential when using CTC to interpolate a language model (and some sort of length factor L(Y)) using interpolation weights that are trained on a dev set:
- korean_translation: 앞서 언급한 강력한 조건부 독립성 가정(입력이 주어졌을 때 시간 t의 출력이 t-1의 출력과 독립적이라는 가정) 때문에, CTC는 (어텐션 기반 인코더-디코더 아키텍처와 달리) 데이터에 대한 언어 모델을 암시적으로 학습하지 않습니다. 따라서 CTC를 사용할 때는 개발 세트(dev set)에서 학습된 보간 가중치를 사용하여 언어 모델(및 일종의 길이 인자 L(Y))을 보간하는 것이 필수적입니다.
- study_note: CTC 모델이 자체적으로 언어의 문맥을 학습하지 못하기 때문에 외부 언어 모델과의 결합이 필수적임을 강조합니다.

## Block 6
- type: formula
- source_text: score_CTC(Y|X) = log P_CTC(Y|X) + lambda_1 log P_LM(Y) + lambda_2 L(Y) (26.16)
- korean_translation: score_CTC(Y|X) = log P_CTC(Y|X) + lambda_1 log P_LM(Y) + lambda_2 L(Y) (26.16)
- study_note: CTC 확률, 외부 언어 모델 확률, 그리고 길이 보정 항을 결합한 최종 점수 산출 공식입니다.

## Block 7
- type: subsection_title
- source_text: 26.4.2 CTC Training
- korean_translation: 26.4.2 CTC 학습
- study_note: CTC 모델의 파라미터를 최적화하는 학습 과정에 대해 다룹니다.

## Block 8
- type: paragraph
- source_text: To train a CTC-based ASR system, we use negative log-likelihood loss with a special CTC loss function. Thus the loss for an entire dataset D is the sum of the negative log-likelihoods of the correct output Y for each input X:
- korean_translation: CTC 기반 ASR 시스템을 학습시키기 위해 특별한 CTC 손실 함수와 함께 음의 로그 우도(negative log-likelihood) 손실을 사용합니다. 따라서 전체 데이터셋 D에 대한 손실은 각 입력 X에 대한 정답 출력 Y의 음의 로그 우도의 합입니다.
- study_note: CTC 학습의 목적 함수로 사용되는 음의 로그 우도 손실의 개념을 설명합니다.

## Block 9
- type: formula
- source_text: L_CTC = -sum_{(X,Y) \in D} log P_CTC(Y|X) (26.17)
- korean_translation: L_CTC = -sum_{(X,Y) \in D} log P_CTC(Y|X) (26.17)
- study_note: 데이터셋 전체에 대한 CTC 손실 공식입니다.

## Block 10
- type: paragraph
- source_text: To compute CTC loss function for a single input pair (X, Y), we need the probability of the output Y given the input X. As we saw in Eq. 26.15, to compute the probability of a given output Y we need to sum over all the possible alignments that would collapse to Y. In other words:
- korean_translation: 단일 입력 쌍(X, Y)에 대한 CTC 손실 함수를 계산하려면 입력 X가 주어졌을 때 출력 Y의 확률이 필요합니다. 식 26.15에서 보았듯이, 주어진 출력 Y의 확률을 계산하려면 Y로 축소될 수 있는 가능한 모든 정렬에 대해 합산해야 합니다. 즉:
- study_note: 정답 문장에 대응하는 모든 가능한 정렬들의 확률을 합쳐서 손실을 구하는 원리를 다시 설명합니다.

## Block 11
- type: formula
- source_text: P_CTC(Y|X) = sum_{A \in B^{-1}(Y)} prod_{t=1}^T p(at|ht) (26.18)
- korean_translation: P_CTC(Y|X) = sum_{A \in B^{-1}(Y)} prod_{t=1}^T p(at|ht) (26.18)
- study_note: 출력 Y의 확률을 구하기 위한 정렬 확률들의 합산 공식입니다.

## Block 12
- type: paragraph
- source_text: Naively summing over all possible alignments in not feasible (there are too many alignments). However, we can efficiently compute the sum by using dynamic programming to merge alignments, with a version of the forward-backward algorithm also used to train HMMs (Appendix A) and CRFs. The original dynamic programming algorithms for both training and inference are laid out in (Graves et al., 2006); see (Hannun, 2017) for a detailed explanation of both.
- korean_translation: 가능한 모든 정렬에 대해 단순하게 합산하는 것은 불가능합니다(정렬이 너무 많기 때문입니다). 그러나 HMM(부록 A) 및 CRF 학습에도 사용되는 전방-후방 알고리즘(forward-backward algorithm) 버전을 사용하여 정렬을 병합하고 동적 계획법(dynamic programming)으로 합계를 효율적으로 계산할 수 있습니다. 학습과 추론을 위한 독창적인 동적 계획법 알고리즘은 (Graves et al., 2006)에 제시되어 있으며, 두 가지 모두에 대한 자세한 설명은 (Hannun, 2017)을 참조하십시오.
- study_note: 복잡한 합산 문제를 전방-후방 알고리즘을 이용한 동적 계획법으로 효율적으로 해결할 수 있음을 언급합니다.
