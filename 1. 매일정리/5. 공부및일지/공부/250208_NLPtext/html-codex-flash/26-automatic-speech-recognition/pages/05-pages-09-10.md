# Page 9

## Block 1
- type: other
- source_text: 26.3 • SPEECH RECOGNITION ARCHITECTURE 9
- korean_translation: 26.3 • 음성 인식 아키텍처 9
- study_note: 페이지 상단 헤더입니다.

## Block 2
- type: figure_caption
- source_text: Figure 26.6 Schematic architecture for an encoder-decoder speech recognizer.
- korean_translation: 그림 26.6 인코더-디코더 음성 인식기의 개략적인 아키텍처.
- study_note: 특징 추출, 서브샘플링, 인코더, 디코더로 이어지는 음성 인식 모델의 전체 구조를 보여주는 다이어그램입니다.

## Block 3
- type: paragraph
- source_text: function that is designed to deal well with compression, like the CTC loss function we’ll introduce in the next section.)
- korean_translation: 다음 섹션에서 소개할 CTC 손실 함수와 같이 압축을 잘 다루도록 설계된 함수입니다.)
- study_note: 이전 페이지에서 이어지는 문장의 마무리로, CTC 손실 함수의 역할을 언급합니다.

## Block 4
- type: paragraph
- source_text: The goal of the subsampling is to produce a shorter sequence X = x1, ..., xn that will be the input to the encoder. The simplest algorithm is a method sometimes called low frame rate (Pundak and Sainath, 2016): for time i we stack (concatenate) the acoustic feature vector fi with the prior two vectors fi-1 and fi-2 to make a new vector three times longer. Then we simply delete fi-1 and fi-2. Thus instead of (say) a 40-dimensional acoustic feature vector every 10 ms, we have a longer vector (say 120-dimensional) every 30 ms, with a shorter sequence length n = t/3.
- korean_translation: 서브샘플링(subsampling)의 목표는 인코더의 입력이 될 더 짧은 시퀀스 X = x1, ..., xn을 만드는 것입니다. 가장 간단한 알고리즘은 때때로 낮은 프레임 레이트(low frame rate)라고 불리는 방법입니다(Pundak and Sainath, 2016). 시간 i에 대해 음향 특징 벡터 fi를 이전의 두 벡터 fi-1, fi-2와 쌓아서(결합하여) 세 배 더 긴 새로운 벡터를 만듭니다. 그런 다음 fi-1과 fi-2를 단순히 삭제합니다. 이렇게 하면 예를 들어 10ms마다 40차원 음향 특징 벡터가 있는 대신, 30ms마다 120차원의 더 긴 벡터가 있게 되어 시퀀스 길이가 n = t/3로 짧아집니다.
- study_note: 음성 신호의 긴 프레임 시퀀스를 줄여 인코더의 부담을 덜어주는 서브샘플링 기법 중 하나인 '낮은 프레임 레이트' 방식에 대해 설명합니다.

## Block 5
- type: margin_note
- source_text: low frame rate
- korean_translation: 낮은 프레임 레이트
- study_note: 프레임들을 결합하고 일부를 제거하여 데이터의 길이를 줄이는 방식입니다.

## Block 6
- type: paragraph
- source_text: After this compression stage, encoder-decoders for speech use the same architecture as for MT or other text, composed of either RNNs (LSTMs) or Transformers. For inference, the probability of the output string Y is decomposed as:
- korean_translation: 이 압축 단계 이후, 음성을 위한 인코더-디코더는 RNN(LSTM) 또는 트랜스포머로 구성된 기계 번역(MT)이나 다른 텍스트 처리를 위한 것과 동일한 아키텍처를 사용합니다. 추론(inference) 시 출력 문자열 Y의 확률은 다음과 같이 분해됩니다:
- study_note: 압축 이후의 모델 구조는 일반적인 NLP 모델과 동일하며, 확률 모델링 방식도 유사함을 설명합니다.

## Block 7
- type: formula
- source_text: p(y1, ..., yn) = prod_{i=1}^n p(yi | y1, ..., yi-1, X) (26.7)
- korean_translation: p(y1, ..., yn) = prod_{i=1}^n p(yi | y1, ..., yi-1, X) (26.7)
- study_note: 이전 출력들과 입력 X가 주어졌을 때 다음 문자가 나올 확률들의 곱으로 전체 시퀀스의 확률을 나타내는 연쇄 법칙(chain rule) 적용 식입니다.

## Block 8
- type: paragraph
- source_text: We can produce each letter of the output via greedy decoding:
- korean_translation: 우리는 그리디 디코딩(greedy decoding)을 통해 출력의 각 문자를 생성할 수 있습니다:
- study_note: 매 단계에서 가장 확률이 높은 문자를 선택하는 가장 단순한 디코딩 방식입니다.

## Block 9
- type: formula
- source_text: y_hat_i = argmax_{char \in Alphabet} p(char | y1, ..., yi-1, X) (26.8)
- korean_translation: y_hat_i = argmax_{char \in Alphabet} p(char | y1, ..., yi-1, X) (26.8)
- study_note: 그리디 디코딩의 수학적 표현입니다.

## Block 10
- type: paragraph
- source_text: Alternatively we can use beam search as described in the next section. this is particularly relevant when we are adding a language model.
- korean_translation: 대안으로 다음 섹션에서 설명할 빔 서치(beam search)를 사용할 수 있습니다. 이는 언어 모델을 추가할 때 특히 중요합니다.
- study_note: 더 정교한 탐색 방식인 빔 서치의 필요성을 언급합니다.

## Block 11
- type: section_title
- source_text: Adding a language model
- korean_translation: 언어 모델 추가하기
- study_note: 음성 인식 모델에 별도의 언어 모델을 결합하여 성능을 높이는 방법을 다룹니다.

## Block 12
- type: paragraph
- source_text: Since an encoder-decoder model is essentially a conditional language model, encoder-decoders implicitly learn a language model for the output domain of letters from their training data. However, the training data (speech paired with text transcriptions) may not include sufficient text to train a good language model. After all, it’s easier to find enormous amounts of pure text training data than it is to find text paired with speech. Thus we can usually improve a model at least slightly by incorporating a very large language model.
- korean_translation: 인코더-디코더 모델은 본질적으로 조건부 언어 모델이므로, 학습 데이터를 통해 출력 영역인 문자에 대한 언어 모델을 암시적으로 학습합니다. 그러나 학습 데이터(텍스트 전사문과 쌍을 이룬 음성)에는 좋은 언어 모델을 학습시키기에 충분한 텍스트가 포함되어 있지 않을 수 있습니다. 결국, 음성과 쌍을 이룬 텍스트보다 방대한 양의 순수 텍스트 학습 데이터를 찾는 것이 훨씬 쉽습니다. 따라서 매우 큰 규모의 언어 모델을 통합함으로써 보통 모델의 성능을 조금이라도 향상시킬 수 있습니다.
- study_note: 음성 인식 모델이 스스로 언어 구조를 학습하긴 하지만, 음성-텍스트 쌍 데이터보다 텍스트 단독 데이터가 훨씬 구하기 쉽기 때문에 외부 언어 모델을 결합하는 것이 유리하다는 점을 설명합니다.

## Block 13
- type: paragraph
- source_text: The simplest way to do this is to use beam search to get a final beam of hypothesized sentences; this beam is sometimes called an n-best list. We then use a language model to rescore each hypothesis on the beam. The scoring is doing by interpolating the score assigned by the language model with the encoder-decoder score
- korean_translation: 이를 위한 가장 간단한 방법은 빔 서치를 사용하여 최종 가설 문장들의 빔(beam)을 얻는 것입니다. 이 빔은 때때로 n-best 리스트라고 불립니다. 그런 다음 언어 모델을 사용하여 빔에 있는 각 가설의 점수를 다시 매깁니다(rescore). 점수 계산은 언어 모델이 부여한 점수와 인코더-디코더 점수를 보간(interpolate)하여 이루어집니다. [다음 페이지에서 계속]
- study_note: 빔 서치로 얻은 여러 후보(n-best list)에 대해 외부 언어 모델로 다시 점수를 매겨 최적의 결과를 선택하는 리스코어링(rescoring) 과정을 설명합니다.

## Block 14
- type: margin_note
- source_text:
  n-best list
  rescore
- korean_translation:
  n-best 리스트
  리스코어 (다시 점수 매기기)
- study_note: 여러 후보군을 유지하고 사후에 점수를 조정하는 과정을 나타내는 용어입니다.

## Block 15
- type: other
- source_text: 1 There are also more complex alternatives for subsampling, like using a convolutional net that downsamples with max pooling, or layers of pyramidal RNNs, RNNs where each successive layer has half the number of RNNs as the previous layer.
- korean_translation: 1 맥스 풀링을 사용하여 다운샘플링하는 컨볼루션 네트워크를 사용하거나, 각 계층이 이전 계층의 절반 수의 RNN을 갖는 피라미드형 RNN(pyramidal RNNs) 계층을 사용하는 등 서브샘플링을 위한 더 복잡한 대안들도 있습니다.
- study_note: 서브샘플링을 위한 더 고도화된 기술적 대안들을 소개하는 각주입니다.

# Page 10

## Block 1
- type: other
- source_text: 10 CHAPTER 26 • AUTOMATIC SPEECH RECOGNITION AND TEXT-TO-SPEECH
- korean_translation: 10 제26장 • 자동 음성 인식 및 텍스트 음성 변환
- study_note: 페이지 상단 헤더입니다.

## Block 2
- type: paragraph
- source_text: used to create the beam, with a weight lambda tuned on a held-out set. Also, since most models prefer shorter sentences, ASR systems normally have some way of adding a length factor. One way to do this is to normalize the probability by the number of characters in the hypothesis |Y|_c. The following is thus a typical scoring function (Chan et al., 2016):
- korean_translation: 빔을 생성하는 데 사용되며, 가중치 lambda는 별도의 검증 세트(held-out set)에서 튜닝됩니다. 또한, 대부분의 모델이 짧은 문장을 선호하기 때문에, ASR 시스템은 보통 길이 인자(length factor)를 추가하는 방법을 사용합니다. 한 가지 방법은 가설의 문자 수 |Y|_c로 확률을 정규화하는 것입니다. 따라서 전형적인 점수 계산 함수는 다음과 같습니다(Chan et al., 2016):
- study_note: 외부 언어 모델 가중치(lambda)와 문장 길이에 따른 편향을 보정하는 길이 정규화(length normalization) 기법을 설명합니다.

## Block 3
- type: formula
- source_text: score(Y|X) = 1/|Y|_c log P(Y|X) + lambda log P_LM(Y) (26.9)
- korean_translation: score(Y|X) = 1/|Y|_c log P(Y|X) + lambda log P_LM(Y) (26.9)
- study_note: 인코더-디코더의 로그 확률(길이로 정규화됨)과 언어 모델의 로그 확률을 결합한 최종 점수 공식입니다.

## Block 4
- type: subsection_title
- source_text: 26.3.1 Learning
- korean_translation: 26.3.1 학습
- study_note: 모델을 학습시키는 손실 함수와 기법을 다룹니다.

## Block 5
- type: paragraph
- source_text: Encoder-decoders for speech are trained with the normal cross-entropy loss generally used for conditional language models. At timestep i of decoding, the loss is the log probability of the correct token (letter) yi:
- korean_translation: 음성을 위한 인코더-디코더는 일반적으로 조건부 언어 모델에 사용되는 일반적인 크로스 엔트로피(cross-entropy) 손실로 학습됩니다. 디코딩의 타임스텝 i에서 손실은 정답 토큰(문자) yi의 로그 확률입니다:
- study_note: 모델 학습의 기본이 되는 크로스 엔트로피 손실 함수의 개념을 설명합니다.

## Block 6
- type: formula
- source_text: L_CE = -log p(yi | y1, ..., yi-1, X) (26.10)
- korean_translation: L_CE = -log p(yi | y1, ..., yi-1, X) (26.10)
- study_note: 각 타임스텝에서의 개별 손실 식입니다.

## Block 7
- type: paragraph
- source_text: The loss for the entire sentence is the sum of these losses:
- korean_translation: 전체 문장에 대한 손실은 이러한 손실들의 합입니다:
- study_note: 시퀀스 전체의 손실을 구하는 방법입니다.

## Block 8
- type: formula
- source_text: L_CE = - sum_{i=1}^m log p(yi | y1, ..., yi-1, X) (26.11)
- korean_translation: L_CE = - sum_{i=1}^m log p(yi | y1, ..., yi-1, X) (26.11)
- study_note: 시퀀스 내 모든 문자의 손실을 합산한 전체 손실 식입니다.

## Block 9
- type: paragraph
- source_text: This loss is then backpropagated through the entire end-to-end model to train the entire encoder-decoder.
- korean_translation: 이 손실은 전체 엔드투엔드(end-to-end) 모델을 통해 역전파(backpropagated)되어 전체 인코더-디코더를 학습시킵니다.
- study_note: 역전파를 통한 모델 파라미터 업데이트 과정을 언급합니다.

## Block 10
- type: paragraph
- source_text: As we described in Chapter 10, we normally use teacher forcing, in which the decoder history is forced to be the correct gold yi rather than the predicted y_hat_i. It’s also possible to use a mixture of the gold and decoder output, for example using the gold output 90% of the time, but with probability .1 taking the decoder output instead:
- korean_translation: 10장에서 설명했듯이, 우리는 보통 디코더의 이력이 예측된 y_hat_i가 아니라 실제 정답 yi가 되도록 강제하는 티처 포싱(teacher forcing)을 사용합니다. 정답 출력과 디코더 출력을 혼합해서 사용하는 것도 가능한데, 예를 들어 90%의 시간은 정답 출력을 사용하지만 0.1의 확률로는 디코더 출력을 대신 취하는 식입니다:
- study_note: 학습 시 이전 단계의 정답을 다음 단계의 입력으로 넣어주는 티처 포싱 기법과 그 변형(scheduled sampling 등)을 설명합니다.

## Block 11
- type: formula
- source_text: L_CE = -log p(yi | y1, ..., y_hat_{i-1}, X) (26.12)
- korean_translation: L_CE = -log p(yi | y1, ..., y_hat_{i-1}, X) (26.12)
- study_note: 예측값을 입력으로 사용할 때의 손실 식 예시입니다.

## Block 12
- type: section_title
- source_text: 26.4 CTC
- korean_translation: 26.4 CTC (Connectionist Temporal Classification)
- study_note: 인코더-디코더의 대안이자 중요한 알고리즘인 CTC를 소개합니다.

## Block 13
- type: paragraph
- source_text: We pointed out in the previous section that speech recognition has two particular properties that make it very appropriate for the encoder-decoder architecture, where the encoder produces an encoding of the input that the decoder uses attention to explore. First, in speech we have a very long acoustic input sequence X mapping to a much shorter sequence of letters Y, and second, it’s hard to know exactly which part of X maps to which part of Y.
- korean_translation: 우리는 이전 섹션에서 음성 인식이 인코더-디코더 아키텍처(인코더가 입력의 인코딩을 생성하고 디코더가 어텐션을 사용하여 이를 탐색하는 방식)에 매우 적합하게 만드는 두 가지 특별한 속성을 가지고 있다고 지적했습니다. 첫째, 음성에서는 매우 긴 음향 입력 시퀀스 X가 훨씬 짧은 문자 시퀀스 Y로 매핑됩니다. 둘째, X의 어떤 부분이 Y의 어떤 부분에 매핑되는지 정확히 알기 어렵습니다.
- study_note: 음성 인식의 특징인 입력-출력 간의 길이 차이와 정렬(alignment)의 모호성을 다시 강조합니다.

## Block 14
- type: paragraph
- source_text: In this section we briefly introduce an alternative to encoder-decoder: an algorithm and loss function called CTC, short for Connectionist Temporal Classification (Graves et al., 2006), that deals with these problems in a very different way. The intuition of CTC is to output a single character for every frame of the input, so that the output is the same length as the input, and then to apply a collapsing function that combines sequences of identical letters, resulting in a shorter sequence.
- korean_translation: 이 섹션에서는 인코더-디코더의 대안으로, 이러한 문제들을 매우 다른 방식으로 다루는 CTC(Connectionist Temporal Classification의 약자, Graves et al., 2006)라는 알고리즘과 손실 함수를 간략히 소개합니다. CTC의 직관은 입력의 모든 프레임에 대해 하나의 문자를 출력하여 출력이 입력과 동일한 길이가 되게 한 다음, 동일한 문자의 시퀀스를 결합하는 축소 함수(collapsing function)를 적용하여 더 짧은 시퀀스를 만드는 것입니다.
- study_note: 모든 프레임에 대해 출력을 내고 나중에 중복을 합치는 CTC의 독특한 방식을 설명합니다.

## Block 15
- type: margin_note
- source_text: CTC
- korean_translation: CTC
- study_note: Connectionist Temporal Classification의 약자로, 음성 인식 및 OCR에서 널리 쓰이는 정렬 기반 학습 방식입니다.
